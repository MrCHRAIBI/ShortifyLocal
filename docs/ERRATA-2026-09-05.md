# ERRATA — Décisions propriétaire du 2026-09-05

Ce fichier prime sur `docs/01` à `docs/04` en cas de conflit. Le stack cible reste Kotlin Android Natif (Clean Architecture + MVVM, Jetpack Compose, Room) tel que porté dans `.planning/`.

## E1 — Licences (prime sur 01 §1.2)

Liste blanche : MIT / Apache 2.0 / BSD / LGPL + GPL-3.0 accepté uniquement pour NewPipeExtractor et ffmpeg-kit-full-gpl.

## E2 — Versions & coordonnées (prime sur 01 §2)

Kotlin 2.4.10 ; AGP 9.4.0 ; Gradle 9.7.1 ; JDK 17 ; minSdk 33 ; compile/targetSdk 36 ; Compose BOM 2026.08.00 ; Hilt 2.60.1 ; Room 2.8.4 + KSP2 2.3.11 ; Navigation 2.10.0 ; WorkManager 2.11.2 ; security-crypto 1.1.0 stable ; play-services-ads 25.4.0 ; UMP 4.0.0 ; ML Kit face-detection 16.1.7 ; Media3 1.11.0 ; OkHttp 5.5.0 ; Coil 3.6.2 ; DataStore 1.2.1 ; NewPipeExtractor v0.26.5 (tag exact JitPack) ; ffmpeg-kit = `dev.ffmpegkit-maintained:ffmpeg-kit-full-gpl:8.1.7` (remplace `com.arthenica:6.0-2` retiré) ; whisper.cpp épinglé b4938 (source CMake/NDK, flags linker `-Wl,-z,max-page-size=16384 -Wl,-z,common-page-size=16384`).

## E3 — Amendements techniques (complètent 01/03/04)

- **Fonction E** : le filtre `subtitles=` doit inclure `fontsdir=<dossier polices bundlées>` + `shaping=complex` + `force_style` (sinon sous-titres invisibles sur Android ; arabe non shapé sans HarfBuzz).
- **Fonction F** : loudnorm en 2 passes (`measured_*` + `linear=true`) suivi de `-ar 48000` (sinon AAC 192 kHz) ; crop `w`/`x` arrondis à l'entier pair (`& ~1`) ; Range Requests HTTP = optimisation AVEC repli automatique en téléchargement complet (403/416/URL expirée) + seek par décodage `-ss/-t`, jamais collage d'octets.
- **Annulation (§3.3)** : `FFmpegKit.cancel(sessionId)` via `invokeOnCancellation` ; abort callback whisper vérifié entre les chunks.
- **Monétisation** : solde + hash écrits en UN SEUL blob atomique `valeur|hex(hmac)` ; catch `AEADBadTagException`/`GeneralSecurityException` à l'ouverture → reconstruction contrôlée + parcours `SECURE_COMPROMISED` ; récompense créditée UNIQUEMENT dans `onUserEarnedReward` ; rechargement de l'ad dans `onAdDismissedFullScreenContent` ; séquence UMP : `requestConsentInfoUpdate` → `loadAndShowConsentFormIfRequired` → `canRequestAds()` (chemin succès ET catch) → `MobileAds.initialize()` ; timeout pub = 10 s ; IDs iOS supprimés (Android-only).
- **Deep link** : parser aussi `onNewIntent` (warm start) ; mutex sur `is_first_launch` (anti double +20).
- **Room** : `@Insert(IGNORE)` retournant −1 → fallback `findByUrl` ; échapper `%` et `_` dans la saisie de recherche (`LIKE ... ESCAPE '\'`) ; supprimer les fichiers physiques AVANT le delete Room.

## E4 — Distribution (nouveau)

Plan A : Play repositionné « studio de montage IA ». Plan B : APK signé en direct (GitHub Releases / site, SHA-256). v1.x : upload vidéo locale (V2PROD-01).
