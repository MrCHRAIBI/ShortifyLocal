---
phase: 02-fondations-natives-spike-critique
plan: 04
type: execute
wave: 3
depends_on: ["02-02", "02-03"]
files_modified:
  - app/src/androidTest/java/com/shortifylocal/ai/WhisperBenchmarkTest.kt
  - scripts/pull_benchmark_p2.sh
  - .planning/phases/02-fondations-natives-spike-critique/02-BENCHMARK.md
  - .planning/phases/02-fondations-natives-spike-critique/benchmark-result.json
  - .planning/PROJECT.md
autonomous: false
requirements: [PROJ-02]
user_setup:
  - service: appareil physique ARM64 + batterie
    why: "Session benchmark ~1 h sur le téléphone réel : batterie < 30 % stricte au départ (30 % pile = run invalide non consigné), appareil branché secteur pour stay-awake (R7)"
    env_vars:
      - name: ANDROID_SERIAL
        source: "adb devices — serial du téléphone ARM64 branché en USB"
    dashboard_config:
      - task: "Amener la batterie STRICTEMENT sous 30 % avant la session benchmark (débrancher/brancher selon le niveau), brancher le secteur ensuite"
        location: "Téléphone — Paramètres > Batterie"
      - task: "Maintenir l'écran susceptible de rester éveillé pendant ~1 h : adb shell svc power stayon true (fait par scripts/pull_benchmark_p2.sh --stayon)"
        location: "Hôte — ligne de commande"

estimate:
  tokens: 55000
  raw_tokens: 55000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Clone frais : git submodule update --init puis build OK (arm64-v8a + x86_64 produites) ; submodule absent → échec CMake explicite avec message actionnable (R1)"
    - "FFmpeg fork : conversion WAV 16 kHz rc=0 + volumedetect rc=0 ; FFprobeKit asserte sample_rate=16000, channels=1, codec=pcm_s16le, durée = source ± 0,1 s, taille > 0 (R2)"
    - "Appariement manifest→whisper par rang après normalisation (minuscules, ponctuation retirée) ; silence TTS inter-mots 200 ms éliminant end N = start N+1 ; |start_whisper − start_manifest| ≤ 200 ms par mot apparié (R3)"
    - "≥ 1 mot requis : transcription vide (0 mot) → échec explicite du test, jamais de PASS vacuo (R3)"
    - "Starts monotones non décroissants et end ≥ start (R3)"
    - "WAV benchmark ∈ [15, 20] min incluses (n minimal asserté) ; TTS muet/absent → abort explicite (exit non-zero) ; repli CC0 réservé aux runs benchmark sans ground truth (R4)"
    - "Alignement LOAD ≥ 16384 (2**14) pour chaque lib*.so extrait de l'APK, 32768 accepté (R5)"
    - "Ensemble vide de .so = échec — compte attendu : libs whisper JNI + libs du fork présentes (R5)"
    - "Triple assert anti-émulateur (ro.kernel.qemu ≠ 1, ro.hardware ∉ {goldfish, ranchu}, ro.product.cpu.abi = arm64-v8a) + ANDROID_SERIAL imposé, échec explicite AVANT lancement, jamais de skip silencieux (R6)"
    - "RTF ≤ 0,50 inclusif (0,50 = porte passée) ; batterie < 30 % stricte au départ de session (30 % pile = run invalide non consigné) (R7)"
    - "OOM consigné verdict « OOM » sans RTF, non bloquant ; seule la porte Base bloque (R7)"
    - "Ordre fixe Tiny → Base → Small + cooldown thermique baseline + 2 °C (attente max 10 min, sinon session abortée et documentée) (R7)"
    - "RTF = durée processing wall-clock / durée audio, 2 décimales, formule + arrondi écrits, temps de chargement exclu du RTF et consigné séparément (run warm) (R7)"
    - "Run atomique : toute interruption (écran éteint, kill, abort) → run rejeté et refait en entier ; appareil maintenu éveillé (stay-awake) pendant la session (R7)"
  artifacts:
    - path: app/src/androidTest/java/com/shortifylocal/ai/WhisperBenchmarkTest.kt
      provides: mesure Tiny→Base→Small (RTF 2 déc., température, pic RAM VmHWM, verdict) — off par défaut, JSON atomique unique (D-07)
    - path: .planning/phases/02-fondations-natives-spike-critique/02-BENCHMARK.md
      provides: fiche remplie DEPUIS benchmark-result.json uniquement + provenance modèles recopiée du manifest (nom|url|taille|sha256|source|date — audit D-01) + défaut Base figé + ligne PROJECT.md Key Decisions
    - path: scripts/pull_benchmark_p2.sh
      provides: stay-awake + pull adb du JSON vers .planning/phases/02-.../benchmark-result.json
  key_links:
    - from: WhisperBenchmarkTest (mesures in-app)
      to: out/benchmark_p2.json (external filesDir)
      via: écriture unique en fin de session complétée — zéro parsing logcat (D-07)
      why: le JSON est la source mécanique unique de la fiche — fiche remplie depuis le JSON, jamais saisie à la main
    - from: baseline thermique (avant run 1)
      to: cooldown avant run N+1
      via: température batterie (EXTRA_TEMPERATURE / 10.0 °C) ≤ baseline + 2 °C, attente max 10 min sinon session abortée
      why: ordre Tiny→Base→Small sans run biaisé (R7)
    - from: porte Base (RTF ≤ 0,50 ET zéro OOM ET zéro throttle)
      to: PROJECT.md Key Decisions (défaut figé Base) ou ERRATA (porte échouée)
      via: branche exclusive — jamais de repli auto Tiny
      why: le livrable de décision de la phase (prohibition ré-élection vitesse-seule)
  prohibitions:
    - statement: "MUST NOT ré-élire automatiquement un modèle sur critère vitesse-seule (repli Tiny) si la porte Base échoue — décision propriétaire en ERRATA uniquement"
      status: resolved
      verification: "judgment (décision humaine consignée en ERRATA)"
    - statement: "MUST NOT ajouter de code de téléchargement réseau de modèles Whisper (SEC-05 = Phase 3) ; modèles générés (TTS) ou poussés via adb"
      status: resolved
      verification: "test — grep zéro client HTTP/DownloadManager dans whisper-native/ + androidTest (bash, fail-closed)"
    - statement: "MUST NOT exposer le spike dans l'UI de production (aucun écran/bouton debug, navigation inchangée à 3 destinations)"
      status: resolved
      verification: "test — grep composables/boutons spike dans app/ = 0 (bash, fail-closed)"
    - statement: "MUST NOT utiliser un service TTS cloud/réseau pour l'asset — android.speech.tts système uniquement, repli CC0 local"
      status: resolved
      verification: "judgment (aucun import SDK TTS tiers)"
    - statement: "MUST NOT créer de workflow CI cloud (GitHub Actions ou équivalent) — verify_p2.sh local uniquement"
      status: resolved
      verification: "test — zéro fichier workflow cloud dans le dépôt (bash, fail-closed)"
    - statement: "MUST NOT patcher/modifier les .so prébuilds du fork (patch ELF post-build) — le check porte sur les binaires tels qu'embarqués"
      status: resolved
      verification: "test + judgment — verify_p2.sh compare le SHA-256 de chaque .so du fork extrait de l'APK à celui de l'AAR résolu (pin Phase 1) ; divergence = échec (bash, fail-closed)"
---

<objective>
Benchmark on-device Tiny/Base/Small et figement du défaut de modèle : `WhisperBenchmarkTest` (off par défaut, arg instrumentation `bench=on`, JSON atomique — D-07/D-03), session réelle ~1 h sur l'appareil physique (checkpoint opérateur), fiche `02-BENCHMARK.md` remplie DEPUIS le JSON + ligne PROJECT.md Key Decisions + recette finale de phase (suite verte + verify_p2.sh + jambe physique P1 soldée).

**User story (MVP) :** **En tant que** développeur-propriétaire, **je veux** un chiffre RTF mesuré sur MON téléphone pour Tiny, Base et Small, **afin que** le choix du modèle par défaut (Base) soit figé par une porte de viabilité objective (RTF ≤ 0,50, zéro OOM, zéro throttle) et que l'UX de progression des phases 4-6 soit calibrée sur des faits.

Purpose: livrable de décision de la phase — le benchmark fige le défaut Base sous porte de viabilité.
Output: WhisperBenchmarkTest + benchmark-result.json + 02-BENCHMARK.md + décision PROJECT.md (ou ERRATA si porte échouée).
</objective>

<execution_context>
@D:/projets/android/locut/.zcode/gsd-core/workflows/execute-plan.md
@D:/projets/android/locut/.zcode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-fondations-natives-spike-critique/02-SPEC.md
@.planning/phases/02-fondations-natives-spike-critique/02-CONTEXT.md
@.planning/phases/02-fondations-natives-spike-critique/02-RESEARCH.md
@.planning/phases/02-fondations-natives-spike-critique/02-PATTERNS.md
</context>

## Artifacts this phase produces

- `WhisperBenchmarkTest.kt` : lecture arg instrumentation `bench` ; session `Tiny → Base → Small` ; mesures in-app (température batterie `EXTRA_TEMPERATURE`/10.0 °C, statut thermique `PowerManager.getCurrentThermalStatus`, % `BATTERY_PROPERTY_CAPACITY`, pic RAM `VmHWM` de `/proc/self/status`) ; JSON atomique `out/benchmark_p2.json` (métadonnées session + entrées par modèle)
- `scripts/pull_benchmark_p2.sh` : `adb shell svc power stayon true` + `adb pull` du JSON vers `.planning/phases/02-fondations-natives-spike-critique/benchmark-result.json`
- `02-BENCHMARK.md` : fiche mécanique (appareil/RAM 4-6 Go/SoC/Android, batterie départ < 30 %, commit b4938, nb threads, SHA-256 WAV + manifest, provenance modèles = lignes `nom|url|taille|sha256|source|date` recopiées de `scripts/models_manifest_p2.txt` — audit D-01, baseline/seuil thermiques, RTF/temp/pic RAM/verdict par modèle, formule RTF + arrondi, défaut final + justification)
- `.planning/PROJECT.md` : ligne Key Decisions « modèle par défaut figé Base » (ou ERRATA si porte échouée)

<tasks>

<task type="auto">
  <name>Task 1: WhisperBenchmarkTest — mesures in-app, cooldown thermique, JSON atomique (R7/D-07/D-03) + pull script</name>
  <files>app/src/androidTest/java/com/shortifylocal/ai/WhisperBenchmarkTest.kt, scripts/pull_benchmark_p2.sh</files>
  <read_first>
    - .planning/phases/02-fondations-natives-spike-critique/02-SPEC.md (R7 cible complète — porte, cooldown, RTF, run atomique)
    - .planning/phases/02-fondations-natives-spike-critique/02-CONTEXT.md (D-03 bins officiels fp16, D-07 off par défaut + JSON source unique)
    - .planning/phases/02-fondations-natives-spike-critique/02-RESEARCH.md (mesures on-device — PowerManager/BatteryManager/VmHWM/stay-awake ; Assumption A1 unité EXTRA_TEMPERATURE)
    - app/src/androidTest/java/com/shortifylocal/ai/SpikeAssets.kt (plan 02-02 — WAV benchmark + garde R6)
    - app/src/main/java/com/shortifylocal/ai/data/native/whisper/WhisperTranscriber.kt + WhisperError.kt (plan 02-01 — LoadFailed(isOutOfMemory) → verdict OOM)
    - scripts/verify_p1.sh (pattern bash des scripts hôte)
  </read_first>
  <action>
    `WhisperBenchmarkTest.kt` : `@Before` → `assertPhysicalArm64()` (plan 02-02). OFF PAR DÉFAUT (D-07) : `InstrumentationRegistry.getArguments().getString("bench") != "on"` → return immédiat (le test reste vert en secondes dans la suite normale). Session (bench=on) : (1) préconditions strictes — `BATTERY_PROPERTY_CAPACITY` < 30 SINON fail explicite « batterie ≥ 30 % — run invalide, non consigné » (décharge monotone : non re-vérifiée par run) ; baseline thermique mesurée AVANT run 1 = température batterie via broadcast sticky `ACTION_BATTERY_CHANGED` (`EXTRA_TEMPERATURE` en dixièmes de °C → /10.0, A1) ; stay-awake assumé côté hôte (pull script). (2) Ordre fixe Tiny → Base → Small (plus gourmand en dernier) sur `getExternalFilesDir(null)/models/ggml-{tiny,base,small}.bin` (bins officiels fp16 poussés par fetch_models_p2.sh — D-03 ; variante quantizée = interdite en P2). (3) Par modèle : temps de chargement mesuré via `System.nanoTime` autour de `WhisperTranscriber.load` et consigné SÉPARÉMENT (exclu du RTF, run warm) ; `OutOfMemoryError` capté → `Failure(LoadFailed(isOutOfMemory = true))` → verdict « OOM » SANS RTF, non bloquant (poursuivre l'ordre) ; sinon `transcribe(benchmarkWav, model, abort = { false })` avec chrono processing wall-clock → `RTF = processing / durée_audio` (durée audio = mesure FFprobe du WAV benchmark, facteur partagé), arrondi 2 décimales (`String.format(java.util.Locale.ROOT, "%.2f", rtf)`) ; température batterie relevée après run ; pic RAM via `VmHWM` parsé dans `/proc/self/status` (kB) ; statut thermique `PowerManager.getCurrentThermalStatus()` avant/après (throttle = statut ≥ `THERMAL_STATUS_MODERATE` pendant le run — méthode nommée dans la fiche, discrétion Claude). (4) Cooldown mécanique avant run N+1 : tant que température batterie > baseline + 2.0 °C → attente par tranches de 10 s, max 10 min ; au-delà → session abortée et DOCUMENTÉE : écrire un marqueur distinct `out/benchmark_session_aborted.json` (raison + dernière température) et ÉCHEC du test — aucun JSON de métriques partiel. (5) ATOMIQUE (D-07/R7) : construire la chaîne JSON complète en mémoire et l'écrire en UNE écriture dans `out/benchmark_p2.json` UNIQUEMENT si la session est complétée — toute interruption (kill, écran, abort) laisse zéro JSON partiel (run rejeté, refait en entier). Contenu : métadonnées session (Build.MODEL, RAM totale via ActivityManager.MemoryInfo.totalMem, SoC via Build.HARDWARE + `ro.soc.model` par reflection SystemProperties, Build.VERSION.RELEASE + SDK_INT, batterie départ %, baseline °C, seuil +2 °C, attente max 10 min, nThreads passé au wrapper, `whisper.cpp` commit « b4938 (371b5a7561823ab2bb32142d2751e35e7534727b) », SHA-256 WAV benchmark + manifest via `java.security.MessageDigest`, ordre des modèles) + entrées par modèle `{model, loadTimeMs, rtf, tempC, peakRamKb, thermalStatus, verdict}`. ZÉRO parsing logcat pour toute métrique (D-07). Repli CC0 : si la génération TTS du WAV fonctionnel a échoué sur cet appareil, le WAV benchmark CC0 poussé via adb est admissible ICI UNIQUEMENT (runs sans ground truth) — noter la provenance de l'asset dans le JSON ; le test mot-à-mot (ground truth) reste bloquant par ailleurs.

    `scripts/pull_benchmark_p2.sh` : pattern P1 (`set -euo pipefail`, ROOT, fail, `MSYS_NO_PATHCONV=1`, wrapper `adb_`, `tr -d '\r'`) ; `adb_ shell svc power stayon true` (option `--stayon`, aussi invoquable avant session) ; `adb_ pull /sdcard/Android/data/com.shortifylocal.ai/files/out/benchmark_p2.json .planning/phases/02-fondations-natives-spike-critique/benchmark-result.json` (échec si absent) ; refuser explicitement une cible émulateur si ANDROID_SERIAL pointe dessus (même triple getprop que verify_p2.sh, section cible).
  </action>
  <precondition>Plans 02-01 (modèles poussés) et 02-02 (WAV benchmark sous files/wavs/) terminés ; l'appareil physique est branché.</precondition>
  <verify>
    <automated>cd "D:/projets/android/locut" && ./gradlew :app:compileDebugAndroidTestSources && bash -n scripts/pull_benchmark_p2.sh && echo "BENCH SCAFFOLD OK"</automated>
    <fails_when>exit non-zero : la classe benchmark ne compile pas ou le pull script a une erreur de syntaxe bash</fails_when>
  </verify>
  <verify>
    <automated>cd "D:/projets/android/locut" && ANDROID_SERIAL="$ANDROID_SERIAL" timeout 120 ./gradlew :app:connectedDebugAndroidTest --tests "com.shortifylocal.ai.WhisperBenchmarkTest"</automated>
    <fails_when>timeout 120 expiré (exécution dépassant ~2 minutes = le test mesure sans l'arg `bench=on`, off par défaut violé) ou exit non-zero — la variable est placée AVANT timeout : la forme `timeout 120 ANDROID_SERIAL=…` échoue (rc 127) dans ce Git Bash</fails_when>
  </verify>
  <acceptance_criteria>
    - Sans `bench=on`, le test retourne immédiatement (grep du early-return sur l'argument instrumentation) — la suite du spike reste exécutable sans la session ~1 h (D-07)
    - L'ordre Tiny → Base → Small est codé en dur dans la liste des modèles ; le cooldown compare à baseline + 2.0 °C avec un cap de 10 minutes puis abort documenté
    - Le RTF est calculé processing/durée_audio, arrondi 2 décimales, temps de chargement consigné séparément (champs distincts `loadTimeMs` et `rtf`)
    - L'écriture du JSON est unique et post-complétion (un seul appel d'écriture, en fin de session) ; un marqueur distinct couvre l'abort de session
    - Aucune lecture de logcat dans la classe (grep `logcat` = 0)
    - Le pull script contient stay-awake, triple garde cible, et pousse le JSON vers .planning/phases/02-fondations-natives-spike-critique/benchmark-result.json
    - KDoc français citant R7/D-03/D-07
  </acceptance_criteria>
  <reversibility rating="costly">Les mesures consignées référencent les bins officiels fp16 exacts (D-03) — changer de variante de modèle invalide le benchmark et impose une session complète de re-mesure.</reversibility>
  <done>Le harnais benchmark est compilable, off par défaut prouvé, pull script prêt — la session réelle est déléguée au checkpoint opérateur (Task 3).</done>
</task>

<task type="auto">
  <name>Task 2: Recette finale de phase — suite verte sur serial physique, verify_p2.sh, refus émulateur, jambe P1 soldée</name>
  <files>.planning/phases/02-fondations-natives-spike-critique/02-04-SUMMARY.md</files>
  <read_first>
    - .planning/phases/02-fondations-natives-spike-critique/02-SPEC.md (AC R6 — refus émulateur vérifié ; contrainte jambe physique UAT P1 prérequis de clôture)
    - scripts/verify_emulator_p1.sh (recette P1 à rejouer sur le serial physique)
    - .planning/STATE.md (préoccupation ouverte : jambe physique D-03 — captures screens/device-*.png)
  </read_first>
  <action>
    Recette exécutée par l'executor avec l'appareil branché (précondition) : (1) démonstration du refus émulateur — démarrer l'AVD x86_64 (émulateur -avd <nom>, présent depuis Phase 1), `ANDROID_SERIAL=<serial émulateur> ./gradlew :app:connectedDebugAndroidTest` → suite en échec EXPLICITE (garde in-test « ÉMULATEUR DÉTECTÉ ») AVANT toute exécution de spike — consigner la sortie ; couper l'émulateur. (2) Suite complète verte sur le serial physique : `ANDROID_SERIAL=<serial physique> ./gradlew :app:connectedDebugAndroidTest` (WhisperBenchmarkTest y est skippé — off par défaut). (3) `bash scripts/verify_p2.sh` avec ANDROID_SERIAL=<serial physique> (section cible active). (4) Jambe physique P1 soldée : `ANDROID_SERIAL=<serial physique> bash scripts/verify_emulator_p1.sh` puis copier les captures vers `.planning/phases/01-chafaudage-d-cisions-bloquantes/screens/device-shell.png` + `device-after-toggle.png` (clôture de la préoccupation ouverte de STATE.md) ; l'app s'installe et se lance sur Android 13+ physique (volet install/launch de PROJ-02). (5) Vérification clone frais (AC R1) — si le temps de build le permet : `git clone --recurse-submodules "D:/projets/android/locut" "$TMP/p2fresh"` puis `./gradlew :whisper-native:assembleDebug` dans le clone ; sinon consigner dans le SUMMARY comme human-check restant (la garde CMake est déjà prouvée en 02-01). Aucune UI ajoutée nulle part (prohibition spike-UI — grep composables spike = 0 dans app/src/main).
  </action>
  <precondition>L'appareil physique ARM64 est branché avec ANDROID_SERIAL exporté ; plans 02-01 à 02-03 terminés ; la batterie n'a pas besoin d'être < 30 % pour cette recette (seule la session benchmark l'exige).</precondition>
  <verify>
    <automated>cd "D:/projets/android/locut" && ANDROID_SERIAL="$ANDROID_SERIAL" ./gradlew :app:connectedDebugAndroidTest && ANDROID_SERIAL="$ANDROID_SERIAL" bash scripts/verify_p2.sh</automated>
    <fails_when>exit non-zero de la suite ou de la batterie : un test du spike échoue sur le serial physique, ou une section verify_p2 (dont cible getprop) est rouge</fails_when>
  </verify>
  <acceptance_criteria>
    - La sortie de la démo émulateur (consignée dans le SUMMARY) montre un échec explicite contenant « ÉMULATEUR » avant l'exécution des tests — AC R6 « refus vérifié »
    - La suite connectedDebugAndroidTest est 100 % verte sur le serial physique (4 classes + benchmark skippé)
    - verify_p2.sh vert avec la section cible active (triple assert getprop sur le physique)
    - Les captures device-shell.png / device-after-toggle.png existent dans .planning/phases/01-chafaudage-d-cisions-bloquantes/screens/ (jambe P1 soldée)
    - `grep -rn "Composable\|Button" app/src/main/java/com/shortifylocal/ai/ | grep -i "spike\|bench\|whisper" | wc -l` = 0 (prohibition spike-UI)
  </acceptance_criteria>
  <done>La chaîne native est prouvée sur l'appareil physique (suite verte + batterie mécanique), l'émulateur est refusé mécaniquement, la jambe UAT P1 est soldée — per R6 + contrainte de clôture.</done>
</task>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 3: Session benchmark réelle (~1 h) puis fiche 02-BENCHMARK.md depuis le JSON + décision PROJECT.md/ERRATA</name>
  <files>.planning/phases/02-fondations-natives-spike-critique/02-BENCHMARK.md, .planning/phases/02-fondations-natives-spike-critique/benchmark-result.json, .planning/PROJECT.md, docs/ERRATA-2026-09-05.md</files>
  <read_first>
    - .planning/phases/02-fondations-natives-spike-critique/02-SPEC.md (R7 — contenu exact de la fiche, porte Base, branche ERRATA)
    - .planning/phases/02-fondations-natives-spike-critique/02-CONTEXT.md (D-07 JSON source mécanique unique)
    - .planning/PROJECT.md (section Key Decisions — format de ligne à ajouter)
    - app/src/androidTest/java/com/shortifylocal/ai/WhisperBenchmarkTest.kt (Task 1 — champs du JSON à consommer)
    - .planning/phases/02-fondations-natives-spike-critique/benchmark-result.json (après session — source unique)
  </read_first>
  <precondition>Task 1 et Task 2 terminées ; l'appareil physique ARM64 est branché ; la batterie peut être amenée sous 30 % ; ~1 h de disponibilité de l'appareil.</precondition>
  <action>
    ACTION OPÉRATEUR (conditions non simulables par l'agent — batterie < 30 %, session ~1 h sans interruption) puis REPRISE AGENT pour la fiche : détails dans `<instructions>` et `<verification>` ci-dessous. L'agent a automatisé tout l'automatisable (Task 1 : harnais + pull script ; Task 2 : recette) ; l'humain fait UNE chose : mener la session benchmark ; l'agent reprend ensuite pour la fiche et la décision. La fiche recopie INTÉGRALEMENT les lignes de provenance modèles `nom|url|taille|sha256|source|date` depuis `scripts/models_manifest_p2.txt` (section « Provenance des modèles » — clause audit D-01 : SHA-256 + URL + date des modèles consignés dans 02-BENCHMARK.md, en complément du SHA-256 des WAV/manifest).
  </action>
  <instructions>
    L'agent a déjà automatisé : WhisperBenchmarkTest (off par défaut, cooldown thermique, JSON atomique), scripts/pull_benchmark_p2.sh (stay-awake + pull + triple garde cible), la recette complète (Task 2).

    L'ACTION HUMAINE (l'unique) : (1) amener la batterie du téléphone STRICTEMENT sous 30 % (30 % pile = run invalide non consigné) ; (2) brancher le secteur (stay-awake) puis lancer depuis la racine : `ANDROID_SERIAL=<serial> bash scripts/pull_benchmark_p2.sh --stayon` ; (3) lancer la session : `ANDROID_SERIAL=<serial> ./gradlew :app:connectedDebugAndroidTest -Pandroid.testInstrumentationRunnerArguments.bench=on --tests "com.shortifylocal.ai.WhisperBenchmarkTest"` — durée ~1 h, NE PAS toucher l'appareil (run atomique : toute interruption = session refaite en entier) ; (4) relancer `bash scripts/pull_benchmark_p2.sh` pour ramener `benchmark-result.json` puis répondre au checkpoint.
  </instructions>
  <verification>
    <human-check>L'opérateur confirme : batterie < 30 % au départ, appareil branché secteur, session lancée avec `bench=on` sur le serial physique et menée SANS interruption ; le JSON benchmark-result.json existe dans le dossier de phase après pull.</human-check>
    <automated>cd "D:/projets/android/locut" && test -f .planning/phases/02-fondations-natives-spike-critique/benchmark-result.json && for f in "RTF" "b4938" "Tiny" "Base" "Small" "OOM" "https://huggingface.co" "2026-" ; do grep -q "$f" .planning/phases/02-fondations-natives-spike-critique/02-BENCHMARK.md || exit 1; done && grep -q "Base" .planning/PROJECT.md && echo "FICHE OK"</automated>
    <fails_when>exit non-zero : JSON absent (session non menée ou non pullée), fiche incomplète (rubrique R7 manquante), ou PROJECT.md sans la ligne du défaut</fails_when>
  </verification>
  <resume-signal>Répondre « session done » quand le JSON est pullé (ou décrire l'interruption : session abortée thermiquement, batterie ≥ 30 %, kill — l'agent documente et la session est refaite en entier) ; l'agent remplit alors 02-BENCHMARK.md depuis le JSON, tranche la porte Base (PROJECT.md Key Decisions si RTF Base ≤ 0,50 ET zéro OOM ET zéro throttle ; sinon amendement daté dans docs/ERRATA-2026-09-05.md consignant la décision propriétaire — JAMAIS de repli automatique sur Tiny), et commette fiche + JSON + PROJECT.md (+ ERRATA le cas échéant).</resume-signal>
  <acceptance_criteria>
    - 02-BENCHMARK.md contient toutes les rubriques de l'AC R7 et chaque valeur numérique s'y retrouvant existe à l'identique dans benchmark-result.json (source mécanique — comparer RTF/température/pic RAM par modèle)
    - La porte Base est tranchée explicitement : soit PROJECT.md Key Decisions porte la ligne « défaut Base figé », soit docs/ERRATA-2026-09-05.md porte un amendement daté de décision propriétaire — aucune troisième issue, aucun repli auto Tiny
    - Tiny et Small sont documentés documentation-only (jamais auto-élus) dans la fiche
    - La section « Provenance des modèles » de la fiche recopie les lignes `nom|url|taille|sha256|source|date` de `scripts/models_manifest_p2.txt` pour les 3 modèles (audit D-01 — URL + date présents, pas seulement les SHA-256 WAV/manifest)
    - La fiche écrit la formule RTF et l'arrondi (2 décimales, chargement exclu)
  </acceptance_criteria>
  <done>Le défaut de modèle est figé par mesure (ou la porte échouée est escaladée en ERRATA), la fiche est la projection mécanique du JSON — per R7/D-07.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| instrumentation → JSON de métriques | toute métrique falsifiée ou partielle corrompt la décision de modèle |
| conditions matérielles → mesures | batterie/thermie biaisent le RTF si non gardées |
| fiche ← JSON | la fiche saisie à la main cesse d'être une preuve |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-11 | Repudiation | métriques saisies à la main dans la fiche | high | mitigate | le JSON est la source mécanique unique (D-07) — la fiche est remplie depuis le JSON, valeurs comparables 1:1 ; zéro parsing logcat |
| T-02-12 | Repudiation | run biaisé thermiquement / batterie ≥ 30 % | medium | mitigate | batterie < 30 % stricte en précondition ; cooldown baseline + 2 °C (max 10 min puis abort documenté) ; ordre fixe Tiny→Base→Small |
| T-02-13 | DoS | session infinie ou JSON partiel accepté | medium | mitigate | cap d'attente thermique 10 min ; écriture JSON unique en fin de session complétée (run atomique) ; marqueur distinct pour session abortée |
| T-02-14 | Tampering | auto-réélection du modèle (repli Tiny) | high | mitigate | la porte n'a que deux issues codées : PROJECT.md (passée) ou ERRATA (échouée) — aucune branche de repli automatique dans le code |
</threat_model>

<verification>
- `./gradlew :app:compileDebugAndroidTestSources` vert ; benchmark off par défaut prouvé (suite rapide sans bench=on)
- Suite connectedDebugAndroidTest verte sur serial physique + verify_p2.sh vert (section cible active)
- Refus émulateur démontré explicitement (AC R6)
- benchmark-result.json présent + 02-BENCHMARK.md complète + PROJECT.md Key Decisions (ou ERRATA)
- Jambe physique UAT P1 soldée (captures screens/device-*.png)
</verification>

<success_criteria>
- AC R7 complet : fiche appareil/RAM/SoC/Android, batterie départ < 30 %, b4938, nb threads, SHA-256 WAV + manifest, baseline/seuil thermiques, Tiny→Base→Small avec cooldown, RTF 2 décimales (chargement exclu), température + pic RAM + verdict par modèle, OOM non bloquant, run atomique stay-awake
- Porte Base tranchée (défaut figé en PROJECT.md ou décision ERRATA — jamais de repli auto)
- Tous les critères d'acceptation de la SPEC sont couverts par les 4 plans de la phase
</success_criteria>

<output>
Create `.planning/phases/02-fondations-natives-spike-critique/02-04-SUMMARY.md` when done
</output>
