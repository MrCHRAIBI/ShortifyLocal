---
phase: 02-fondations-natives-spike-critique
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - settings.gradle.kts
  - gradle/libs.versions.toml
  - build.gradle.kts
  - app/build.gradle.kts
  - whisper-native/build.gradle.kts
  - whisper-native/src/main/AndroidManifest.xml
  - whisper-native/src/main/cpp/CMakeLists.txt
  - whisper-native/src/main/cpp/whisper_jni.c
  - whisper-native/whisper.cpp
  - app/src/main/java/com/shortifylocal/ai/data/native/whisper/WordTimestamp.kt
  - app/src/main/java/com/shortifylocal/ai/data/native/whisper/WhisperError.kt
  - app/src/main/java/com/shortifylocal/ai/data/native/whisper/WhisperResult.kt
  - app/src/main/java/com/shortifylocal/ai/data/native/whisper/WhisperTranscriber.kt
  - scripts/fetch_models_p2.sh
  - scripts/models_manifest_p2.txt
autonomous: true
requirements: [PROJ-02]

estimate:
  tokens: 80000
  raw_tokens: 80000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Clone frais : git submodule update --init puis build OK (arm64-v8a + x86_64 produites) ; submodule absent → échec CMake explicite avec message actionnable (R1)"
    - "FFmpeg fork : conversion WAV 16 kHz rc=0 + volumedetect rc=0 ; FFprobeKit asserte sample_rate=16000, channels=1, codec=pcm_s16le, durée = source ± 0,1 s, taille > 0 (R2)"
    - "Appariement manifest→whisper par rang après normalisation (minuscules, ponctuation retirée) ; silence TTS inter-mots 200 ms éliminant end N = start N+1 ; |start_whisper − start_manifest| ≤ 200 ms par mot apparié (R3)"
    - "≥ 1 mot requis : transcription vide (0 mot) → échec explicite du test, jamais de PASS vacuo (R3)"
    - "Starts monotones non décroissants et end ≥ start (R3)"
    - "WAV benchmark ∈ [15, 20] min incluses (n minimal asserté) ; TTS muet/absent → abort explicite (exit non-zero) ; repli CC0 réservé aux runs benchmark sans ground truth (R4)"
    - "Alignement LOAD ≥ 16384 (2**14) pour chaque lib*.so extrait de l'APK, 32768 accepté (R5)"
    - "Ensemble vide de .so = échec — compte attendu : libs whisper JNI + libs du fork présentes (R5)"
    - "Triple assert anti-émulateur (ro.kernel.qemu ≠ 1, ro.hardware ∉ {goldfish, ranchu}, ro.product.cpu.abi = arm64-v8a) + ANDROID_SERIAL imposé, échec explicite AVANT lancement, jamais de skip silencieux (R6)"
    - "RTF ≤ 0,50 inclusif (0,50 = porte passée) ; batterie < 30 % stricte au départ de session (30 % pile = run invalide non consigné) (R7)"
    - "OOM consigné verdict « OOM » sans RTF, non bloquant ; seule la porte Base bloque (R7)"
    - "Ordre fixe Tiny → Base → Small + cooldown thermique baseline + 2 °C (attente max 10 min, sinon session abortée et documentée) (R7)"
    - "RTF = durée processing wall-clock / durée audio, 2 décimales, formule + arrondi écrits, temps de chargement exclu du RTF et consigné séparément (run warm) (R7)"
    - "Run atomique : toute interruption (écran éteint, kill, abort) → run rejeté et refait en entier ; appareil maintenu éveillé (stay-awake) pendant la session (R7)"
  artifacts:
    - path: whisper-native/ (module Android library)
      provides: build natif whisper.cpp b4938 (submodule 371b5a7561823ab2bb32142d2751e35e7534727b) → libwhisper_jni.so pour arm64-v8a + x86_64, flags linker 16 KB
    - path: app/src/main/java/com/shortifylocal/ai/data/native/whisper/
      provides: contrat typé D-04/D-06 — WordTimestamp, WhisperError (sealed), WhisperResult, WhisperTranscriber.load/transcribe
    - path: scripts/fetch_models_p2.sh + scripts/models_manifest_p2.txt
      provides: pin anti-TOFU D-01 — téléchargement Tiny/Base/Small (75/142/466 Mo) + SHA-256 vérifié + push adb
  key_links:
    - from: gradle/libs.versions.toml (ndk = "29.0.14206865")
      to: whisper-native/build.gradle.kts (ndkVersion)
      via: ndkVersion = libs.versions.ndk.get()
      why: épinglage NDK unique ≥ r28 (E2) — zéro version inline
    - from: whisper_jni.c (centiseconds natives)
      to: WhisperTranscriber (startMs/endMs)
      via: mapping cs × 10 → ms, site de conversion unique
      why: Pitfall 1 — timestamps ×10 faux si unités confondues
    - from: WhisperNative.nativeRequestCancel (flag atomique)
      to: jni_abort_callback (ggml_abort_callback entre chunks)
      via: trampoline C lisant l'atomic_bool
      why: D-05/E3 — annulation double circuit propagée au natif
  prohibitions:
    - statement: "MUST NOT ré-élire automatiquement un modèle sur critère vitesse-seule (repli Tiny) si la porte Base échoue — décision propriétaire en ERRATA uniquement"
      status: resolved
      verification: "judgment (décision humaine consignée en ERRATA)"
    - statement: "MUST NOT ajouter de code de téléchargement réseau de modèles Whisper (SEC-05 = Phase 3) ; modèles générés (TTS) ou poussés via adb"
      status: resolved
      verification: "test — grep zéro client HTTP/DownloadManager dans whisper-native/ + androidTest (bash, fail-closed)"
    - statement: "MUST NOT exposer le spike dans l'UI de production (aucun écran/bouton debug, navigation inchangée à 3 destinations)"
      status: resolved
      verification: "test — grep composables/boutons spike dans app/ = 0 (bash, fail-closed)"
    - statement: "MUST NOT utiliser un service TTS cloud/réseau pour l'asset — android.speech.tts système uniquement, repli CC0 local"
      status: resolved
      verification: "judgment (aucun import SDK TTS tiers)"
    - statement: "MUST NOT créer de workflow CI cloud (GitHub Actions ou équivalent) — verify_p2.sh local uniquement"
      status: resolved
      verification: "test — zéro fichier workflow cloud dans le dépôt (bash, fail-closed)"
    - statement: "MUST NOT patcher/modifier les .so prébuilds du fork (patch ELF post-build) — le check porte sur les binaires tels qu'embarqués"
      status: resolved
      verification: "test + judgment — verify_p2.sh compare le SHA-256 de chaque .so du fork extrait de l'APK à celui de l'AAR résolu (pin Phase 1) ; divergence = échec (bash, fail-closed)"
---

<objective>
Tracer natif de bout en bout : du submodule whisper.cpp épinglé b4938 à la `libwhisper_jni.so` (2 ABIs) packagée dans `:app`, avec le wrapper Kotlin JNI contractuel (D-04/D-05/D-06) et le pin anti-TOFU des modèles (D-01).

**User story (MVP) :** **En tant que** développeur-propriétaire du projet, **je veux** la chaîne de build native complète (submodule → CMake/NDK → `.so` → APK → vérification mécanique) fonctionnelle et épinglée, **afin que** les preuves on-device des plans suivants s'exécutent sur un socle natif reproductible et non sur des hypothèses.

Purpose: le chemin critique absolu du projet — sans module natif compilable ni contrat JNI typé, aucune preuve de transcription ni benchmark n'est possible.
Output: module `:whisper-native` vert sur 2 ABIs, wrapper `data/native/whisper/` complet, `scripts/fetch_models_p2.sh` + manifest SHA-256, entrées catalogue NDK/androidTest.
</objective>

> **Watch item exécution (checker scope_sanity, itération 1) :** ce plan s'exécute au seuil haut du budget (15 fichiers, estimate 80 000 tokens ≈ 80 %). Si le contexte consommé dépasse ~60 % à l'entrée de la Task 3, détacher les scripts modèles (`scripts/fetch_models_p2.sh` + `scripts/models_manifest_p2.txt`) vers un plan 02-05 wave 2 — scission optionnelle, non imposée.

<execution_context>
@D:/projets/android/locut/.zcode/gsd-core/workflows/execute-plan.md
@D:/projets/android/locut/.zcode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-fondations-natives-spike-critique/02-SPEC.md
@.planning/phases/02-fondations-natives-spike-critique/02-CONTEXT.md
@.planning/phases/02-fondations-natives-spike-critique/02-RESEARCH.md
@.planning/phases/02-fondations-natives-spike-critique/02-PATTERNS.md
@docs/ERRATA-2026-09-05.md
</context>

## Artifacts this phase produces

- **Module `whisper-native/`** : `build.gradle.kts` (android library, ndkVersion catalogue, abiFilters arm64-v8a + x86_64), `src/main/AndroidManifest.xml` (minimal), `src/main/cpp/CMakeLists.txt` (options `WHISPER_BUILD_EXAMPLES/TESTS/SERVER OFF`, `GGML_OPENMP OFF`, garde submodule `FATAL_ERROR`, flags `-Wl,-z,max-page-size=16384 -Wl,-z,common-page-size=16384`), `src/main/cpp/whisper_jni.c` (symboles `Java_com_shortifylocal_ai_whisper_1native_WhisperNative_nativeLoadContext` / `_nativeFreeContext` / `_nativeTranscribe` / `_nativeRequestCancel` / `_nativeLastStatusCode` / `_nativeAbortFired` + trampoline `jni_abort_callback`), submodule `whisper-native/whisper.cpp` épinglé `371b5a7561823ab2bb32142d2751e35e7534727b` (= b4938 = v1.9.3), sortie `libwhisper_jni.so` par ABI
- **Kotlin `data/native/whisper/`** : `WordTimestamp(word: String, startMs: Long, endMs: Long)`, `WhisperError` (sealed : `LoadFailed(isOutOfMemory: Boolean)`, `Aborted`, `InferenceFailed(code: Int)`, `InvalidInput`), `WhisperResult<T>` (sealed : `Success`/`Failure`), `WhisperTranscriber` (companion `load(model: File)`, suspend `transcribe(wav, model, abort)`), objet interne `com.shortifylocal.ai.whisper_native.WhisperNative` (externals JNI)
- **Catalogue & wiring** : `ndk = "29.0.14206865"`, `androidxTestRunner = "1.7.0"`, `androidxTestExtJunit = "1.3.0"`, plugin `android-library`, `include(":whisper-native")`, `implementation(project(":whisper-native"))`, `testInstrumentationRunner = "androidx.test.runner.AndroidJUnitRunner"`
- **Scripts** : `scripts/fetch_models_p2.sh`, `scripts/models_manifest_p2.txt` (3 lignes URL|taille|sha256|source|date)

<tasks>

<task type="tracer">
  <name>Task 1: Tracer — submodule b4938 → CMake/NDK → libwhisper_jni.so (2 ABIs) + garde submodule absent</name>
  <files>settings.gradle.kts, gradle/libs.versions.toml, build.gradle.kts, whisper-native/build.gradle.kts, whisper-native/src/main/AndroidManifest.xml, whisper-native/src/main/cpp/CMakeLists.txt, whisper-native/src/main/cpp/whisper_jni.c, whisper-native/whisper.cpp (submodule)</files>
  <read_first>
    - .planning/phases/02-fondations-natives-spike-critique/02-RESEARCH.md (sections Pattern 1, Code Examples CMakeLists, Pitfalls 2/11/12 — options verbatim b4938)
    - .planning/phases/02-fondations-natives-spike-critique/02-PATTERNS.md (Shared Pattern 1 discipline catalogue, bloc gradle du module)
    - gradle/libs.versions.toml (blocs [versions]/[libraries]/[plugins] + commentaire ffmpegKit lignes 26-30 = gabarit de provenance)
    - settings.gradle.kts, build.gradle.kts (racine — points d'insertion)
    - docs/ERRATA-2026-09-05.md (E2 : pin b4938, flags linker 16 KB)
  </read_first>
  <action>
    Ajouter le submodule : `git submodule add https://github.com/ggml-org/whisper.cpp.git whisper-native/whisper.cpp` puis `git -C whisper-native/whisper.cpp checkout 371b5a7561823ab2bb32142d2751e35e7534727b` (SHA complet = b4938 = v1.9.3, recherche § Standard Stack) — ne JAMAIS modifier le contenu du submodule ; committer `.gitmodules` + le gitlink.

    Catalogue `gradle/libs.versions.toml` : ajouter `ndk = "29.0.14206865"` avec le commentaire de provenance du bloc ffmpegKit (dernière stable developer.android.com/ndk/downloads, vérifiée 2026-09-06, clause fraîcheur E2 ; repli documenté hors ligne : `28.2.13676358` déjà installée) — per D-01/E2, épinglage r29 stable posé par la note de portée de la phase ; ajouter `androidxTestRunner = "1.7.0"`, `androidxTestExtJunit = "1.3.0"` (Package Legitimacy Audit : first-party Google, verdict OK — aucun checkpoint requis, aucune entrée [ASSUMED]/[SUS]) ; blocs [libraries] `androidx-test-runner`, `androidx-test-ext-junit` ; bloc [plugins] `android-library = { id = "com.android.library", version.ref = "agp" }`. Mettre à jour le commentaire de la ligne `whisperCpp = "b4938"` (entrée vivante, plus « commentaire »).

    `settings.gradle.kts` : `include(":whisper-native")` après `include(":app")`. `build.gradle.kts` racine : `alias(libs.plugins.android.library) apply false`.

    `whisper-native/build.gradle.kts` : plugins par alias `libs.plugins.android.library` ; `namespace = "com.shortifylocal.ai.whisper_native"` ; `compileSdk = 37` (commentaire amendement E2 identique à :app) ; `minSdk = 33` ; `ndkVersion = libs.versions.ndk.get()` (zéro version inline) ; `ndk { abiFilters += listOf("arm64-v8a", "x86_64") }` (x86_64 compile-only, jamais exécuté) ; `externalNativeBuild { cmake { path = file("src/main/cpp/CMakeLists.txt"); version = "3.22.1" } }` ; arguments CMake `-DGGML_OPENMP=OFF` (défaut b4938 = ON, Pitfall 2) ; `kotlin { jvmToolchain(17) }` non requis (aucune source Kotlin dans le module). Manifest library minimal 2-3 lignes, sans `<application>` ni permissions.

    `CMakeLists.txt` : reprendre le squelette verbatim de 02-RESEARCH.md § Code Examples — `cmake_minimum_required(VERSION 3.22)`, garde `if(NOT EXISTS whisper.cpp/CMakeLists.txt) message(FATAL_ERROR "whisper.cpp submodule absent — git submodule update --init --recursive")` (AC R1 : message actionnable), `BUILD_SHARED_LIBS OFF`, `WHISPER_BUILD_EXAMPLES/TESTS/SERVER OFF`, `GGML_OPENMP OFF`, `add_subdirectory`, `add_library(whisper_jni SHARED whisper_jni.c)`, `target_link_libraries(whisper_jni PRIVATE whisper log)`, includes `whisper.cpp/include` + `whisper.cpp/ggml/include`, `target_link_options` avec les deux flags 16 KB de l'errata E2.

    `whisper_jni.c` : bindings complets (première occurrence — suivre RESEARCH.md § Code Examples trampoline). État global : `static _Atomic int g_cancel; static _Atomic int g_abort_fired; static int g_last_code;`. Trampoline `static bool jni_abort_callback(void* data)` retournant `atomic_load(&g_cancel) != 0` (et posant `g_abort_fired = 1` si true) — signature exacte `typedef bool (*ggml_abort_callback)(void * data)` de `ggml/include/ggml.h` à b4938. Fonctions JNI (préfixe EXACT `Java_com_shortifylocal_ai_whisper_1native_WhisperNative_` — l'underscore de `whisper_native` devient `_1`, Pitfall 11 UnsatisfiedLinkError) : `nativeLoadContext(JNIEnv*, jobject, jstring modelPath)` via `whisper_init_from_file_with_params` (API b4938), retour 0 si échec ; `nativeFreeContext(jlong)` via `whisper_free` ; `nativeRequestCancel()` posant `g_cancel = 1` ; `nativeTranscribe(jlong ctx, jstring wavPath, jstring language, jint nThreads)` retournant `jobjectArray` de `jstring` formatés `word\t0\tend` avec t0/t1 en CENTISECONDS (unités natives b4938 — PAS de conversion côté C, Pitfall 1) lus via `whisper_full_n_segments` / `whisper_full_get_segment_text` / `whisper_full_get_segment_t0` / `whisper_full_get_segment_t1` ; `params` posés nativement : `whisper_full_default_params(WHISPER_SAMPLING_GREEDY)` puis `token_timestamps = true`, `max_len = 1`, `split_on_word = true`, `language` depuis l'argument (défaut b4938 = "en", Pitfall 4), `n_threads = nThreads` (défaut b4938 = min(4, hw), Pitfall 4), `print_progress = print_realtime = print_timestamps = false`, `abort_callback = jni_abort_callback`, `abort_callback_user_data = &g_cancel`, `vad` laissé au défaut `false` (Pitfall 12) ; stocker le code retour brut de `whisper_full` dans `g_last_code` (0 = OK, négatifs -6/-8/-9 = abort, autres négatifs = erreur) et réinitialiser `g_cancel`/`g_abort_fired` en début de transcribe ; `nativeLastStatusCode()` et `nativeAbortFired()` exposant ces compteurs.
  </action>
  <precondition>Le réseau est disponible pour cloner le submodule whisper.cpp depuis GitHub (clone one-shot ; ensuite tout est local).</precondition>
  <verify>
    <automated>cd "D:/projets/android/locut" && git submodule status whisper-native/whisper.cpp | grep -q "371b5a7561823ab2bb32142d2751e35e7534727b" && ./gradlew :whisper-native:assembleDebug && [ "$(find whisper-native/build -name libwhisper_jni.so -path '*arm64-v8a*' | wc -l)" -ge 1 ] && [ "$(find whisper-native/build -name libwhisper_jni.so -path '*x86_64*' | wc -l)" -ge 1 ] && echo "TRACER OK"</automated>
    <fails_when>exit non-zero : submodule absent ou pas au SHA épinglé, échec de compilation CMake/NDK, ou zéro libwhisper_jni.so pour arm64-v8a ou x86_64 dans les sorties de build</fails_when>
  </verify>
  <verify>
    <automated>cd "D:/projets/android/locut" && mv whisper-native/whisper.cpp whisper-native/whisper.cpp.off && { ./gradlew :whisper-native:assembleDebug > build/p2guard.log 2>&1; RC=$?; mv whisper-native/whisper.cpp.off whisper-native/whisper.cpp; [ "$RC" -ne 0 ] && grep -q "submodule update --init" build/p2guard.log && echo "GUARD OK"; }</automated>
    <fails_when>exit 0 du bloc entier : le build réussit SANS submodule (garde morte) ou échoue sans le message actionnable « submodule update --init »</fails_when>
  </verify>
  <acceptance_criteria>
    - `git ls-files` liste `.gitmodules` et le gitlink `whisper-native/whisper.cpp` ; `git -C whisper-native/whisper.cpp rev-parse HEAD` = 371b5a7561823ab2bb32142d2751e35e7534727b
    - `gradle/libs.versions.toml` contient `ndk = "29.0.14206865"` avec commentaire de provenance + date, `androidxTestRunner`, `androidxTestExtJunit`, et le plugin `android-library` (version.ref = agp) ; aucune version inline hors catalogue (grep `29\.0\.14206865` absent de whisper-native/build.gradle.kts)
    - CMakeLists.txt contient `GGML_OPENMP OFF`, `WHISPER_BUILD_EXAMPLES OFF`, les deux flags `-Wl,-z,max-page-size=16384` et `-Wl,-z,common-page-size=16384`, et le `FATAL_ERROR` actionnable
    - whisper_jni.c contient `ggml_abort_callback`, `token_timestamps`, `max_len`, `split_on_word`, le préfixe `Java_com_shortifylocal_ai_whisper_1native_WhisperNative_`
    - `include(":whisper-native")` présent dans settings.gradle.kts ; alias android-library en racine
  </acceptance_criteria>
  <done>Le module `:whisper-native` compile les 2 ABIs depuis le submodule épinglé, la garde submodule-absent échoue avec un message actionnable, le catalogue porte le pin NDK r29 — per D-01/E2 (clause fraîcheur).</done>
</task>

<task type="auto">
  <name>Task 2: Wrapper Kotlin JNI contractuel (D-04/D-05/D-06) + packaging dans :app</name>
  <files>app/build.gradle.kts, app/src/main/java/com/shortifylocal/ai/data/native/whisper/WordTimestamp.kt, app/src/main/java/com/shortifylocal/ai/data/native/whisper/WhisperError.kt, app/src/main/java/com/shortifylocal/ai/data/native/whisper/WhisperResult.kt, app/src/main/java/com/shortifylocal/ai/data/native/whisper/WhisperTranscriber.kt</files>
  <read_first>
    - .planning/phases/02-fondations-natives-spike-critique/02-RESEARCH.md (§ Code Examples wrapper Kotlin, Pattern 2 trampoline, Pitfalls 1/3/4)
    - .planning/phases/02-fondations-natives-spike-critique/02-PATTERNS.md (assignation WhisperTranscriber.kt — conventions ThemeRepository.kt : KDoc français à référence décisionnelle, tri des imports)
    - app/src/main/java/com/shortifylocal/ai/data/local/preferences/ThemeRepository.kt (style KDoc + structure de classe)
    - app/build.gradle.kts (bloc dependencies, point d'insertion à côté de libs.ffmpegkit.full.gpl)
    - whisper-native/src/main/cpp/whisper_jni.c (Task 1 — signatures natives à refléter)
  </read_first>
  <action>
    `WordTimestamp.kt` : `class WordTimestamp(val word: String, val startMs: Long, val endMs: Long)` — contrat D-04, unités en millisecondes.

    `WhisperError.kt` : `sealed interface WhisperError` avec `data class LoadFailed(val isOutOfMemory: Boolean)`, `data object Aborted`, `data class InferenceFailed(val code: Int)`, `data object InvalidInput` — per D-06 : l'OOM au chargement est une branche distinguable (le benchmark consigne le verdict « OOM » sans ambiguïté), les codes natifs négatifs (-6/-8/-9) avec flag abort posé élisent `Aborted` (JAMAIS une erreur générique — D-06), jamais d'exception comme chemin contractuel.

    `WhisperResult.kt` : `sealed interface WhisperResult<out T>` avec `data class Success<T>(val value: T)` / `data class Failure(val error: WhisperError)` — porteuse du `Result<List<WordTimestamp>, WhisperError>` de D-06 (le `Result` stdlib Kotlin est mono-paramètre ; ce type scellé IMPLEMENTE le contrat verrouillé sans l'amender).

    `WhisperTranscriber.kt` : `class WhisperTranscriber private constructor(private val modelPath: String, private val contextHandle: Long)`. Companion : `init { System.loadLibrary("whisper_jni") }` (chargement unique, un-seul-.so) ; `fun load(model: File): WhisperResult<WhisperTranscriber>` — fichier absent → `Failure(InvalidInput)` ; `catch (e: OutOfMemoryError)` → `Failure(LoadFailed(isOutOfMemory = true))` ; handle natif 0 → `Failure(LoadFailed(isOutOfMemory = false))`. Contrat D-04 verbatim : `suspend fun transcribe(wav: File, model: File, abort: () -> Boolean): WhisperResult<List<WordTimestamp>>` sur `withContext(Dispatchers.Default)` (jamais le main thread) — `model` doit égal `modelPath` de l'instance (sinon `InvalidInput`, pas de rechargement silencieux) ; wav absent → `InvalidInput`.

    Annulation double circuit D-05 : lancer un watchdog dans le scope `transcribe` (boucle ~100 ms : `if (abort() || !isActive) WhisperNative.nativeRequestCancel()`) ET `coroutineContext[Job]?.invokeOnCancellation { WhisperNative.nativeRequestCancel() }` — les deux circuits (lambda abort + annulation coroutine, OU logique) alimentent le même flag natif lu par `jni_abort_callback` entre chunks (errata E3).

    Résultat : appeler `WhisperNative.nativeTranscribe(handle, wav.absolutePath, "fr", nThreads)` (langue "fr" posée côté Kotlin — défaut natif "en" interdit, Pitfall 4 ; `nThreads` = `Runtime.getRuntime().availableProcessors().coerceAtMost(4)` par défaut, paramètre interne surchargeable pour le benchmark qui le consigne) puis lire `nativeLastStatusCode()` et `nativeAbortFired()` : code 0 → `Success(mots)` ; code négatif ET abortFired → `Failure(Aborted)` (D-06 — jamais générique) ; code négatif sans flag → `Failure(InferenceFailed(code))`. Mapping des éléments `word\tt0\tend` : split sur tab, filtrer mots vides/blancs, conversion unique `startMs = t0Cs * 10L`, `endMs = t1Cs * 10L` (Pitfall 1 — un seul site de conversion centiseconds→ms). Libérer proprement : la classe implémente `AutoCloseable` (appel `nativeFreeContext`) pour un usage `use { }` en instrumentation.

    Objet interne `com.shortifylocal.ai.whisper_native.WhisperNative` (package = namespace du module, symboles préfixés `Java_com_shortifylocal_ai_whisper_1native_WhisperNative_`) déclarant les externals : `nativeLoadContext`, `nativeFreeContext`, `nativeTranscribe`, `nativeRequestCancel`, `nativeLastStatusCode`, `nativeAbortFired` — `internal` pour ne pas fuir hors de la couche data.

    `app/build.gradle.kts` : ajouter `implementation(project(":whisper-native"))   // P2 : wrapper JNI en couche data (D-04) — exercé par instrumentation seule` dans le bloc dependencies, à côté de la ligne ffmpegkit. Ajouter aussi dans `defaultConfig` : `testInstrumentationRunner = "androidx.test.runner.AndroidJUnitRunner"` et les dépendances `androidTestImplementation(libs.androidx.test.runner)` + `androidTestImplementation(libs.androidx.test.ext.junit)` (première suite androidTest du dépôt — catalogue alimenté en Task 1).

    KDoc français citant D-04 (suspend one-shot), D-05 (double circuit), D-06 (sealed exhaustif, OOM distinguable), E2/E3 — style ThemeRepository.kt, imports triés android → com.shortifylocal.ai → tiers → kotlinx/java.
  </action>
  <verify>
    <automated>cd "D:/projets/android/locut" && ./gradlew :app:assembleDebug && SO=$(find whisper-native/build -name libwhisper_jni.so -path '*arm64-v8a*' | head -1) && [ "$(grep -ac Java_com_shortifylocal_ai_whisper_1native_WhisperNative_nativeTranscribe "$SO")" -ge 1 ] && echo "WRAPPER OK"</automated>
    <fails_when>exit non-zero : échec de compilation Kotlin (:app), ou zéro occurrence du symbole JNI `Java_com_shortifylocal_ai_whisper_1native_WhisperNative_nativeTranscribe` dans libwhisper_jni.so (futur UnsatisfiedLinkError)</fails_when>
  </verify>
  <acceptance_criteria>
    - `WordTimestamp` expose exactement `word: String, startMs: Long, endMs: Long` (D-04) ; `WhisperError` est sealed avec la branche `LoadFailed(isOutOfMemory)` distinguable et `Aborted` (D-06)
    - La signature `suspend fun transcribe(wav: File, model: File, abort: () -> Boolean)` existe et tourne sur `Dispatchers.Default` (D-04)
    - Le double circuit D-05 est présent : lecture de `abort()` ET réaction à l'annulation coroutine, les deux convergeant vers `nativeRequestCancel`
    - La conversion cs→ms (`* 10L`) n'apparaît qu'au mapping JNI→Kotlin ; aucun fichier whisper-native ne convertit d'unité
    - `:app` dépend de `project(":whisper-native")` via le catalogue ; aucun code UI/composable ajouté (prohibition spike-UI)
    - KDoc français avec références D-04/D-05/D-06 sur chaque fichier
  </acceptance_criteria>
  <reversibility rating="costly">La signature transcribe/load + WhisperError est le contrat repris par l'instrumentation P2 (plans 02-02/02-04) et le mapping worker Phase 4 (D-04 : changement = 2 call sites cassés).</reversibility>
  <done>Le wrapper contractuel compile, la `.so` embarque les symboles JNI attendus, `:app:assembleDebug` package le tout — per D-04/D-05/D-06.</done>
</task>

<task type="auto">
  <name>Task 3: Pin anti-TOFU des modèles — scripts/fetch_models_p2.sh + manifest SHA-256 (D-01)</name>
  <files>scripts/fetch_models_p2.sh, scripts/models_manifest_p2.txt</files>
  <read_first>
    - .planning/phases/02-fondations-natives-spike-critique/02-CONTEXT.md (D-01, D-02, D-03 — provenance, pas de TOFU, push adb)
    - .planning/phases/02-fondations-natives-spike-critique/02-RESEARCH.md (§ Installation — URLs + tailles + SHA-256 verbatim ; Pitfall 10 push adb)
    - .planning/phases/02-fondations-natives-spike-critique/02-PATTERNS.md (fetch_models_p2.sh — analogues verify_p1.sh + verify_emulator_p1.sh, wrapper adb_, MSYS_NO_PATHCONV)
    - scripts/verify_p1.sh (squelette fail/check_present/exiger_fichier à copier)
    - scripts/verify_emulator_p1.sh (wrapper adb_, tr -d '\r', rejet actionnable)
  </read_first>
  <action>
    `scripts/models_manifest_p2.txt` : manifest versionné anti-TOFU (valeurs copiées des sommes officielles, source + date consignées — D-01), une ligne par modèle au format `nom|url|taille_octets|sha256|source|date` : `ggml-tiny.bin|https://huggingface.co/ggerganov/whisper.cpp/resolve/main/ggml-tiny.bin|77691713|be07e048e1e599ad46341c8d2a135645097a538221678b7acdd1b1919c6e1b21|api/models/ggerganov/whisper.cpp/tree/main (lfs.oid)|2026-09-06` ; idem `ggml-base.bin` 147951465 / `60ed5bc3dd14eea856493d334349b405782ddcaf0028d4b5df4088345fba2efe` ; idem `ggml-small.bin` 487601967 / `1be3a9b2063867b937e64e2ec7483364a79917e157fa98c5d94b5c1fffea987b`. En-tête du manifest : licence Whisper (MIT modifié) ∈ liste blanche E1, bins officiels fp16 standard ggerganov (D-03 — référence conservative, quantization = décision Phase 3).

    `scripts/fetch_models_p2.sh` : pattern batterie P1 — `set -euo pipefail`, `ROOT` déduit de `BASH_SOURCE`, `fail()` (« ÉCHEC: … » stderr + exit 1), `exiger_fichier`, `export MSYS_NO_PATHCONV=1`, wrapper `adb_() { adb ${ANDROID_SERIAL:+-s "$ANDROID_SERIAL"} "$@"; }`, `tr -d '\r'` sur toute sortie `adb shell`. Flux : (1) répertoire cible `$P2_MODELS_DIR` (défaut `$HOME/shortifylocal-p2-models` — HORS repo : zéro `.bin` versionné) ; (2) licence vérifiée à l'exécution : `grep -qi "MIT" whisper-native/whisper.cpp/LICENSE` sinon fail (liste blanche E1) ; (3) pour chaque ligne du manifest : si le fichier existe ET `sha256sum` correspond → skip (idempotent), sinon `curl -L --fail -o` puis re-hash ; divergence → `fail "modèle X — SHA-256 divergent : attendu <pin>, obtenu <lu>"` (rejet actionnable, pas de TOFU) ; taille réelle ≠ taille manifest → fail ; (4) push adb (sauf `--no-push` en argument 1 ou si `SKIP_PUSH=1`) : `adb_ push <fichier> /sdcard/Android/data/com.shortifylocal.ai/files/models/` (D-02 : getExternalFilesDir app-scoped, persistant) ; en cas de refus (Android 11+), repli garanti APK debuggable : push vers `/data/local/tmp/` puis `adb_ shell run-as com.shortifylocal.ai mkdir -p files/models` + `run-as ... cp` (Pitfall 10) ; push possible seulement si `adb devices` non vide — sans appareil, afficher « push différé (mode hôte) » et sortir 0 UNIQUEMENT en `--no-push`. Zéro appel réseau vers autre chose que les 3 URLs manifest (prohibition : aucun téléchargement de modèles DANS l'app — ce script est hôte, dev-time).
  </action>
  <precondition>Le submodule whisper-native/whisper.cpp existe (Task 1) — la vérification de licence lit son fichier LICENSE.</precondition>
  <verify>
    <automated>cd "D:/projets/android/locut" && bash scripts/fetch_models_p2.sh --no-push && [ "$(git ls-files '*.bin' | wc -l)" -eq 0 ] && [ "$(grep -c '^ggml-' scripts/models_manifest_p2.txt)" -eq 3 ] && echo "MODELES OK"</automated>
    <fails_when>exit non-zero : SHA-256 divergent du pin, taille inexacte, licence absente, téléchargement incomplet — ou au moins un .bin versionné au repo, ou manifest ne listant pas les 3 modèles</fails_when>
  </verify>
  <acceptance_criteria>
    - Les 3 fichiers ggml-tiny.bin / ggml-base.bin / ggml-small.bin existent dans $P2_MODELS_DIR avec leurs tailles exactes (77691713 / 147951465 / 487601967) et SHA-256 égaux au manifest
    - `git status --porcelain` ne montre aucun .bin nouveau (zéro binaire versionné — D-01)
    - Sans `--no-push` ni appareil : le script n'échoue pas mais annonce le push différé ; avec `ANDROID_SERIAL` défini et appareil branché : les fichiers sont présents sous /sdcard/Android/data/com.shortifylocal.ai/files/models/ (D-02)
    - Un SHA-256 falsifié dans le manifest de test (édition locale temporaire) fait échouer le script avec message contenant « divergent »
    - Le script contient `MSYS_NO_PATHCONV=1` et le wrapper `adb_` (portabilité Git Bash/Windows)
  </acceptance_criteria>
  <done>Les 3 bins officiels fp16 sont téléchargeables/vérifiés contre le pin anti-TOFU versionné, poussables via adb vers getExternalFilesDir — per D-01/D-02/D-03.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| HuggingFace → machine dev | les bins ggml traversent le réseau — point d'entrée de tout modèle substitué |
| GitHub → submodule | le code C/C++ whisper.cpp entre dans le build |
| Gradle cache → APK | les `.so` du fork entrent tels quels dans l'APK |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-01 | Tampering | fetch_models_p2.sh / bins ggml | high | mitigate | manifest SHA-256 versionné anti-TOFU (D-01), divergence = échec explicite ; interdiction miroirs (D-03) ; zéro .bin versionné |
| T-02-02 | Tampering | submodule whisper.cpp | medium | mitigate | pin SHA complet 371b5a75… + garde CMake FATAL_ERROR actionnable ; jamais de version flottante |
| T-02-SC | Tampering | installs Maven (androidx.test runner 1.7.0 / ext:junit 1.3.0) | low | accept | Package Legitimacy Audit présent dans 02-RESEARCH.md — first-party Google, verdict OK, zéro [ASSUMED]/[SUS] : aucun checkpoint requis |
| T-02-03 | Tampering | flags 16 KB absents du link | high | mitigate | flags errata E2 dans target_link_options + NDK r29 (aligne par défaut) — vérification mécanique au plan 02-03 |
</threat_model>

<verification>
- `./gradlew :whisper-native:assembleDebug :app:assembleDebug` vert (2 ABIs + packaging)
- Garde submodule-absent : build sans `whisper-native/whisper.cpp` → échec explicite « git submodule update --init »
- `bash scripts/fetch_models_p2.sh --no-push` vert + zéro `.bin` versionné
- Symbole JNI `Java_com_shortifylocal_ai_whisper_1native_WhisperNative_nativeTranscribe` présent dans libwhisper_jni.so arm64-v8a
</verification>

<success_criteria>
- Sur un clone frais : `git submodule update --init` puis build OK des 2 ABIs (AC R1 — la démonstration complète clone-frais est à la recette du plan 02-04)
- NDK épinglé r29 `29.0.14206865` dans le catalogue avec date de vérification, câblé via `ndkVersion` (AC R1/R5 prérequis)
- Contrat D-04/D-06 compilable et consommable par l'instrumentation : WordTimestamp / WhisperError / WhisperResult / WhisperTranscriber
- Pin anti-TOFU D-01 opérationnel (manifest + SHA vérifiés au téléchargement + push adb)
</success_criteria>

<output>
Create `.planning/phases/02-fondations-natives-spike-critique/02-01-SUMMARY.md` when done
</output>
