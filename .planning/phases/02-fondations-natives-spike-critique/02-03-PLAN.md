---
phase: 02-fondations-natives-spike-critique
plan: 03
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - scripts/verify_p2.sh
autonomous: true
requirements: [PROJ-02]

estimate:
  tokens: 35000
  raw_tokens: 35000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Clone frais : git submodule update --init puis build OK (arm64-v8a + x86_64 produites) ; submodule absent → échec CMake explicite avec message actionnable (R1)"
    - "FFmpeg fork : conversion WAV 16 kHz rc=0 + volumedetect rc=0 ; FFprobeKit asserte sample_rate=16000, channels=1, codec=pcm_s16le, durée = source ± 0,1 s, taille > 0 (R2)"
    - "Appariement manifest→whisper par rang après normalisation (minuscules, ponctuation retirée) ; silence TTS inter-mots 200 ms éliminant end N = start N+1 ; |start_whisper − start_manifest| ≤ 200 ms par mot apparié (R3)"
    - "≥ 1 mot requis : transcription vide (0 mot) → échec explicite du test, jamais de PASS vacuo (R3)"
    - "Starts monotones non décroissants et end ≥ start (R3)"
    - "WAV benchmark ∈ [15, 20] min incluses (n minimal asserté) ; TTS muet/absent → abort explicite (exit non-zero) ; repli CC0 réservé aux runs benchmark sans ground truth (R4)"
    - "Alignement LOAD ≥ 16384 (2**14) pour chaque lib*.so extrait de l'APK, 32768 accepté (R5)"
    - "Ensemble vide de .so = échec — compte attendu : libs whisper JNI + libs du fork présentes (R5)"
    - "Triple assert anti-émulateur (ro.kernel.qemu ≠ 1, ro.hardware ∉ {goldfish, ranchu}, ro.product.cpu.abi = arm64-v8a) + ANDROID_SERIAL imposé, échec explicite AVANT lancement, jamais de skip silencieux (R6)"
    - "RTF ≤ 0,50 inclusif (0,50 = porte passée) ; batterie < 30 % stricte au départ de session (30 % pile = run invalide non consigné) (R7)"
    - "OOM consigné verdict « OOM » sans RTF, non bloquant ; seule la porte Base bloque (R7)"
    - "Ordre fixe Tiny → Base → Small + cooldown thermique baseline + 2 °C (attente max 10 min, sinon session abortée et documentée) (R7)"
    - "RTF = durée processing wall-clock / durée audio, 2 décimales, formule + arrondi écrits, temps de chargement exclu du RTF et consigné séparément (run warm) (R7)"
    - "Run atomique : toute interruption (écran éteint, kill, abort) → run rejeté et refait en entier ; appareil maintenu éveillé (stay-awake) pendant la session (R7)"
  artifacts:
    - path: scripts/verify_p2.sh
      provides: batterie mécanique locale R5/R6 — 16 KB (llvm-readelf + zipalign), SHA-256 .so fork APK == AAR, ensemble vide = échec, zéro workflow cloud, triple assert getprop
  key_links:
    - from: gradle/libs.versions.toml (entrée ndk)
      to: llvm-readelf utilisé par verify_p2.sh
      via: lecture de l'entrée catalogue pour résoudre $SDK/ndk/<v>/toolchains/llvm/prebuilt/windows-x86_64/bin/llvm-readelf.exe
      why: le script vérifie avec la MÊME toolchain que le build épinglé — aucune divergence d'outil
    - from: AAR fork 8.1.7 résolu (cache Gradle, pin P1 SHA-256 374d3734…)
      to: lib*.so extraites de l'APK
      via: comparaison SHA-256 par nom de lib par ABI
      why: détection de tout patch ELF post-build (prohibition) — les bins embarqués doivent être ceux de Maven Central
    - from: ANDROID_SERIAL (opérateur)
      to: garde getprop avant toute exécution
      via: ro.kernel.qemu / ro.hardware / ro.product.cpu.abi / ro.build.version.sdk
      why: R6 — refus explicite de l'émulateur AVANT lancement, jamais de skip silencieux
  prohibitions:
    - statement: "MUST NOT ré-élire automatiquement un modèle sur critère vitesse-seule (repli Tiny) si la porte Base échoue — décision propriétaire en ERRATA uniquement"
      status: resolved
      verification: "judgment (décision humaine consignée en ERRATA)"
    - statement: "MUST NOT ajouter de code de téléchargement réseau de modèles Whisper (SEC-05 = Phase 3) ; modèles générés (TTS) ou poussés via adb"
      status: resolved
      verification: "test — verify_p2.sh section 7bis : grep -rE OkHttpClient|HttpURLConnection|HttpsURLConnection|DownloadManager|java.net.URL sur whisper-native/src + app/src/androidTest = 0, sinon fail « code de téléchargement réseau détecté (MUST NOT P2) » (bash, fail-closed)"
    - statement: "MUST NOT exposer le spike dans l'UI de production (aucun écran/bouton debug, navigation inchangée à 3 destinations)"
      status: resolved
      verification: "test — grep composables/boutons spike dans app/ = 0 (bash, fail-closed)"
    - statement: "MUST NOT utiliser un service TTS cloud/réseau pour l'asset — android.speech.tts système uniquement, repli CC0 local"
      status: resolved
      verification: "judgment (aucun import SDK TTS tiers)"
    - statement: "MUST NOT créer de workflow CI cloud (GitHub Actions ou équivalent) — verify_p2.sh local uniquement"
      status: resolved
      verification: "test — zéro fichier workflow cloud dans le dépôt (bash, fail-closed)"
    - statement: "MUST NOT patcher/modifier les .so prébuilds du fork (patch ELF post-build) — le check porte sur les binaires tels qu'embarqués"
      status: resolved
      verification: "test + judgment — verify_p2.sh compare le SHA-256 de chaque .so du fork extrait de l'APK à celui de l'AAR résolu (pin Phase 1) ; divergence = échec (bash, fail-closed)"
---

<objective>
Batterie de vérification mécanique locale `scripts/verify_p2.sh` (pattern Phase 1, zéro CI cloud) : alignement 16 KB de chaque `.so` embarqué (`llvm-readelf` + `zipalign -P 16`), intégrité des binaires du fork (SHA-256 APK == AAR résolu), ensemble vide = échec, zéro workflow cloud, et garde opérateur triple assert anti-émulateur (R5 + volet hôte de R6).

**User story (MVP) :** **En tant que** développeur-propriétaire, **je veux** une batterie bash locale qui refuse mécaniquement tout APK mal aligné ou binaire du fork substitué, **afin que** la contrainte 16 KB de Play (échéance 2027) soit vérifiée à chaque itération sans dépendre d'aucune CI cloud.

Purpose: le critère roadmap « alignement 16 KB vérifié mécaniquement » — falsifiable, local, reproductible.
Output: `scripts/verify_p2.sh` vert sur l'APK ARM64 + démonstrations négatives fail-closed.
</objective>

<execution_context>
@D:/projets/android/locut/.zcode/gsd-core/workflows/execute-plan.md
@D:/projets/android/locut/.zcode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-fondations-natives-spike-critique/02-SPEC.md
@.planning/phases/02-fondations-natives-spike-critique/02-CONTEXT.md
@.planning/phases/02-fondations-natives-spike-critique/02-RESEARCH.md
@.planning/phases/02-fondations-natives-spike-critique/02-PATTERNS.md
</context>

## Artifacts this phase produces

- `scripts/verify_p2.sh` — sections : (1) cible getprop (si appareil/ANDROID_SERIAL), (2) APK présent, (3) extraction lib*.so (compte attendu), (4) `llvm-readelf -l` LOAD ≥ 16384/32768 par `.so`, (5) `zipalign -v -c -P 16 4`, (6) SHA-256 `.so` fork APK == AAR, (7) grep zéro workflow cloud, (7bis) grep zéro téléchargement réseau (prohibition MUST NOT P2), (8) récap « P2 OK — n/n sections vertes »

<tasks>

<task type="auto">
  <name>Task 1: verify_p2.sh — 16 KB + intégrité fork + zéro CI cloud + garde getprop</name>
  <files>scripts/verify_p2.sh</files>
  <read_first>
    - .planning/phases/02-fondations-natives-spike-critique/02-SPEC.md (R5 cible complète, R6 volet hôte)
    - .planning/phases/02-fondations-natives-spike-critique/02-RESEARCH.md (§ 16 KB, Code Examples vérification readelf — accepter `align 0x4000` ET `align 2**14`, zipalign build-tools ≥ 35 ; Pitfall 9 résolution NDK)
    - .planning/phases/02-fondations-natives-spike-critique/02-PATTERNS.md (verify_p2.sh — analogue exact verify_p1.sh : helpers fail/check_present/exiger_fichier, en-tête, récap)
    - scripts/verify_p1.sh (squelette à étendre — lignes 1-15, 21-24, 48-58, 112-122)
    - scripts/verify_emulator_p1.sh (wrapper adb_, MSYS_NO_PATHCONV, getprop + tr -d '\r')
    - gradle/libs.versions.toml (entrée ndk — source de la version de llvm-readelf)
  </read_first>
  <action>
    Étendre le pattern `verify_p1.sh` : `set -euo pipefail`, `ROOT` depuis `BASH_SOURCE`, `fail()` (« ÉCHEC: … » stderr + exit 1), `check_present`, `exiger_fichier`, `export MSYS_NO_PATHCONV=1`, wrapper `adb_() { adb ${ANDROID_SERIAL:+-s "$ANDROID_SERIAL"} "$@"; }`, `tr -d '\r'` sur toute sortie `adb shell`. Aucun repli silencieux : fichier absent = échec.

    Résolution d'outils : SDK depuis `local.properties` (`sdk.dir`) ou `$ANDROID_HOME` ; version NDK lue depuis l'entrée `ndk` de `gradle/libs.versions.toml` (grep `^ndk = ` — le script vérifie avec la toolchain du build) → `READELF="$SDK/ndk/$NDK_VER/toolchains/llvm/prebuilt/windows-x86_64/bin/llvm-readelf.exe"` (exiger_fichier) ; `ZIPALIGN` = build-tools ≥ 35 (préférer 37.0.0, détecter la plus haute présente) ; APK = `$P2_APK` si définie, sinon `app/build/outputs/apk/debug/app-debug.apk` (exiger — absent = échec).

    Section cible (R6 volet hôte) : si `ANDROID_SERIAL` est défini OU un appareil est branché — exiger le serial dans `adb devices`, puis asserts getprop : `ro.kernel.qemu` vide/0 (≠ 1) ; `ro.hardware` ∉ {goldfish, ranchu} ; `ro.product.cpu.abi` == arm64-v8a ; `ro.build.version.sdk` ≥ 33. Toute violation → fail explicite AVANT tout autre check (jamais de skip silencieux). Sans appareil : section sautée avec message « cible physique absente — checks APK seuls » (la recette du plan 02-04 impose ANDROID_SERIAL, la section devient bloquante).

    Section 16 KB (R5) : `unzip -o -q "$APK" 'lib/*' -d "$TMP" 2>/dev/null || true` — extraction tolérante OBLIGATOIRE : sur un APK sans `lib/`, unzip sort rc=11 (« filename not matched ») et `set -e` aborterait le script AVANT le compteur, privant la démo négative du Task 2 de son message d'échec ; la tolérance garantit que l'ensemble vide atteint bien le `fail` ci-dessous (fail-closed préservé : le compteur à zéro exit non-zero) ; compter `lib*.so` — ensemble vide → fail « aucun .so dans l'APK (compte attendu : libs whisper JNI + libs fork) » ; exiger `libwhisper_jni.so` présent pour arm64-v8a ET x86_64 et `libffmpegkit.so` présent (compte attendu — les libs du fork = 10 par ABI selon l'AAR 8.1.7) ; pour CHAQUE `lib*.so` : `"$READELF" -l` → chaque ligne LOAD doit présenter un align ≥ 16384 — parser les DEUX notations (`align 0x4000`/`0x8000` hexadécimale ET `align 2**14`/`2**15` symbolique ; awk : convertir `0x*` via strtonum, `2**N` via 2^N) ; toute ligne LOAD < 16384 → fail avec le nom du `.so` et la valeur lue (le fork est déjà aligné — on VÉRIFIE, on ne répare jamais ; prohibition patch).

    Section zipalign : `"$ZIPALIGN" -v -c -P 16 4 "$APK" | tail -1` doit contenir `Verification successful` sinon fail.

    Section intégrité fork : localiser `ffmpeg-kit-full-gpl-8.1.7.aar` dans `$HOME/.gradle/caches/modules-2/files-2.1/dev.ffmpegkit-maintained/` (introuvable → fail, pattern verify_p1.sh ligne 17), unzip en temp, et pour chaque `lib*.so` du fork extraite de l'APK : `sha256sum` APK == `sha256sum` AAR (même nom, même ABI) — divergence → fail « .so du fork divergent de l'AAR résolu — patch ELF post-build interdit ».

    Section zéro CI cloud : compter les workflows cloud dans le dépôt (`ls .github/workflows/*` 2>/dev/null, `.gitlab-ci.yml`, `.circleci/config.yml`) — total ≠ 0 → fail.

    Section 7bis — zéro téléchargement réseau (prohibition MUST NOT P2, fail-closed) : `NET_MATCHES=$(grep -rEl "OkHttpClient|HttpURLConnection|HttpsURLConnection|DownloadManager|java\.net\.URL" whisper-native/src app/src/androidTest 2>/dev/null || true)` — compteur (nb lignes de $NET_MATCHES) ≠ 0 → fail « code de téléchargement réseau détecté (MUST NOT P2) » en listant les fichiers matchés. Les répertoires absents sont tolérés par le `2>/dev/null || true` (le grep n'abort pas sous set -e) ; seul un match réel déclenche l'échec. Section comptée dans le récap n/n.

    Récap final : « P2 OK — n/n sections vertes » avec compteur de sections (pattern verify_p1.sh lignes 121-122).
  </action>
  <precondition>`./gradlew :app:assembleDebug` a produit app-debug.apk (plans 02-01) ; le cache Gradle contient l'AAR fork 8.1.7 (pin Phase 1).</precondition>
  <verify>
    <automated>cd "D:/projets/android/locut" && ./gradlew :app:assembleDebug && bash scripts/verify_p2.sh</automated>
    <fails_when>exit non-zero ou ligne « ÉCHEC: » dans la sortie : alignement LOAD insuffisant, zipalign non successful, `.so` du fork divergent de l'AAR, ensemble vide, workflow cloud détecté, ou cible émulateur sous ANDROID_SERIAL</fails_when>
  </verify>
  <acceptance_criteria>
    - Le script sort « P2 OK » sur l'APK de build courant avec toutes les sections vertes et un compteur explicite
    - Chaque `lib*.so` extraite est passée à llvm-readelf (le log liste chaque fichier et son align LOAD) — les deux notations 0x4000 et 2**14 sont acceptées (testé en lisant les libs du fork, forme 0x4000)
    - La version de llvm-readelf provient de l'entrée `ndk` du catalogue (le script la grep, pas de chemin en dur d'une autre NDK)
    - La section getprop applique les 4 asserts (qemu/hardware/abi/sdk) quand ANDROID_SERIAL est défini
    - Le grep workflows cloud retourne 0 sur ce dépôt
    - La section 7bis retourne compteur 0 sur ce dépôt (aucun match OkHttpClient/HttpURLConnection/HttpsURLConnection/DownloadManager/java.net.URL dans whisper-native/src + app/src/androidTest) ; un fichier de test falsifié contenant un de ces symboles fait échouer le script avec « MUST NOT P2 »
    - En-tête + helpers copiés du pattern verify_p1.sh (fail sur stderr + exit 1, aucun repli silencieux)
  </acceptance_criteria>
  <done>verify_p2.sh vérifie mécaniquement 16 KB + intégrité fork + zéro cloud + cible physique sur l'APK réel — per R5/R6.</done>
</task>

<task type="auto">
  <name>Task 2: Démonstrations négatives fail-closed (ensemble vide, alignement insuffisant)</name>
  <files>scripts/verify_p2.sh</files>
  <read_first>
    - scripts/verify_p2.sh (Task 1 — variables d'env supportées : P2_APK, P2_TMP)
    - .planning/phases/02-fondations-natives-spike-critique/02-SPEC.md (AC R5 : « le script échoue sur alignement < 16384, ensemble vide ou .so divergent »)
  </read_first>
  <action>
    Rendre les démonstrations négatives possibles SANS modifier le script pour le production : `verify_p2.sh` accepte `P2_APK` (chemin APK alternatif, défaut app-debug.apk) — aucune autre porte dérogeable (fail-closed assumé). Démonstrations à exécuter une fois (et consigner dans le SUMMARY) : (1) APK sans lib/ : construire un zip factice (`mktemp -d`, un fichier texte, `jar -cMf "$TMP/fake.apk" -C "$TMP" dummy.txt` — l'outil `zip` est absent du Git Bash hôte, `jar` du JDK 17 est présent, vérifié 2026-09-06) et lancer `P2_APK=<fake> bash scripts/verify_p2.sh` → exit non-zero avec « aucun .so » ; (2) si un émulateur est disponible : `ANDROID_SERIAL=<serial émulateur> bash scripts/verify_p2.sh` → fail explicite « ÉMULATEUR » (la garde refuse AVANT les checks APK). Restaurer l'état (rm -rf du temp). Ne JAMAIS affaiblir un check pour faire passer une démonstration.
  </action>
  <precondition>Task 1 terminée (script complet) ; pour la démo émulateur : un AVD x86_64 existe (prouvé Phase 1, scripts/verify_emulator_p1.sh).</precondition>
  <verify>
    <automated>cd "D:/projets/android/locut" && TMP=$(mktemp -d) && echo x > "$TMP/dummy.txt" && jar -cMf "$TMP/fake.apk" -C "$TMP" dummy.txt && if P2_APK="$TMP/fake.apk" bash scripts/verify_p2.sh > "$TMP/out.log" 2>&1; then rm -rf "$TMP"; echo "GARDE MORTE"; exit 1; else grep -q "ÉCHEC" "$TMP/out.log" && echo "FAIL-CLOSED OK"; fi; RC=$?; rm -rf "$TMP"; exit $RC</automated>
    <fails_when>exit 0 du script sur un APK sans aucun .so (garde ensembliste morte) ou absence de ligne « ÉCHEC: » dans la sortie</fails_when>
  </verify>
  <acceptance_criteria>
    - La démonstration ensemble-vide sort non-zero avec un message contenant « ÉCHEC »
    - La démonstration émulateur (si AVD disponible) sort non-zero avant tout check APK avec un message explicite — consignée dans le SUMMARY avec le serial utilisé
    - `bash scripts/verify_p2.sh` reste vert sur l'APK réel après les démonstrations (aucun état muté)
  </acceptance_criteria>
  <done>Les gardes R5 sont prouvées falsifiables : ensemble vide refuse, script vert restauré — fail-closed mécanique.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| APK extrait → checker ELF | un `.so` substitué/patché ne doit jamais passer |
| opérateur → cible adb | un émulateur annoncé comme physique ne doit jamais exécuter le spike |
| AAR Maven Central → comparaison | référence d'intégrité du fork (pin P1) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-08 | Tampering | `.so` du fork patché/remplacé dans l'APK | high | mitigate | SHA-256 de chaque lib APK == même lib dans l'AAR 8.1.7 résolu (pin P1 374d3734…) ; divergence = échec — les bins embarqués restent ceux de Maven Central |
| T-02-09 | Tampering | faux vert (script qui ne vérifie rien) | medium | mitigate | démonstrations négatives fail-closed (ensemble vide, émulateur) ; helpers/copie du pattern P1 éprouvé ; aucune variable désactivant un check |
| T-02-10 | Elevation of Privilege | dérive vers CI cloud (check perdu) | low | mitigate | section zéro workflow cloud dans le script lui-même + prohibition scellée (grep = 0) |
</threat_model>

<verification>
- `bash scripts/verify_p2.sh` vert sur app-debug.apk (sections getprop/APK/readelf/zipalign/SHA/cloud)
- Démo ensemble-vide : exit non-zero avec « ÉCHEC »
- Démo émulateur (si AVD) : refus explicite avant les checks APK
</verification>

<success_criteria>
- AC R5 : alignement LOAD ≥ 16384 (32768 accepté) pour chaque `.so`, zipalign -P 16 OK, ensemble vide = échec, SHA-256 fork APK == AAR, exit non-zero sinon, zéro workflow CI cloud
- Volet hôte R6 : le triple assert getprop refuse explicitement une cible émulateur sous ANDROID_SERIAL
</success_criteria>

<output>
Create `.planning/phases/02-fondations-natives-spike-critique/02-03-SUMMARY.md` when done
</output>
