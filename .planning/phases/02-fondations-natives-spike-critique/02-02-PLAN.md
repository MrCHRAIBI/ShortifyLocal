---
phase: 02-fondations-natives-spike-critique
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - app/src/androidTest/java/com/shortifylocal/ai/SpikeAssets.kt
  - app/src/androidTest/java/com/shortifylocal/ai/TtsAssetGenerationTest.kt
  - app/src/androidTest/java/com/shortifylocal/ai/FfmpegForkTest.kt
  - app/src/androidTest/java/com/shortifylocal/ai/WordTranscriptionTest.kt
  - app/src/androidTest/java/com/shortifylocal/ai/AbortPropagationTest.kt
autonomous: true
requirements: [PROJ-02]
user_setup:
  - service: appareil physique ARM64
    why: "Le spike s'exécute exclusivement sur le téléphone ARM64 Android 13+ (R6 : triple assert anti-émulateur, émulateur interdit)"
    env_vars:
      - name: ANDROID_SERIAL
        source: "adb devices — serial du téléphone ARM64 branché en USB (débogage USB activé)"
    dashboard_config:
      - task: "Installer un moteur TTS avec voix française (Synthèse vocale Google) et vérifier la lecture française"
        location: "Téléphone — Paramètres > Gestion globale > Synthèse vocale"

estimate:
  tokens: 70000
  raw_tokens: 70000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Clone frais : git submodule update --init puis build OK (arm64-v8a + x86_64 produites) ; submodule absent → échec CMake explicite avec message actionnable (R1)"
    - "FFmpeg fork : conversion WAV 16 kHz rc=0 + volumedetect rc=0 ; FFprobeKit asserte sample_rate=16000, channels=1, codec=pcm_s16le, durée = source ± 0,1 s, taille > 0 (R2)"
    - "Appariement manifest→whisper par rang après normalisation (minuscules, ponctuation retirée) ; silence TTS inter-mots 200 ms éliminant end N = start N+1 ; |start_whisper − start_manifest| ≤ 200 ms par mot apparié (R3)"
    - "≥ 1 mot requis : transcription vide (0 mot) → échec explicite du test, jamais de PASS vacuo (R3)"
    - "Starts monotones non décroissants et end ≥ start (R3)"
    - "WAV benchmark ∈ [15, 20] min incluses (n minimal asserté) ; TTS muet/absent → abort explicite (exit non-zero) ; repli CC0 réservé aux runs benchmark sans ground truth (R4)"
    - "Alignement LOAD ≥ 16384 (2**14) pour chaque lib*.so extrait de l'APK, 32768 accepté (R5)"
    - "Ensemble vide de .so = échec — compte attendu : libs whisper JNI + libs du fork présentes (R5)"
    - "Triple assert anti-émulateur (ro.kernel.qemu ≠ 1, ro.hardware ∉ {goldfish, ranchu}, ro.product.cpu.abi = arm64-v8a) + ANDROID_SERIAL imposé, échec explicite AVANT lancement, jamais de skip silencieux (R6)"
    - "RTF ≤ 0,50 inclusif (0,50 = porte passée) ; batterie < 30 % stricte au départ de session (30 % pile = run invalide non consigné) (R7)"
    - "OOM consigné verdict « OOM » sans RTF, non bloquant ; seule la porte Base bloque (R7)"
    - "Ordre fixe Tiny → Base → Small + cooldown thermique baseline + 2 °C (attente max 10 min, sinon session abortée et documentée) (R7)"
    - "RTF = durée processing wall-clock / durée audio, 2 décimales, formule + arrondi écrits, temps de chargement exclu du RTF et consigné séparément (run warm) (R7)"
    - "Run atomique : toute interruption (écran éteint, kill, abort) → run rejeté et refait en entier ; appareil maintenu éveillé (stay-awake) pendant la session (R7)"
  artifacts:
    - path: app/src/androidTest/java/com/shortifylocal/ai/ (première suite androidTest du dépôt)
      provides: SpikeAssets (garde R6 in-test + génération TTS), TtsAssetGenerationTest (R4), FfmpegForkTest (R2), WordTranscriptionTest (R3), AbortPropagationTest (D-06/E3)
    - path: getExternalFilesDir(null)/{wavs,models,out}/ sur l'appareil
      provides: artefacts persistants inspectables en adb (D-02) — WAV fonctionnel + manifest, WAV benchmark, modèles
  key_links:
    - from: TtsAssetGenerationTest (synthèse mot-à-mot + silences 200 ms)
      to: manifest.json {word,start,end}
      via: durée FFprobe de chaque WAV mot converti 16 kHz mono s16, concat sample-exact
      why: ground truth par construction (D-08) — le contrat ±200 ms de R3 n'a pas de référence sinon
    - from: FfmpegForkTest conversion
      to: contrat d'entrée whisper (16 kHz mono pcm_s16le)
      via: FFprobeKit.getMediaInformation — 5 propriétés assertées
      why: isole les fautes FFmpeg des fautes whisper (R2, non reporté à Phase 4)
    - from: AbortPropagationTest (abort = { true })
      to: WhisperError.Aborted
      via: code natif négatif (-6/-8/-9) + flag g_abort_fired
      why: preuve mécanique E3 au niveau wrapper (D-06) — jamais de succès tronqué
  prohibitions:
    - statement: "MUST NOT ré-élire automatiquement un modèle sur critère vitesse-seule (repli Tiny) si la porte Base échoue — décision propriétaire en ERRATA uniquement"
      status: resolved
      verification: "judgment (décision humaine consignée en ERRATA)"
    - statement: "MUST NOT ajouter de code de téléchargement réseau de modèles Whisper (SEC-05 = Phase 3) ; modèles générés (TTS) ou poussés via adb"
      status: resolved
      verification: "test — grep zéro client HTTP/DownloadManager dans whisper-native/ + androidTest (bash, fail-closed)"
    - statement: "MUST NOT exposer le spike dans l'UI de production (aucun écran/bouton debug, navigation inchangée à 3 destinations)"
      status: resolved
      verification: "test — grep composables/boutons spike dans app/ = 0 (bash, fail-closed)"
    - statement: "MUST NOT utiliser un service TTS cloud/réseau pour l'asset — android.speech.tts système uniquement, repli CC0 local"
      status: resolved
      verification: "judgment (aucun import SDK TTS tiers)"
    - statement: "MUST NOT créer de workflow CI cloud (GitHub Actions ou équivalent) — verify_p2.sh local uniquement"
      status: resolved
      verification: "test — zéro fichier workflow cloud dans le dépôt (bash, fail-closed)"
    - statement: "MUST NOT patcher/modifier les .so prébuilds du fork (patch ELF post-build) — le check porte sur les binaires tels qu'embarqués"
      status: resolved
      verification: "test + judgment — verify_p2.sh compare le SHA-256 de chaque .so du fork extrait de l'APK à celui de l'AAR résolu (pin Phase 1) ; divergence = échec (bash, fail-closed)"
---

<objective>
Preuves on-device du spike : assets TTS générés par le test (R4/D-08/D-09), commandes FFmpeg du fork 8.1.7 prouvées avec assertions FFprobeKit (R2), transcription mot-à-mot validée contre le manifest (R3) et abort callback exercé au niveau wrapper (D-06/E3) — exclusivement sur l'appareil physique ARM64 via `connectedDebugAndroidTest`.

**User story (MVP) :** **En tant que** développeur-propriétaire, **je veux** que chaque brique native (FFmpeg fork, transcription whisper, annulation) soit prouvée par une assertion mécanique sur le téléphone réel, **afin que** le pipeline des phases 4-5 repose sur des faits mesurés et non sur des hypothèses d'émulateur.

Purpose: le cœur du spike critique — la première exécution réelle de code natif du projet.
Output: 4 classes androidTest + helper `SpikeAssets`, suite `connectedDebugAndroidTest` verte sur le serial physique.
</objective>

<execution_context>
@D:/projets/android/locut/.zcode/gsd-core/workflows/execute-plan.md
@D:/projets/android/locut/.zcode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-fondations-natives-spike-critique/02-SPEC.md
@.planning/phases/02-fondations-natives-spike-critique/02-CONTEXT.md
@.planning/phases/02-fondations-natives-spike-critique/02-RESEARCH.md
@.planning/phases/02-fondations-natives-spike-critique/02-PATTERNS.md
</context>

## Artifacts this phase produces

- `SpikeAssets.kt` : garde `assertPhysicalArm64()` (Build.SUPPORTED_ABIS[0] == arm64-v8a, Build.HARDWARE hors {goldfish, ranchu}, `ro.kernel.qemu` via reflection `android.os.SystemProperties`), générateur TTS mot-à-mot (défense en profondeur R6 + assets R4)
- `TtsAssetGenerationTest.kt` : WAV fonctionnel ~1 min + manifest exact + garde D-09 + WAV benchmark [15, 20] min
- `FfmpegForkTest.kt` : conversion 16 kHz mono + volumedetect + 5 assertions FFprobeKit + annulation `FFmpegKit.cancel(sessionId)` câblée sur `invokeOnCancellation` (E3)
- `WordTranscriptionTest.kt` : mot-à-mot vs manifest (normalisation, rang, ±200 ms, monotone, end ≥ start, ≥ 1 mot)
- `AbortPropagationTest.kt` : `WhisperError.Aborted` élu mécaniquement (lambda abort + annulation coroutine)

<tasks>

<task type="auto">
  <name>Task 1: SpikeAssets (garde R6 + TTS mot-à-mot + garde D-09) et TtsAssetGenerationTest (R4/D-08/D-02)</name>
  <files>app/src/androidTest/java/com/shortifylocal/ai/SpikeAssets.kt, app/src/androidTest/java/com/shortifylocal/ai/TtsAssetGenerationTest.kt</files>
  <read_first>
    - .planning/phases/02-fondations-natives-spike-critique/02-SPEC.md (R4 — bornes, abort TTS muet, repli CC0 benchmark-only)
    - .planning/phases/02-fondations-natives-spike-critique/02-CONTEXT.md (D-02 stockage externe app-scoped, D-08 texte français, D-09 garde TTS dégradé)
    - .planning/phases/02-fondations-natives-spike-critique/02-RESEARCH.md (Pattern 3 attente TTS, Pitfalls 6/8, Code Examples triple assert, section volumedetect)
    - .planning/phases/02-fondations-natives-spike-critique/02-PATTERNS.md (conventions ThemeModeTest.kt — JUnit4, KDoc français à référence décisionnelle)
    - app/src/test/java/com/shortifylocal/ai/presentation/theme/ThemeModeTest.kt (conventions JUnit4 du dépôt)
    - app/src/main/java/com/shortifylocal/ai/data/native/whisper/WhisperTranscriber.kt (plan 02-01 — API consommée)
  </read_first>
  <action>
    `SpikeAssets.kt` (objet utilitaire androidTest) : (1) `fun assertPhysicalArm64()` — `Build.SUPPORTED_ABIS.firstOrNull() == "arm64-v8a"` sinon IllegalStateException « ABI != arm64-v8a » ; `Build.HARDWARE !in setOf("goldfish", "ranchu")` sinon « ÉMULATEUR DÉTECTÉ » ; `ro.kernel.qemu` lu via reflection `Class.forName("android.os.SystemProperties").getMethod("get", String::class.java).invoke(null, "ro.kernel.qemu")` — si la reflection échoue, s'appuyer sur les checks Build (le garde primaire reste le script hôte getprop du plan 02-03) ; appelé au `@Before` de CHAQUE classe de test du spike (R6 : défense en profondeur, jamais de skip silencieux). (2) Génération TTS (D-08) : constante `FRENCH_TEXT` versionnée dans le fichier — 90 à 110 mots TOUS distincts, aucun nom propre, aucun chiffre, aucune abréviation/trait d'union/apostrophe, ponctuation limitée à points et virgules ; assertion du compte (90 ≤ mots distincts ≤ 110). (3) `suspend fun ensureFunctionalAsset(ctx: Context): Pair<File, File>` — TextToSpeech(ctx) : statut init ≠ SUCCESS → abort explicite ; `setLanguage(Locale.FRENCH)` + `isLanguageAvailable` ∈ {LANG_MISSING_DATA, LANG_NOT_SUPPORTED} → abort explicite ; pour chaque mot : conversion du WAV mot en 16 kHz mono pcm_s16le via FFmpeg du fork, durée mesurée via FFprobeKit, `synthesizeToFile` (signature `CharSequence, Bundle, File, String` — l'ancienne HashMap est dépréciée) avec `utteranceId = "w_$i"` ; le retour n'est qu'une mise en file (Pitfall 6) — suspendre sur `UtteranceProgressListener.onDone/onError(utteranceId, errorCode)` via CompletableDeferred, `onError` → abort explicite avec errorCode ; concat FFmpeg (`-f concat -safe 0`) alternant mot + silence 200 ms généré par `anullsrc=r=16000:cl=mono:t=0.2` ; manifest JSON (kotlinx-serialization) `{word, start, end}` en ms accumulés par construction (end_i = start_i + durée mot — le silence n'appartient à aucun mot, élimine end N = start N+1, R3) ; sorties sous `ctx.getExternalFilesDir(null)/wavs/` (D-02 : persistant, pas de nettoyage). (4) Garde D-09 exécutée AVANT retour : `|duréeWAV(final, FFprobe) − end_dernier_manifest| ≤ 0.5` ET `mean_volume` (pass volumedetect, regex tolérante sur `session.getAllLogsAsString()`, Pitfall 8) ≥ plancher de silence `-45.0` dB — sinon IllegalStateException « TTS muet ou dégradé — abort explicite (D-09), jamais de preuve dégradée ». (5) `suspend fun ensureBenchmarkWav(ctx: Context, functional: File): File` — plus petit n tel que n × durée_fonctionnelle ≥ 900 s (concat n copies via `-f concat`), assert durée totale ≤ 1200 s sinon abort ; bornes [15, 20] min INCLUSES ; écrit sous `wavs/benchmark.wav` (non versionné).

    `TtsAssetGenerationTest.kt` : `@Before` → `assertPhysicalArm64()` ; test 1 « l'asset fonctionnel respecte la garde D-09 et le manifest est exact par construction » — appelle `ensureFunctionalAsset`, ré-asserte D-09 + manifest ≥ 90 entrées, chaque `end ≥ start` ; test 2 « le WAV benchmark est dans les bornes 15 à 20 minutes incluses » — appelle `ensureBenchmarkWav`, FFprobe durée ∈ [900, 1200] s. TTS absent/muet → les aborts explicites font ÉCHOUER le test (jamais de PASS vacuo, R4). Repli CC0 : NON implémenté ici — il est réservé aux runs benchmark du plan 02-04 (admissible uniquement sans ground truth, SPEC R4).
  </action>
  <precondition>Un appareil physique ARM64 Android 13+ est branché (ANDROID_SERIAL exporté) avec un moteur TTS français installé ; `:app` dépend de `:whisper-native` (plan 02-01 terminé).</precondition>
  <verify>
    <automated>cd "D:/projets/android/locut" && ANDROID_SERIAL="$ANDROID_SERIAL" ./gradlew :app:connectedDebugAndroidTest --tests "com.shortifylocal.ai.TtsAssetGenerationTest"</automated>
    <fails_when>exit non-zero : garde anti-émulateur déclenchée, TTS absent/muet (D-09), bornes [15, 20] min violées, ou manifest vide — aucun PASS vacuo toléré</fails_when>
  </verify>
  <verify>
    <automated>cd "D:/projets/android/locut" && [ "$(adb -s "$ANDROID_SERIAL" shell "ls /sdcard/Android/data/com.shortifylocal.ai/files/wavs/" | tr -d '\r' | grep -cE '\.wav|manifest')" -ge 2 ]</automated>
    <fails_when>compteur inférieur à 2 (pas au moins un .wav et le manifest.json sous files/wavs/) : les artefacts ne persistent pas sous getExternalFilesDir (contrat D-02 d'inspection adb violé) — la comparaison -ge 2 rend la garde falsifiable (exit non-zero)</fails_when>
  </verify>
  <acceptance_criteria>
    - `SpikeAssets.assertPhysicalArm64` est appelé depuis le @Before des 4 classes de test du spike (grep `assertPhysicalArm64` ≥ 4 occurrences dans androidTest)
    - Le manifest liste entre 90 et 110 mots, tous distincts, sans chiffre ni apostrophe ni trait d'union ; chaque entrée a end ≥ start
    - La garde D-09 compare durée WAV FFprobe vs manifest (tolérance 0,5 s) ET mean_volume vs plancher −45 dB ; une voix muette simule un échec explicite
    - Le WAV benchmark est produit par concat du WAV fonctionnel avec n minimal et sa durée FFprobe ∈ [900, 1200] s
    - Les artefacts vivent sous getExternalFilesDir(null)/wavs/ et survivent au teardown du test (D-02)
    - KDoc français citant R4/D-08/D-09/D-02 ; noms de tests en backticks français
  </acceptance_criteria>
  <done>L'asset fonctionnel + manifest existent sur l'appareil avec la garde D-09 verte, le WAV benchmark est borné [15, 20] min — per R4/D-08/D-09/D-02.</done>
</task>

<task type="auto">
  <name>Task 2: FfmpegForkTest — preuve fork 8.1.7 (R2) + annulation E3</name>
  <files>app/src/androidTest/java/com/shortifylocal/ai/FfmpegForkTest.kt</files>
  <read_first>
    - .planning/phases/02-fondations-natives-spike-critique/02-SPEC.md (R2 — les 5 propriétés WAV, rc=0 seul insuffisant)
    - .planning/phases/02-fondations-natives-spike-critique/02-RESEARCH.md (Pitfalls 7/8 — pool 10 threads, volumedetect dans les logs ; Security Domain — executeWithArguments)
    - docs/ERRATA-2026-09-05.md (E3 — FFmpegKit.cancel(sessionId) via invokeOnCancellation)
    - app/src/androidTest/java/com/shortifylocal/ai/SpikeAssets.kt (Task 1 — source WAV fonctionnelle)
    - AAR fork 8.1.7 (javap/sources.jar du cache Gradle pour signatures exactes : FFmpegKit.executeWithArgumentsAsync, FFprobeKit.getMediaInformation, ReturnCode, MediaInformation.getSize/getDuration, StreamInformation.getSampleRate/getChannelLayout/getCodec)
  </read_first>
  <action>
    Helper suspend dans la classe : `executeAwait(args: Array<String>): FFmpegSession` via `suspendCancellableCoroutine` autour de `FFmpegKit.executeWithArgumentsAsync(args, completeCallback)` — préférence à la variante arguments tableau (aucun parsing d'espaces des chemins — mitigation injection, Security Domain RESEARCH ; fallback `executeAsync(String)` seulement si la signature tableau est absente de l'AAR, à vérifier par javap avant) ; `invokeOnCancellation { FFmpegKit.cancel(session.sessionId) }` — câblage E3 verbatim ; les callbacks arrivent sur le pool fixe de 10 threads du fork (Pitfall 7) — ne jamais y toucher l'UI, suspendre uniquement sur la complétion de session. `@Before` → `assertPhysicalArm64()`.

    Test 1 « la conversion en WAV 16 kHz mono pcm_s16le retourne rc=0 » : source = WAV fonctionnel (`SpikeAssets.ensureFunctionalAsset`, régénéré si absent — les tests restent indépendants), sortie sous `wavs/converted.wav` ; args `-y -i <src> -ar 16000 -ac 1 -c:a pcm_s16le <out>` ; assert `ReturnCode.isSuccess(session.returnCode)`.

    Test 2 « le pass volumedetect retourne rc=0 et expose mean_volume » : args `-i <converted> -af volumedetect -f null -` ; rc succès + regex tolérante `mean_volume:\s*(-?\d+(?:\.\d+)?)` sur `session.getAllLogsAsString()` (Pitfall 8 — le résultat vit dans les LOGS, pas dans rc ; [ASSUMED A2] format standard FFmpeg figé par assertion tolérante).

    Test 3 « FFprobeKit asserte les 5 propriétés du contrat d'entrée whisper » : `FFprobeKit.getMediaInformation(out)` → stream : `sampleRate == 16000`, `channelLayout` contient `mono` (ou `channels == 1`), `codec == "pcm_s16le"`, `|durée(out) − durée(source)| ≤ 0.1` s, `size > 0` — les 5 assertées mécaniquement (R2 : rc=0 seul ne suffit pas).

    Test 4 « cancel(sessionId) interrompt effectivement la session (E3) » : entrée longue = concat ~3 copies du WAV fonctionnel en temporaire (quelques minutes d'audio, transcode > 2 s), lancer `executeAwait`, annuler la coroutine après 500 ms (`job.cancel()`), attendre → `session.returnCode == ReturnCode.CANCEL` (session annulée via `FFmpegKit.cancel(sessionId)`, jamais `cancel()` global SIGINT toutes sessions).
  </action>
  <precondition>Appareil physique ARM64 branché (ANDROID_SERIAL) ; plan 02-01 terminé (fork 8.1.7 déjà dépendance :app, wrapper compilé).</precondition>
  <verify>
    <automated>cd "D:/projets/android/locut" && ANDROID_SERIAL="$ANDROID_SERIAL" ./gradlew :app:connectedDebugAndroidTest --tests "com.shortifylocal.ai.FfmpegForkTest"</automated>
    <fails_when>exit non-zero : rc ≠ 0 sur la conversion ou volumedetect, une des 5 propriétés FFprobe fausse (sample_rate/canals/codec/durée/taille), ou cancel ≠ ReturnCode.CANCEL</fails_when>
  </verify>
  <acceptance_criteria>
    - Les 4 tests sont verts sur le serial physique ARM64 ; `connectedDebugAndroidTest` est la première suite androidTest exécutée du dépôt
    - Les 5 propriétés WAV sont assertées par code (grep `16000`, `pcm_s16le`, `mono`/`channels`, tolérance `0.1`, `size > 0` dans le fichier de test) — pas seulement rc
    - `invokeOnCancellation` câble `FFmpegKit.cancel(session.sessionId)` (E3) ; aucun appel à `FFmpegKit.cancel()` global
    - La conversion et volumedetect passent par la variante arguments tableau (aucune concaténation de String commande avec chemins)
    - KDoc français citant R2 + E3
  </acceptance_criteria>
  <done>Le fork 8.1.7 exécute réellement des commandes sur l'appareil : conversion + volumedetect rc=0, 5 propriétés FFprobe vraies, annulation E3 effective — per R2.</done>
</task>

<task type="auto">
  <name>Task 3: WordTranscriptionTest (R3) + AbortPropagationTest (D-06/E3)</name>
  <files>app/src/androidTest/java/com/shortifylocal/ai/WordTranscriptionTest.kt, app/src/androidTest/java/com/shortifylocal/ai/AbortPropagationTest.kt</files>
  <read_first>
    - .planning/phases/02-fondations-natives-spike-critique/02-SPEC.md (R3 — contrat d'appariement complet, 0 mot = échec)
    - .planning/phases/02-fondations-natives-spike-critique/02-CONTEXT.md (D-05 double circuit, D-06 branche Aborted exercée)
    - .planning/phases/02-fondations-natives-spike-critique/02-RESEARCH.md (Pitfalls 1/3/5 — centisecondes, codes négatifs, max_len=1 sémantique)
    - app/src/main/java/com/shortifylocal/ai/data/native/whisper/WhisperTranscriber.kt + WhisperError.kt + WhisperResult.kt (plan 02-01 — contrat à consommer)
    - app/src/androidTest/java/com/shortifylocal/ai/SpikeAssets.kt (Task 1 — WAV + manifest)
    - scripts/fetch_models_p2.sh (plan 02-01 — provenance des modèles poussés)
  </read_first>
  <action>
    `WordTranscriptionTest.kt` : `@Before` → `assertPhysicalArm64()` puis `runBlocking { SpikeAssets.ensureFunctionalAsset(...) }` ; charge le modèle Tiny poussé sous `getExternalFilesDir(null)/models/ggml-tiny.bin` via `WhisperTranscriber.load` — `Failure` → échec explicite actionnable (modèle absent → rappeler `bash scripts/fetch_models_p2.sh`). Test principal « la transcription mot-à-mot respecte le contrat manifest ±200 ms » : `WhisperTranscriber.use { transcribe(wavFonctionnel, modelTiny, abort = { false }) }` → `Success(words)` sinon fail avec l'erreur sérialisée ; ASSERTIONS R3 complètes : `words.isNotEmpty()` (0 mot → fail « transcription vide — PASS vacuo interdit ») ; normalisation identique des deux côtés (`lowercase()` + `replace(Regex("[^\\p{L}\\p{N}\\s]"), "")` + trim) puis appariement manifest→whisper PAR RANG sur min(taille manifest, taille words) — la regex DOIT être écrite avec les backslashes doubles Kotlin `Regex("[^\\p{L}\\p{N}\\s]")` exactement : la forme avalée `Regex("[^p{L}p{N}s]")` (backslash perdu) détruirait les mots (classe ne gardant que {,L,N,s}) ; pour chaque paire : `|startWhisper − startManifest| ≤ 200` ms ; starts de words non décroissants ; chaque `endMs ≥ startMs` ; cross-check unités (Pitfall 1) : `words.first().startMs < 2000` et `words.last().endMs ≤ duréeWAV_ms + 500` — un facteur 10 est ainsi détecté structurellement. Les paramètres natifs (`token_timestamps=true, max_len=1, split_on_word=true`, langue `fr`) sont posés par le wrapper (plan 02-01) — le test les documente en KDoc sans les re-posérer. Ponctuation collée type `chien,` absorbée par la normalisation (Pitfall 5) — jamais de re-comptage strict segments == mots.

    `AbortPropagationTest.kt` : `@Before` idem. Test 1 « un abort kotlin true est propagé en WhisperError Aborted » : charger Tiny puis `transcribe(wavFonctionnel, modelTiny, abort = { true })` → attendu `Failure(Aborted)` — JAMAIS `Success` (succès tronqué = échec du test, Pitfall 3), JAMAIS `InferenceFailed` (les codes négatifs -6/-8/-9 avec flag posé élisent Aborted, D-06) ; le callback natif est évalué après la première passe encodeur (~quelques secondes sur 1 min d'audio) — timeout de test généreux (2 min). Test 2 « l'annulation coroutine alimente le même circuit (D-05) » : lancer `async { transcribe(...) }` avec `abort = { false }`, `cancel()` après 300 ms, `await` dans un runCatching → résultat final `Failure(Aborted)` (circuit coroutine OU lambda, errata E3).
  </action>
  <precondition>Modèles poussés sur l'appareil : `bash scripts/fetch_models_p2.sh` (avec push, ANDROID_SERIAL défini) a déposé ggml-tiny.bin sous files/models/ ; plans 02-01 + Task 1 de ce plan terminés.</precondition>
  <verify>
    <automated>cd "D:/projets/android/locut" && ANDROID_SERIAL="$ANDROID_SERIAL" ./gradlew :app:connectedDebugAndroidTest --tests "com.shortifylocal.ai.WordTranscriptionTest" --tests "com.shortifylocal.ai.AbortPropagationTest"</automated>
    <fails_when>exit non-zero : 0 mot transcrit (PASS vacuo), un |Δstart| > 200 ms, starts décroissants, end < start, ou l'abort résout en Success tronqué / InferenceFailed au lieu de WhisperError.Aborted</fails_when>
  </verify>
  <acceptance_criteria>
    - L'appariement applique exactement : normalisation minuscules + retrait ponctuation, puis rang — le code du test contient la regex de normalisation sous la forme EXACTE `Regex("[^\\p{L}\\p{N}\\s]")` (backslashes doubles Kotlin ; grep `\\p\{L\}` ≥ 1 occurrence et grep `[^p{L}p{N}s]` = 0) et l'itération par rang
    - Toute transcription à 0 mot fait échouer le test avec un message explicite (jamais de PASS vacuo)
    - Les asserts unités (premier start < 2000 ms) rendent un bug centisecondes structurellement détectable
    - AbortPropagationTest assert `Failure(Aborted)` sur les DEUX circuits (lambda `abort = { true }` et `cancel()` coroutine) et échoue sur tout autre verdict
    - Les deux classes appellent `assertPhysicalArm64()` au @Before
    - KDoc français citant R3/D-05/D-06/E3
  </acceptance_criteria>
  <done>La transcription mot-à-mot est validée mécaniquement contre le manifest TTS et l'abort callback est prouvé au niveau wrapper — per R3/D-05/D-06.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| moteur TTS système → WAV | le contenu audio généré conditionne toute la ground truth |
| fork ffmpeg-kit (`.so` prébuilds) → sessions | commandes exécutées avec des chemins de fichiers |
| wrapper JNI → whisper.cpp | entrée WAV/paramètres traversant la frontière JVM/C |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-04 | Repudiation | TTS muet/dégradé → preuve biaisée | high | mitigate | garde D-09 pré-assert (durée ±0,5 s + plancher volumedetect −45 dB) → abort explicite, jamais de preuve dégradée |
| T-02-05 | Tampering | injection d'arguments FFmpeg par chemin | low | mitigate | chemins construits depuis getExternalFilesDir (aucun input utilisateur en P2) + `executeWithArgumentsAsync(String[])` (pas de concat shell) |
| T-02-06 | Tampering | PASS vacuo (test vert sans preuve réelle) | medium | mitigate | 0 mot = échec explicite ; garde R6 au @Before de chaque classe ; artefacts persistants inspectables adb (D-02) |
| T-02-07 | DoS | session FFmpeg jamais annulée (CPU brûlé) | medium | mitigate | câblage E3 : `invokeOnCancellation → FFmpegKit.cancel(sessionId)`, testé mécaniquement (Task 2, test 4) |
</threat_model>

<verification>
- `ANDROID_SERIAL=<serial physique> ./gradlew :app:connectedDebugAndroidTest` : classes TtsAssetGenerationTest, FfmpegForkTest, WordTranscriptionTest, AbortPropagationTest vertes
- Artefacts persistants sous getExternalFilesDir (WAV fonctionnel, manifest.json, WAV benchmark, converted.wav) inspectables en adb
- La garde in-test refuse un émulateur avec un message explicite (défense en profondeur du triple assert R6)
</verification>

<success_criteria>
- R2 : conversion rc=0 + volumedetect rc=0 + 5 propriétés FFprobe vraies sur l'appareil physique
- R3 : mot-à-mot validé (≥ 1 mot, ±200 ms par rang, monotone, end ≥ start) avec token_timestamps=true / max_len=1 / split_on_word=true
- R4 : assets générés par le test, bornes [15, 20] min, garde D-09, aucun binaire versionné
- D-06 : branche Aborted exercée mécaniquement sur les deux circuits d'annulation
</success_criteria>

<output>
Create `.planning/phases/02-fondations-natives-spike-critique/02-02-SUMMARY.md` when done
</output>
