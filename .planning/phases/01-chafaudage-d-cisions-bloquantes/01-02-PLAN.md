---
phase: 01-chafaudage-d-cisions-bloquantes
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - app/src/main/java/com/shortifylocal/ai/domain/model/.gitkeep
  - app/src/main/java/com/shortifylocal/ai/domain/repository/.gitkeep
  - app/src/main/java/com/shortifylocal/ai/domain/usecase/.gitkeep
  - app/src/main/java/com/shortifylocal/ai/data/repository/.gitkeep
  - app/src/main/java/com/shortifylocal/ai/worker/.gitkeep
  - app/src/main/java/com/shortifylocal/ai/util/.gitkeep
  - app/src/main/java/com/shortifylocal/ai/data/local/room/AppDatabase.kt
  - app/src/main/java/com/shortifylocal/ai/di/DatabaseModule.kt
  - app/src/main/java/com/shortifylocal/ai/presentation/theme/ThemeMode.kt
  - app/src/main/java/com/shortifylocal/ai/data/local/preferences/ThemeRepository.kt
  - app/src/main/java/com/shortifylocal/ai/presentation/theme/ThemeViewModel.kt
  - app/src/test/java/com/shortifylocal/ai/presentation/theme/ThemeModeTest.kt
autonomous: true
requirements: [PROJ-01]
user_setup: []
estimate:
  tokens: 40000
  raw_tokens: 40000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "AppDatabase v1 sans entités compile avec exportSchema=true et le schéma app/schemas/com.shortifylocal.ai.data.local.room.AppDatabase/1.json est généré et commité"
    - "La base Room est nommée shortify_local.db via le module DI"
    - "La préférence thème (SYSTEM/LIGHT/DARK) est lue et écrite réellement dans DataStore « settings » ; absent = suit le système (D-05)"
  prohibitions:
    - statement: "Aucun fallbackToDestructiveMigration() sur la base Room initialisée en P1"
      status: resolved
      verification: "grep -rn 'fallbackToDestructiveMigration' app/ → 0 résultat"
    - statement: "domain/ reste du Kotlin pur — zéro import android.*/androidx.*"
      status: resolved
      verification: "grep -rnE '^import (android|androidx)\\.' app/src/main/java/com/shortifylocal/ai/domain/ → 0 résultat"
---

<objective>
Plomberie prête pour les Phases 3–4 : arborescence Clean Architecture normative (packages `presentation/ domain/ data/ worker/ di/ util/`), Room 2.8.4 initialisée v1 sans entités avec schéma exporté commité, et la persistance du thème clair/sombre lue/écrite réellement dans DataStore (D-05) derrière un ViewModel injectable — le « real DB read/write » du walking skeleton.

Purpose: SPEC R4/R5 — le squelette d'architecture est contrôlable mécaniquement (purity grep, schéma commité) et la bascule de thème a une source de vérité persistée avant que la coquille visuelle (01-03) ne la consomme.
Output: `AppDatabase` v1 + `DatabaseModule`, `ThemeMode` + `ThemeRepository` (DataStore) + `ThemeViewModel` + `ThemeModeTest`, packages squelette avec .gitkeep.
</objective>

<execution_context>
@D:/projets/android/locut/.zcode/gsd-core/workflows/execute-plan.md
@D:/projets/android/locut/.zcode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-chafaudage-d-cisions-bloquantes/01-SPEC.md
@.planning/phases/01-chafaudage-d-cisions-bloquantes/01-CONTEXT.md
@.planning/phases/01-chafaudage-d-cisions-bloquantes/01-RESEARCH.md
@docs/01-Description.md
</context>

## Artifacts this phase produces

(Vue d'ensemble phase complète dans 01-01-PLAN.md — ce plan livre la partie plomberie/persistance.)
- **Packages** : `domain/{model,repository,usecase}`, `data/repository`, `data/local/room`, `data/local/preferences`, `worker/`, `util/` (les vides avec .gitkeep)
- **Classes** : `AppDatabase`, `DatabaseModule`, `ThemeMode` (+ logique pure testable), `ThemeRepository`, `ThemeViewModel`, `ThemeModeTest`
- **Schéma** : `app/schemas/com.shortifylocal.ai.data.local.room.AppDatabase/1.json` (commité)

<tasks>

<task type="auto">
  <name>Task 1: Arborescence Clean Architecture + Room v1 sans entités + DI</name>
  <files>app/src/main/java/com/shortifylocal/ai/domain/model/.gitkeep, app/src/main/java/com/shortifylocal/ai/domain/repository/.gitkeep, app/src/main/java/com/shortifylocal/ai/domain/usecase/.gitkeep, app/src/main/java/com/shortifylocal/ai/data/repository/.gitkeep, app/src/main/java/com/shortifylocal/ai/worker/.gitkeep, app/src/main/java/com/shortifylocal/ai/util/.gitkeep, app/src/main/java/com/shortifylocal/ai/data/local/room/AppDatabase.kt, app/src/main/java/com/shortifylocal/ai/di/DatabaseModule.kt</files>
  <read_first>
    - .planning/phases/01-chafaudage-d-cisions-bloquantes/01-SPEC.md (exigences 4 et 5 + acceptance : purity grep, schéma commité, nom de DB)
    - .planning/phases/01-chafaudage-d-cisions-bloquantes/01-RESEARCH.md (Recommended Project Structure ; Pitfalls 5 et 7 ; Assumptions A1/A2/A3)
    - docs/01-Description.md §3.1–§3.2 (couches, règles strictes, arborescence normative)
    - app/build.gradle.kts (plugin room + ksp déjà câblés au plan 01-01)
  </read_first>
  <action>
Créer l'arborescence normative docs/01 §3.2 restreinte au P1 : packages `domain/model`, `domain/repository`, `domain/usecase`, `data/repository`, `worker/`, `util/` — chacun avec un `.gitkeep` (git ne trace pas les dossiers vides). Les sous-packages presentation sont créés par 01-03 ; `data/local/room` et `data/local/preferences` naissent avec leurs fichiers ci-dessous.

Créer `data/local/room/AppDatabase.kt` : `@Database(entities = [], version = 1, exportSchema = true) abstract class AppDatabase : RoomDatabase()` avec companion `const val DB_NAME = "shortify_local.db"`. ZÉRO entité (les 4 entités arrivent en Phase 3) — si KSP refuse une base sans entité (hypothèse A3 du research), NE PAS ajouter d'entité jeton : STOP et escalader au propriétaire de spec. Aucun appel de migration destructive nulle part (rappel DATA-05) — le builder est construit sans configurer de fallback.

Créer `di/DatabaseModule.kt` : `@Module @InstallIn(SingletonComponent::class) object DatabaseModule` avec `@Provides @Singleton fun provideDatabase(@ApplicationContext context: Context): AppDatabase = Room.databaseBuilder(context, AppDatabase::class.java, AppDatabase.DB_NAME).build()` — le nom `shortify_local.db` vit dans ce builder (aucune ouverture réelle en P1 : rien n'injecte la base, le provider est paresseux).

Vérifier la pureté du domain : aucun fichier du package `domain/` n'importe android.* ni androidx.* (en P1 le package ne contient que des .gitkeep — le grep d'acceptation pose la barre dès maintenant pour les Phases 3+).
  </action>
  <verify>
    <automated>cd "D:/projets/android/locut" && ./gradlew :app:assembleDebug</automated>
    <fails_when>non-zero exit, or "BUILD FAILED" in output</fails_when>
    <automated>cd "D:/projets/android/locut" && test -f app/schemas/com.shortifylocal.ai.data.local.room.AppDatabase/1.json && python -c "import json;json.load(open('app/schemas/com.shortifylocal.ai.data.local.room.AppDatabase/1.json'))" 2>/dev/null || node -e "JSON.parse(require('fs').readFileSync('app/schemas/com.shortifylocal.ai.data.local.room.AppDatabase/1.json','utf8'))"</automated>
    <fails_when>schema JSON absent (non-zero test) or unparseable JSON</fails_when>
    <automated>cd "D:/projets/android/locut" && git check-ignore app/schemas/com.shortifylocal.ai.data.local.room.AppDatabase/1.json; echo "ignored=$?"</automated>
    <fails_when>output prints "ignored=0" (the schema would be excluded by .gitignore — pitfall 5)</fails_when>
    <automated>cd "D:/projets/android/locut" && grep -rn "fallbackToDestructiveMigration" app/ | wc -l</automated>
    <fails_when>the count is greater than 0</fails_when>
    <automated>cd "D:/projets/android/locut" && grep -rnE "^import (android|androidx)\." app/src/main/java/com/shortifylocal/ai/domain/ | wc -l</automated>
    <fails_when>the count is greater than 0</fails_when>
  </verify>
  <acceptance_criteria>
    - `./gradlew :app:assembleDebug` vert avec AppDatabase v1 (zero entité) et le module DI compilés par KSP2
    - `app/schemas/com.shortifylocal.ai.data.local.room.AppDatabase/1.json` existe, est un JSON valide, version 1, et n'est pas gitigné
    - `DatabaseModule` fournit AppDatabase avec le nom « shortify_local.db »
    - Les 6 packages squelette existent avec .gitkeep ; purity grep domain = 0
    - Aucune occurrence de migration destructive dans app/
  </acceptance_criteria>
  <done>SPEC R5 complète : plomberie Room/DI prouvée par le build + schéma commité ; SPEC R4 : arborescence en place et contrôlable.</done>
</task>

<task type="auto" tdd="true" >
  <name>Task 2: Persistance thème DataStore (D-05) + ViewModel + test unitaire</name>
  <reversibility rating="costly">D-05 : la sémantique « suit le système tant qu'aucun choix explicite, sinon override persisté » devient le comportement observable du premier lancement — la changer après coup exige de redéfinir la logique de préférence et sa persistance. Implémenter exactement la sémantique D-05.</reversibility>
  <files>app/src/main/java/com/shortifylocal/ai/presentation/theme/ThemeMode.kt, app/src/main/java/com/shortifylocal/ai/data/local/preferences/ThemeRepository.kt, app/src/main/java/com/shortifylocal/ai/presentation/theme/ThemeViewModel.kt, app/src/test/java/com/shortifylocal/ai/presentation/theme/ThemeModeTest.kt</files>
  <read_first>
    - .planning/phases/01-chafaudage-d-cisions-bloquantes/01-CONTEXT.md (D-05 verbatim : isSystemInDarkTheme comme valeur initiale, l'override explicite prime ensuite)
    - .planning/phases/01-chafaudage-d-cisions-bloquantes/01-RESEARCH.md (Pattern 7 — idiome DataStore 1.2.1 ; « Don't Hand-Roll » ligne persistence)
    - docs/02-UI-UX.md §1.5 (thème clair/sombre persisté DataStore)
    - app/src/main/java/com/shortifylocal/ai/data/local/preferences/ (fichiers du package s'ils existent déjà)
  </read_first>
  <behavior>
    - ThemeMode expose `nextExplicitMode(current: ThemeMode, systemInDarkTheme: Boolean): ThemeMode` : SYSTEM + système sombre → LIGHT ; SYSTEM + système clair → DARK ; LIGHT → DARK ; DARK → LIGHT
    - ThemeMode.valueOf(themeMode.name) fait un aller-retour exact pour les 3 valeurs (SYSTEM, LIGHT, DARK)
    - Le DataStore « settings » stocke le NOM de l'enum sous la clé `theme_mode` ; clé absente ⇒ lecture = SYSTEM (suit le système, D-05)
    - setThemeMode écrit atomiquement via edit { } et le flow émet la nouvelle valeur
  </behavior>
  <action>
Créer `presentation/theme/ThemeMode.kt` : `enum class ThemeMode { SYSTEM, LIGHT, DARK }` + la fonction pure `nextExplicitMode` décrite dans <behavior> (companion object) — la logique de bascule est pure et testable, sans dépendance Android.

Créer `data/local/preferences/ThemeRepository.kt` (Pattern 7) : délégation fichier `private val Context.themeDataStore by preferencesDataStore(name = "settings")` ; `val THEME_MODE_KEY = stringPreferencesKey("theme_mode")` ; `class ThemeRepository @Inject constructor(@ApplicationContext private val context: Context)` avec `val themeMode: Flow<ThemeMode> = context.themeDataStore.data.map { it[THEME_MODE_KEY]?.let { v -> ThemeMode.valueOf(v) } ?: ThemeMode.SYSTEM }` et `suspend fun setThemeMode(mode: ThemeMode) { context.themeDataStore.edit { it[THEME_MODE_KEY] = mode.name } }`. La préférence thème ne va JAMAIS dans Room ni dans le stockage chiffré (non sensible → DataStore ; le coffre-fort est une décision Phase 3).

Créer `presentation/theme/ThemeViewModel.kt` : `@HiltViewModel class ThemeViewModel @Inject constructor(private val themeRepository: ThemeRepository) : ViewModel()` avec `val themeMode: StateFlow<ThemeMode> = themeRepository.themeMode.stateIn(viewModelScope, SharingStarted.WhileSubscribed(5_000), ThemeMode.SYSTEM)` et `fun toggleTheme(systemInDarkTheme: Boolean)` lançant dans viewModelScope `themeRepository.setThemeMode(ThemeMode.nextExplicitMode(themeMode.value, systemInDarkTheme))` (premier tap : mode explicite opposé au thème effectif, puis LIGHT↔DARK).

Écrire D'ABORD le test `app/src/test/java/com/shortifylocal/ai/presentation/theme/ThemeModeTest.kt` (JUnit 4) : aller-retour valueOf(name) pour les 3 valeurs ; nextExplicitMode(SYSTEM, true)=LIGHT ; nextExplicitMode(SYSTEM, false)=DARK ; nextExplicitMode(LIGHT, _)=DARK ; nextExplicitMode(DARK, _)=LIGHT. Le lancer (ROUGE impossible ici car les classes n'existent pas encore — écrire test + classes dans le même commit si le runner exige la compilation : le RED est la compilation du test avant l'implémentation de nextExplicitMode), puis implémenter jusqu'au VERT.

Structure MVVM respectée : ViewModel expose StateFlow, aucune logique dans le Composable (la consommation arrive au plan 01-03).
  </action>
  <verify>
    <automated>cd "D:/projets/android/locut" && ./gradlew :app:testDebugUnitTest</automated>
    <fails_when>non-zero exit, or "BUILD FAILED", or "0 tests completed" in the test report summary</fails_when>
    <automated>cd "D:/projets/android/locut" && grep -c "testcase" app/build/test-results/testDebugUnitTest/TEST-com.shortifylocal.ai.presentation.theme.ThemeModeTest.xml</automated>
    <fails_when>the count is less than 5 (round-trip x3 + nextExplicitMode cases must all be present)</fails_when>
    <automated>cd "D:/projets/android/locut" && ./gradlew :app:assembleDebug</automated>
    <fails_when>non-zero exit, or "BUILD FAILED" in output</fails_when>
  </verify>
  <acceptance_criteria>
    - ThemeModeTest passe : 5+ cas verts (aller-retour enum + les 4 transitions nextExplicitMode)
    - ThemeRepository lit/écrit la clé `theme_mode` du DataStore « settings », absent ⇒ SYSTEM
    - ThemeViewModel est @HiltViewModel injecté par constructeur, expose StateFlow<ThemeMode>, et compile dans le graphe Hilt (assembleDebug vert en est la preuve)
    - La sémantique D-05 exacte : premier lancement = système ; toute bascule écrit une préférence explicite
  </acceptance_criteria>
  <done>La source de vérité du thème existe et est testée : DataStore « settings », sémantique D-05, ViewModel injectable consommé par 01-03.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| App → stockage local | DataStore « settings » (préférence non sensible) ; Room v1 (vide) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-05 | Information disclosure | DataStore « settings » | low | accept | N'y vivre que la préférence thème (non sensible) ; solde/hash/clé iront exclusivement dans le stockage chiffré Phase 3 (SEC-01) — séparation posée dès P1 |
| T-01-06 | Tampering | Base Room | low | accept | Base vide en P1, nom fixe, aucune migration destructive autorisée (DATA-05) ; HMAC/intégrité = Phase 3 |
| T-01-SC | Tampering | Artefacts KSP2/Hilt/Room résolus au build | high | mitigate | Déjà couverts par T-01-SC du plan 01-01 (catalogue épinglé + checksum) ; ce plan n'introduit aucune nouvelle dépendance |
</threat_model>

<verification>
- assembleDebug vert : graphe Hilt valide (DatabaseModule), schéma Room 1.json généré + non-gitigné
- testDebugUnitTest vert : ThemeModeTest (5+ cas)
- Purity domain = 0 ; aucune migration destructive ; DB nommée shortify_local.db
</verification>

<success_criteria>
Plomberie R4/R5 en place et mécaniquement contrôlable ; la persistance du thème (D-05) existe, testée, avec un ViewModel prêt à être branché sur la coquille du plan 01-03.
</success_criteria>

<output>
Create `.planning/phases/01-chafaudage-d-cisions-bloquantes/01-02-SUMMARY.md` when done
</output>
