---
phase: 01-chafaudage-d-cisions-bloquantes
plan: 03
type: execute
wave: 3
depends_on: ["01-02"]
files_modified:
  - app/src/main/java/com/shortifylocal/ai/presentation/theme/Color.kt
  - app/src/main/java/com/shortifylocal/ai/presentation/theme/Shape.kt
  - app/src/main/java/com/shortifylocal/ai/presentation/theme/Shadow.kt
  - app/src/main/java/com/shortifylocal/ai/presentation/theme/Theme.kt
  - app/src/main/java/com/shortifylocal/ai/presentation/navigation/AppShell.kt
  - app/src/main/java/com/shortifylocal/ai/presentation/ui/home/HomeScreen.kt
  - app/src/main/java/com/shortifylocal/ai/presentation/ui/history/HistoryScreen.kt
  - app/src/main/java/com/shortifylocal/ai/presentation/ui/settings/SettingsScreen.kt
  - app/src/main/java/com/shortifylocal/ai/MainActivity.kt
  - app/src/main/res/values/strings.xml
autonomous: true
requirements: [PROJ-01]
estimate:
  tokens: 40000
  raw_tokens: 40000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "L'APK lancé affiche une coquille à 3 onglets (Accueil / Historique / Paramètres) navigables, libellés via ressources"
    - "Les tokens Soft-Clean sont définis dans le thème : couleurs §1.1 clair/sombre, rayons 32/24/16/28, UNE seule ombre douce verticale (5 % clair / 30 % sombre)"
    - "Le thème suit le système au premier lancement puis applique l'override persisté ; la bascule de la coquille écrit dans DataStore via ThemeViewModel (D-05)"
    - "Structure RTL-ready : alignements Start/End uniquement, supportsRtl=true (déjà posé au manifest au plan 01-01)"
  prohibitions:
    - statement: "Aucune chaîne UI codée en dur dans la coquille — les libellés d'onglets passent par les ressources"
      status: resolved
      verification: "grep -rnE '(text|label|contentDescription)[[:space:]]*=[[:space:]]*\"' app/src/main/java/com/shortifylocal/ai/presentation/ → 0 résultat"
    - statement: "Aucun alignement Left/Right — Start/End uniquement (structure RTL dès le premier composant)"
      status: resolved
      verification: "grep -rnE 'Alignment\\.(Left|Right)|TextAlign\\.(Left|Right)' app/src/main/java/com/shortifylocal/ai/ → 0 résultat"
    - statement: "Aucun neumorphisme (ombre double clair+sombre, élément de la couleur du fond) dans la coquille"
      status: resolved
      verification: "judgment — revue visuelle du thème contre docs/02-UI-UX.md §0/§1.2 (vérification humaine fin de phase, plan 01-04)"
---

<objective>
Coquille Compose thémée Soft-Clean : tokens couleurs/rayons/ombre du design system (docs/02 §1.1–1.3) mappés sur Material 3, navigation à 3 destinations vides (Accueil / Historique / Paramètres) via Navigation Compose, et câblage MainActivity → ThemeViewModel → DataStore avec bouton de bascule — le walking skeleton UI complet sur données réelles.

Purpose: SPEC R2/R3 — l'app lancée montre les 3 onglets navigables thémés Soft-Clean clair/sombre, et la bascule persiste (l'install/launch + persistance sur cibles sont prouvés au plan 01-04).
Output: `SoftCleanTokens`/`SoftCleanShapes`/`softShadow`/`ShortifyLocalTheme` (+ `LocalSoftCleanDark`), `AppShell` + 3 écrans, MainActivity câblé, chaînes ressources.
</objective>

<execution_context>
@D:/projets/android/locut/.zcode/gsd-core/workflows/execute-plan.md
@D:/projets/android/locut/.zcode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-chafaudage-d-cisions-bloquantes/01-SPEC.md
@.planning/phases/01-chafaudage-d-cisions-bloquantes/01-CONTEXT.md
@.planning/phases/01-chafaudage-d-cisions-bloquantes/01-RESEARCH.md
@docs/02-UI-UX.md
</context>

## Artifacts this phase produces

(Vue d'ensemble phase complète dans 01-01-PLAN.md — ce plan livre la coquille visuelle.)
- **Classes** : `SoftCleanTokens`, `SoftCleanShapes`, `Modifier.softShadow`, `ShortifyLocalTheme` + `LocalSoftCleanDark`, `AppShell` (routes `accueil`/`historique`/`parametres`), `HomeScreen`, `HistoryScreen`, `SettingsScreen`
- **Ressources ajoutées** : `tab_accueil`, `tab_historique`, `tab_parametres`, `settings_appearance`, `theme_toggle_cd` dans strings.xml
- **Modifié** : `MainActivity` (setContent → ShortifyLocalTheme → AppShell)

<tasks>

<task type="auto">
  <name>Task 1: Tokens Soft-Clean + thème Material 3 (D-05/D-06)</name>
  <files>app/src/main/java/com/shortifylocal/ai/presentation/theme/Color.kt, app/src/main/java/com/shortifylocal/ai/presentation/theme/Shape.kt, app/src/main/java/com/shortifylocal/ai/presentation/theme/Shadow.kt, app/src/main/java/com/shortifylocal/ai/presentation/theme/Theme.kt</files>
  <read_first>
    - docs/02-UI-UX.md §1.1–§1.3 (tokens NORMATIFS : couleurs, ombre unique, rayons — valeurs exactes)
    - .planning/phases/01-chafaudage-d-cisions-bloquantes/01-CONTEXT.md (D-05 sémantique, D-06 mapping à discrétion sous contrainte du résultat visuel)
    - .planning/phases/01-chafaudage-d-cisions-bloquantes/01-RESEARCH.md (Pattern 6 — mapping recommandé + signature Modifier.shadow vérifiée dans androidx)
    - app/src/main/java/com/shortifylocal/ai/presentation/theme/ThemeMode.kt (créé au plan 01-02 — réutilisé, ne pas le redéfinir)
  </read_first>
  <action>
Créer `presentation/theme/Color.kt` : `object SoftCleanTokens` avec EXACTEMENT les valeurs normatives docs/02 §1.1 + ombre §1.2 — BackgroundLight 0xFFEEF0F2, BackgroundDark 0xFF0E0E10, CardLight 0xFFFFFFFF, CardDark 0xFF1A1A1E, PrimaryTextLight 0xFF1A1A1A, PrimaryTextDark 0xFFFFFFFF, SecondaryTextLight 0xFF8A8F98, SecondaryTextDark 0xFF9A9AA2, PrimaryButtonLight 0xFF111111, PrimaryButtonDark 0xFFFFFFFF, AccentActiveLight 0xFFE4590C, AccentActiveDark 0xFFFF7A3D, ScoreExcellent 0xFF22C55E, ShadowLight = noir alpha 0.05f, ShadowDark = noir alpha 0.30f.

Créer `presentation/theme/Shape.kt` : `val SoftCleanShapes = Shapes(extraSmall = RoundedCornerShape(16.dp), small = RoundedCornerShape(16.dp), medium = RoundedCornerShape(24.dp), large = RoundedCornerShape(28.dp), extraLarge = RoundedCornerShape(32.dp))` — mapping D-06 (champs/chips 16, boutons 16, images 24, pilule nav 28, grandes cartes/modales 32).

Créer `presentation/theme/Shadow.kt` : `fun Modifier.softShadow(shape: Shape, darkTheme: Boolean): Modifier = shadow(elevation = 6.dp, shape = shape, clip = false, ambientColor = if (darkTheme) SoftCleanTokens.ShadowDark else SoftCleanTokens.ShadowLight, spotColor = idem)` (Option A du research, §1.2 autorise Modifier.shadow OU drawBehind ; UNE seule ombre — jamais deux superposées).

Créer `presentation/theme/Theme.kt` : `val LocalSoftCleanDark = compositionLocalOf { false }` ; `@Composable fun ShortifyLocalTheme(themeMode: ThemeMode, content: @Composable () -> Unit)` avec `val dark = when (themeMode) { SYSTEM -> isSystemInDarkTheme(); LIGHT -> false; DARK -> true }` (D-05 exact), construction `val base = if (dark) darkColorScheme() else lightColorScheme()` puis `MaterialTheme(colorScheme = base.copy(background = bg, surface = bg, surfaceContainer = card, onBackground = primaryText, onSurface = primaryText, onSurfaceVariant = secondaryText, primary = primaryButton, onPrimary = blanc en clair / noir en sombre, secondary = accentActive, onSecondary = Color.White), shapes = SoftCleanShapes, content = { CompositionLocalProvider(LocalSoftCleanDark provides dark, content) })`. Typographie : défaut Material 3 (la typographie complète et les polices bundlées sont Phase 6 — hors périmètre P1).
  </action>
  <verify>
    <automated>cd "D:/projets/android/locut" && ./gradlew :app:assembleDebug</automated>
    <fails_when>non-zero exit, or "BUILD FAILED" in output</fails_when>
    <automated>cd "D:/projets/android/locut" && for hex in 0xFFEEF0F2 0xFF0E0E10 0xFF1A1A1E 0xFF8A8F98 0xFF9A9AA2 0xFF111111 0xFFE4590C 0xFFFF7A3D 0xFF22C55E; do grep -q "$hex" app/src/main/java/com/shortifylocal/ai/presentation/theme/Color.kt || echo "MISSING $hex"; done</automated>
    <fails_when>any "MISSING" line is printed (a normative token hex is absent from Color.kt)</fails_when>
    <automated>cd "D:/projets/android/locut" && grep -c "RoundedCornerShape" app/src/main/java/com/shortifylocal/ai/presentation/theme/Shape.kt</automated>
    <fails_when>the count is less than 4 (radii 16/24/28/32 must all appear)</fails_when>
  </verify>
  <acceptance_criteria>
    - Color.kt contient les 14 tokens normatifs (grep positifs des 9 hex distincts + alpha 0.05f/0.30f)
    - Shape.kt mappe les 4 rayons normatifs 32/24/16/28 sur les slots M3
    - softShadow utilise les couleurs d'ombre tokens (5 % clair / 30 % sombre) — une seule ombre
    - ShortifyLocalTheme implémente le when D-05 exact et fournit LocalSoftCleanDark
    - assembleDebug vert (les composables theme compilent)
  </acceptance_criteria>
  <done>Design system Soft-Clean définis en thème (SPEC R2/acceptance 6) ; la sémantique D-05 est dans le when du thème.</done>
</task>

<task type="tracer">
  <name>Task 2: Coquille 3 onglets + bascule thème câblée DataStore (chemin complet)</name>
  <files>app/src/main/java/com/shortifylocal/ai/presentation/navigation/AppShell.kt, app/src/main/java/com/shortifylocal/ai/presentation/ui/home/HomeScreen.kt, app/src/main/java/com/shortifylocal/ai/presentation/ui/history/HistoryScreen.kt, app/src/main/java/com/shortifylocal/ai/presentation/ui/settings/SettingsScreen.kt, app/src/main/java/com/shortifylocal/ai/MainActivity.kt, app/src/main/res/values/strings.xml</files>
  <read_first>
    - .planning/phases/01-chafaudage-d-cisions-bloquantes/01-RESEARCH.md (Pattern 8 — navigation shell, options navigate, icônes)
    - .planning/phases/01-chafaudage-d-cisions-bloquantes/01-SPEC.md (exigences 2 et 3 + acceptance 9/10)
    - docs/02-UI-UX.md §1.6 (règles RTL) et §3 (navigation — le BottomNavPill définitif est Phase 6, P1 = NavigationBar standard stylée par les tokens)
    - app/src/main/java/com/shortifylocal/ai/presentation/theme/ThemeViewModel.kt et ThemeMode.kt (créés au plan 01-02)
    - app/src/main/java/com/shortifylocal/ai/MainActivity.kt (placeholder du plan 01-01 — état courant à lire avant édition)
  </read_first>
  <action>
Compléter `strings.xml` : `tab_accueil` = Accueil, `tab_historique` = Historique, `tab_parametres` = Paramètres, `settings_appearance` = Apparence, `theme_toggle_cd` = « Basculer le thème clair/sombre ». (FR par défaut — i18n 7 langues = Phase 7.)

Créer `presentation/ui/home/HomeScreen.kt`, `ui/history/HistoryScreen.kt`, `ui/settings/SettingsScreen.kt` : écrans vides ASSUMÉS P1 (décision SPEC — contenu réel Phase 6). Home et History : `Box(modifier = Modifier.fillMaxSize(), contentAlignment = Alignment.Center)` contenant un `Text(stringResource(R.string.tab_accueil))` (respectivement tab_historique) stylé titleLarge/couleur onBackground. SettingsScreen(themeMode: ThemeMode, onThemeToggle: () -> Unit) : Column centrée avec un Text du libellé settings_appearance + une rangée horizontale (Arrangement/Alignment en Start/End ou Center UNIQUEMENT) : libellé du mode courant via ressources (réutiliser theme_toggle_cd pour la description d'accessibilité) et `IconButton(onClick = onThemeToggle) { Icon(imageVector = <icône selon le mode : SYSTEM → Icons.Filled.BrightnessAuto, LIGHT → Icons.Filled.LightMode, DARK → Icons.Filled.DarkMode>, contentDescription = stringResource(R.string.theme_toggle_cd)) }` (icônes du material-icons-extended).

Créer `presentation/navigation/AppShell.kt` : `@Composable fun AppShell(themeMode: ThemeMode, onThemeToggle: (Boolean) -> Unit)` — `rememberNavController()` ; Scaffold(bottomBar = { val dark = LocalSoftCleanDark.current; NavigationBar(containerColor = MaterialTheme.colorScheme.surfaceContainer, modifier = Modifier.softShadow(shape = RoundedCornerShape(28.dp), darkTheme = dark)) { liste des 3 TabSpec(route, labelRes, icon) : « accueil »/tab_accueil/Icons.AutoMirrored.Filled.Home, « historique »/tab_historique/Icons.AutoMirrored.Filled.History (icône directionnelle auto-flippée RTL), « parametres »/tab_parametres/Icons.Filled.Settings ; NavigationBarItem(selected = currentBackStackEntry route == route, onClick = navController.navigate(route) { launchSingleTop = true; restoreState = true; popUpTo(graph.findStartDestination().id) { saveState = true } }, icon, label = Text(stringResource(labelRes))) } }) ; contenu = NavHost(navController, startDestination = "accueil") avec les 3 composable() : HomeScreen(), HistoryScreen(), SettingsScreen(themeMode = themeMode, onThemeToggle = { onThemeToggle(LocalSoftCleanDark.current) }). TOUT libellé via ressources ; TOUT alignement Start/End/Center.

Réécrire `MainActivity.kt` : `class MainActivity : ComponentActivity()` avec onCreate → `setContent { val vm: ThemeViewModel = hiltViewModel(); val mode by vm.themeMode.collectAsStateWithLifecycle(); ShortifyLocalTheme(themeMode = mode) { AppShell(themeMode = mode, onThemeToggle = { systemDark -> vm.toggleTheme(systemDark) }) } }` — le chemin complet toggle → ViewModel → Repository → DataStore → recomposition est câblé (imports : hiltViewModel depuis androidx.hilt.navigation.compose, collectAsStateWithLifecycle depuis lifecycle-runtime-compose).
  </action>
  <verify>
    <automated>cd "D:/projets/android/locut" && ./gradlew :app:assembleDebug</automated>
    <fails_when>non-zero exit, or "BUILD FAILED" in output</fails_when>
    <automated>cd "D:/projets/android/locut" && grep -rnE 'Alignment\.(Left|Right)|TextAlign\.(Left|Right)' app/src/main/java/com/shortifylocal/ai/ | wc -l</automated>
    <fails_when>the count is greater than 0</fails_when>
    <automated>cd "D:/projets/android/locut" && grep -rnE '(text|label|contentDescription)[[:space:]]*=[[:space:]]*"' app/src/main/java/com/shortifylocal/ai/presentation/ | wc -l</automated>
    <fails_when>the count is greater than 0 (a literal UI string bypasses string resources)</fails_when>
    <automated>cd "D:/projets/android/locut" && for k in tab_accueil tab_historique tab_parametres theme_toggle_cd; do grep -q "name=\"$k\"" app/src/main/res/values/strings.xml || echo "MISSING $k"; done</automated>
    <fails_when>any "MISSING" line is printed (a tab label resource is absent)</fails_when>
  </verify>
  <acceptance_criteria>
    - assembleDebug vert : la coquille complète (thème + nav + 3 écrans + ViewModel) compile
    - Les 3 routes accueil/historique/parametres existent avec navigation launchSingleTop/restoreState/saveState
    - La bascule : SettingsScreen → vm.toggleTheme(systemDark) → DataStore → recomposition — chemin complet câblé, aucune étape en attente
    - Grep alignements Left/Right = 0 ; grep littéraux UI = 0 ; supportsRtl=true déjà dans le manifest (plan 01-01)
    - Les 3 libellés d'onglets + theme_toggle_cd + settings_appearance passent par strings.xml
  </acceptance_criteria>
  <done>Walking skeleton UI complet : l'APK lancé (prouvé au plan 01-04) affiche 3 onglets thémés Soft-Clean et la bascule persiste réellement (SPEC R2/R3 — vérification cibles au 01-04).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| UI → ViewModel → DataStore | Seule entrée utilisateur P1 : le tap de bascule de thème (aucun texte libre) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-07 | Input validation | Bascule thème (seule interaction P1) | low | accept | Entrée binaire sans donnée libre ; l'URL YouTube / regex d'entrée = Phase 4 (util/YouTubeRegex) |
| T-01-08 | Tampering | Écarts RTL/alignements cassant la structure | low | mitigate | Grep d'acceptation Start/End + supportsRtl posé au manifest ; icônes directionnelles AutoMirrored (History) |
| T-01-09 | Repudiation (visuel) | Dérive neumorphisme (ombre double) | medium | mitigate | softShadow à couleur token unique (5 %/30 %) ; revue visuelle humaine fin de phase contre docs/02 §0/§1.2 (plan 01-04) |
</threat_model>

<verification>
- assembleDebug vert ; greps prohibitions R2 (littéraux, Left/Right) = 0
- Tokens hex normatifs présents dans Color.kt ; rayons 32/24/16/28 dans Shape.kt
- Chemin de bascule complet sans maillon manquant (MainActivity → vm → DataStore → flow → recomposition)
</verification>

<success_criteria>
SPEC R2 et R3 implémentées : coquille 3 onglets thémée Soft-Clean (tokens §1.1–1.3), structure RTL-ready, thème D-05 avec bascule persistée — prête pour l'installation sur cibles (01-04).
</success_criteria>

<output>
Create `.planning/phases/01-chafaudage-d-cisions-bloquantes/01-03-SUMMARY.md` when done
</output>
