---
phase: 01-chafaudage-d-cisions-bloquantes
plan: 04
type: execute
wave: 4
depends_on: ["01-03"]
files_modified:
  - scripts/verify_p1.sh
  - scripts/verify_emulator_p1.sh
  - .planning/phases/01-chafaudage-d-cisions-bloquantes/screens/emulator-shell.png
  - .planning/phases/01-chafaudage-d-cisions-bloquantes/screens/emulator-after-toggle.png
  - .planning/phases/01-chafaudage-d-cisions-bloquantes/screens/device-shell.png # conditionnel — créé seulement si un appareil physique est attaché (Task 3)
autonomous: true
requirements: [PROJ-01]
user_setup:
  - service: physical_device
    why: "R7 / D-03 : la double cible exige un téléphone physique ARM64 Android 13+ en adb USB pour la vérification fin de phase (émulateur x86_64 déjà disponible, D-01)"
    env_vars: []
    dashboard_config:
      - task: "Connecter le téléphone ARM64 Android 13+ en USB avec le débogage USB activé (visible via adb devices)"
        location: "Sur le téléphone : Options développeur → Débogage USB"
estimate:
  tokens: 35000
  raw_tokens: 35000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "Un clone frais (JDK 17 + Android SDK, sans Gradle global) buildé avec le wrapper produit app-debug.apk installable ; local.properties reste gitigné"
    - "Toutes les versions de gradle/libs.versions.toml correspondent 1:1 à l'errata E2 du 2026-09-05"
    - "Structure RTL : alignements Start/End uniquement dans le code UI, supportsRtl=true dans le manifest"
    - "AppDatabase v1 sans entités compile, exportSchema=true, schéma JSON app/schemas/.../1.json commité"
    - "Le SHA-256 consigné du fork 8.1.7 correspond à l'AAR résolu, téléchargé exclusivement depuis Maven Central"
    - "L'APK s'installe sur émulateur x86_64 ET sur appareil physique arm64 sans jamais charger les .so du fork"
    - "Aucun fichier de build ne référence l'ancienne coordonnée du vendeur ffmpeg retiré"
    - "Force-stop puis relance à froid : le thème restauré est le dernier choisi, sans crash (émulateur ET physique)"
  prohibitions:
    - statement: "Aucun fichier de build ne référence l'artefact ffmpeg-kit du vendeur historique retiré (com.arthenica:ffmpeg-kit-full-gpl)"
      status: resolved
      verification: "grep -rn 'com\\.arthenica' settings.gradle.kts build.gradle.kts app/build.gradle.kts gradle/ → 0 résultat"
    - statement: "Aucune chaîne UI codée en dur dans la coquille — les libellés d'onglets passent par les ressources"
      status: resolved
      verification: "grep -rnE '(text|label|contentDescription)[[:space:]]*=[[:space:]]*\"' app/src/main/java/com/shortifylocal/ai/presentation/ → 0 résultat"
    - statement: "Aucun alignement Left/Right — Start/End uniquement (structure RTL dès le premier composant)"
      status: resolved
      verification: "grep -rnE 'Alignment\\.(Left|Right)|TextAlign\\.(Left|Right)' app/src/main/java/com/shortifylocal/ai/ → 0 résultat"
    - statement: "Aucune permission stockage/localisation/contacts déclarée dans le manifest P1"
      status: resolved
      verification: "grep -nE 'READ_MEDIA|READ_EXTERNAL_STORAGE|ACCESS_.*_LOCATION|READ_CONTACTS' app/src/main/AndroidManifest.xml → 0 résultat"
    - statement: "Aucun neumorphisme (ombre double clair+sombre, élément de la couleur du fond) dans la coquille"
      status: resolved
      verification: "judgment — revue visuelle du thème contre docs/02-UI-UX.md §0/§1.2 (human-check de ce plan)"
    - statement: "Ni kapt ni KSP1 — KSP2 (com.google.devtools.ksp 2.3.11) uniquement"
      status: resolved
      verification: "grep -rni 'kapt' settings.gradle.kts build.gradle.kts app/build.gradle.kts gradle/ → 0 résultat"
    - statement: "Aucun fallbackToDestructiveMigration() sur la base Room initialisée en P1"
      status: resolved
      verification: "grep -rn 'fallbackToDestructiveMigration' app/ → 0 résultat"
    - statement: "Aucun android:usesCleartextTraffic=\"true\" dans le manifest (HTTPS-only)"
      status: resolved
      verification: "grep -n 'usesCleartextTraffic' app/src/main/AndroidManifest.xml → 0 résultat"
---

<objective>
Vérification de bout en bout de la Phase 1 : batterie mécanique scriptée (tous les interdits + checksum + pureté domain + versions E2), puis installation/lancement/navigation/persistance sur émulateur Android 13+ (scriptée, avec captures d'écran), et la vérification humaine fin de phase pour la seconde cible (téléphone physique ARM64 en adb USB, D-03) et la revue visuelle anti-neumorphisme.

Purpose: SPEC R7 + critères d'acceptation 1–12 — c'est le plan qui scelle la phase : chaque interdit devient un check rejouable, la double cible est sondée (le blocage ABI hérité de STATE.md est clos), et les human-checks fin de phase sont consolidés pour l'UAT.
Output: `scripts/verify_p1.sh` (batterie statique), `scripts/verify_emulator_p1.sh` (cible émulateur, réutilisable sur l'appareil via ANDROID_SERIAL), captures d'écran, rapport d'exécution des human-checks.
</objective>

<execution_context>
@D:/projets/android/locut/.zcode/gsd-core/workflows/execute-plan.md
@D:/projets/android/locut/.zcode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-chafaudage-d-cisions-bloquantes/01-SPEC.md
@.planning/phases/01-chafaudage-d-cisions-bloquantes/01-RESEARCH.md
@docs/02-UI-UX.md
@.planning/phases/01-chafaudage-d-cisions-bloquantes/01-CONTEXT.md
</context>

## Artifacts this phase produces

(Vue d'ensemble phase complète dans 01-01-PLAN.md — ce plan livre l'outillage de vérification.)
- **Scripts** : `scripts/verify_p1.sh`, `scripts/verify_emulator_p1.sh`
- **Preuves** : `.planning/phases/01-chafaudage-d-cisions-bloquantes/screens/emulator-shell.png`, `screens/emulator-after-toggle.png`

<tasks>

<task type="auto">
  <name>Task 1: Batterie mécanique scripts/verify_p1.sh (tous les interdits + socle)</name>
  <files>scripts/verify_p1.sh</files>
  <read_first>
    - .planning/phases/01-chafaudage-d-cisions-bloquantes/01-SPEC.md (## Acceptance Criteria et ## Prohibitions — la liste des checks fait foi)
    - .planning/phases/01-chafaudage-d-cisions-bloquantes/01-RESEARCH.md (## Validation Architecture — Phase Requirements → Test Map, lignes R1–R6)
    - scripts/ (état courant du répertoire, s'il existe)
  </read_first>
  <action>
Créer `scripts/verify_p1.sh` : script bash Git Bash-compatible, `set -euo pipefail`, racine déduite de l'emplacement du script, fonction d'aide qui imprime « ÉCHEC: <check> » et sort en non-zéro au premier échec, et un récapitulatif « P1 OK » en fin. Il DOIT exécuter, dans l'ordre : (1) interdits fichiers de build — grep de l'ancienne coordonnée ffmpeg du vendeur historique, grep insensible à la casse du processeur d'annotation legacy interdit, grep de la migration destructive interdite, chacun sur settings.gradle.kts + build.gradle.kts + app/build.gradle.kts + gradle/ en exigeant zéro occurrence (commentaires inclus) ; (2) interdits code UI — grep des alignements Left/Right et TextAlign Left/Right sur app/src/main/java/, grep des littéraux UI directs (affectations text/label/contentDescription avec une chaîne entre guillemets) sur le package presentation, chacun zéro occurrence ; (3) pureté domain — grep des imports android./androidx. dans domain/ à zéro ; (4) manifest — zéro READ_MEDIA*, zéro READ_EXTERNAL_STORAGE, zéro ACCESS_*_LOCATION, zéro READ_CONTACTS, zéro attribut cleartext, présence de supportsRtl="true" et allowBackup="false" ; (5) catalogue — présence (grep -q) de chacune des 21 versions E2 dans gradle/libs.versions.toml : 2.4.10, 9.4.0, 9.7.1, 2026.08.00, 2.60.1, 1.4.0, 2.8.4, 2.3.11, 2.10.0, 2.11.2, 1.1.0, 25.4.0, 4.0.0, 16.1.7, 1.11.0, 5.5.0, 3.6.2, 1.2.1, v0.26.5, 8.1.7, b4938, et de la chaîne SHA-256 du fork ; (6) Room — test -f du schéma `app/schemas/com.shortifylocal.ai.data.local.room.AppDatabase/1.json` ; (7) checksum — sha256sum de l'AAR du fork trouvé par find dans `~/.gradle/caches/modules-2/files-2.1/dev.ffmpegkit-maintained/` comparé à 374d3734755fd4a4f2241da23b93ab0b3bcae9bd39f01cf3bf29a5731914658b. Ne pas utiliser de replis silencieux qui masquent un fichier absent : chaque check doit pouvoir échouer (pas de `|| echo 0` alimentant une comparaison).
  </action>
  <verify>
    <automated>cd "D:/projets/android/locut" && bash scripts/verify_p1.sh</automated>
    <fails_when>non-zero exit, or a line starting with "ÉCHEC:" in output</fails_when>
    <automated>cd "D:/projets/android/locut" && bash -n scripts/verify_p1.sh</automated>
    <fails_when>non-zero exit (syntax error in the script)</fails_when>
  </verify>
  <acceptance_criteria>
    - `bash scripts/verify_p1.sh` se termine en imprimant le récapitulatif P1 OK (exit 0)
    - Chacune des 7 sections de checks est présente dans le script (interdits build, UI, domain, manifest, catalogue E2, schéma Room, checksum)
    - Le script échoue bien si on introduit volontairement une violation (test inverse fait une fois à la main, puis annulé)
  </acceptance_criteria>
  <done>Les 8 interdits de la SPEC et les checks mécaniques R1/R4/R5/R6 sont rejouables en une commande (Wave 0 gap du research comblé).</done>
</task>

<task type="auto">
  <name>Task 2: Cible émulateur — install, lancement, navigation 3 onglets, persistance thème</name>
  <precondition>Un AVD est listable via `emulator -list-avds` (D-01 : Medium_Phone_2 ou Pixel_9a déjà présents sur cette machine) et app/build/outputs/apk/debug/app-debug.apk existe (build du plan 01-01)</precondition>
  <files>scripts/verify_emulator_p1.sh, .planning/phases/01-chafaudage-d-cisions-bloquantes/screens/emulator-shell.png, .planning/phases/01-chafaudage-d-cisions-bloquantes/screens/emulator-after-toggle.png</files>
  <read_first>
    - .planning/phases/01-chafaudage-d-cisions-bloquantes/01-RESEARCH.md (Environment Availability — AVDs disponibles ; Validation Architecture R3/R7 ; adb vocabulary)
    - .planning/phases/01-chafaudage-d-cisions-bloquantes/01-CONTEXT.md (D-03/D-04 — émulateur première cible rapide)
    - scripts/verify_p1.sh (conventions du script précédent : set -euo pipefail, messages ÉCHEC)
  </read_first>
  <action>
Créer `scripts/verify_emulator_p1.sh` : script bash paramétrable par `${ANDROID_SERIAL:-}` (toutes les commandes adb passent par `adb ${ANDROID_SERIAL:+-s "$ANDROID_SERIAL"}` pour être réutilisable sur le téléphone physique) — séquence : (1) si aucun appareil n'est déjà en ligne (`adb devices` sans entrée « device ») et ANDROID_SERIAL est vide, démarrer le premier AVD de `emulator -list-avds` en arrière-plan puis attendre `adb wait-for-device` et `sys.boot_completed = 1` (boucle bornée ~180 s) ; (2) `adb install -r app/build/outputs/apk/debug/app-debug.apk` — l'install x86_64 native sonde l'ABI du fork (les .so embarqués ne sont jamais chargés) ; (3) `adb shell am start -n com.shortifylocal.ai/.MainActivity`, attendre 3 s, vérifier que le process tourne (`adb shell pidof com.shortifylocal.ai` non vide) ; (4) `adb exec-out screencap -p > .planning/phases/01-chafaudage-d-cisions-bloquantes/screens/emulator-shell.png` ; (5) `adb shell uiautomator dump` puis `adb pull` vers un fichier temporaire, et grep des trois libellés Accueil / Historique / Paramètres dans le XML — preuve mécanique des 3 onglets affichés ; (6) bascule : extraire du même dump les bounds du nœud portant `content-desc="Basculer le thème clair/sombre"`, calculer le centre (sed/awk) et envoyer `adb shell input tap X Y` ; attendre 1 s, screencap `emulator-after-toggle.png` ; (7) persistance : `adb shell am force-stop com.shortifylocal.ai`, relancer via am start, attendre 3 s, vérifier pidof non vide ET `adb shell run-as com.shortifylocal.ai cat files/datastore/settings.preferences_pb` contenant le nom du mode choisi (grep -a LIGHT ou DARK) ; (8) imprimer « ÉMU OK ». Si l'extraction des bounds échoue sur une version d'uiautomator, le script imprime le dump pullé et sort en non-zéro avec « ÉCHEC: bascule introuvable » — l'exécuteur retente alors à la main (input tap aux coordonnées lues dans le dump) et consigne le résultat.

L'exécuteur lit ensuite les deux captures avec l'outil de lecture d'images pour confirmer visuellement : coquille 3 onglets visible, thème effectivement changé après le tap, thème restauré après relance.
  </action>
  <verify>
    <automated>cd "D:/projets/android/locut" && bash scripts/verify_emulator_p1.sh</automated>
    <fails_when>non-zero exit, or a line starting with "ÉCHEC:" in output</fails_when>
    <automated>cd "D:/projets/android/locut" && test -s .planning/phases/01-chafaudage-d-cisions-bloquantes/screens/emulator-shell.png && test -s .planning/phases/01-chafaudage-d-cisions-bloquantes/screens/emulator-after-toggle.png</automated>
    <fails_when>either screenshot file is absent or empty (non-zero test)</fails_when>
  </verify>
  <acceptance_criteria>
    - `adb install -r` réussit sur l'émulateur x86_64 (ABI du fork sondée — l'install est la preuve), `am start` lance MainActivity sans crash (pidof non vide)
    - Le dump uiautomator contient les 3 libellés Accueil / Historique / Paramètres (navigation affichée)
    - Après tap de bascule + force-stop + relance : pidof non vide ET le DataStore `settings.preferences_pb` contient le mode choisi (LIGHT ou DARK) — persistance prouvée mécaniquement
    - Les 2 captures PNG existent, sont non vides, et confirment visuellement le changement/restauration de thème
    - Relance à froid propre (critère SPEC 5 côté émulateur)
  </acceptance_criteria>
  <done>Double cible côté émulateur : install + lancement + navigation + persistance scriptés et rejouables (SPEC R7, D-04) ; captures d'écran d'appui pour l'UAT.</done>
</task>

<task type="auto">
  <name>Task 3: Vérification humaine fin de phase — appareil physique ARM64 + revue visuelle Soft-Clean</name>
  <precondition>Pour la lég appareil : un téléphone ARM64 Android 13+ est connecté en USB avec le débogage USB activé (user_setup — visible via `adb devices`) ; à défaut, la lég est reportée telle quelle dans l'UAT</precondition>
  <files>.planning/phases/01-chafaudage-d-cisions-bloquantes/screens/device-shell.png</files>
  <read_first>
    - .planning/phases/01-chafaudage-d-cisions-bloquantes/01-SPEC.md (exigence 7 + acceptance 4/5 — la double cible fait foi)
    - .planning/phases/01-chafaudage-d-cisions-bloquantes/01-CONTEXT.md (D-03 — téléphone ARM64 Android 13+ en adb USB, vérifications scriptées)
    - docs/02-UI-UX.md §0 et §1.2 (grille de revue visuelle anti-neumorphisme)
    - scripts/verify_emulator_p1.sh (réutilisé tel quel sur l'appareil via ANDROID_SERIAL)
  </read_first>
  <action>
Tenter d'abord d'AUTOMATISER la lég appareil : si `adb devices` liste un appareil physique (serial non émulateur), exécuter `ANDROID_SERIAL=<serial> bash scripts/verify_emulator_p1.sh` (le script est déjà paramétré), puis copier les captures obtenues vers `screens/device-shell.png` et consigner le résultat. Si aucun appareil n'est attaché, NE PAS bloquer : consigner la lég comme « à exécuter par l'utilisateur » avec la commande exacte.

Préparer ensuite les éléments de vérification humaine fin de phase (workflow end-of-phase) : (1) lég appareil — installer (`adb install -r`), lancer, naviguer entre les 3 onglets, basculer le thème, force-stop, relancer : thème restauré = dernier choisi, relance à froid propre ; (2) revue visuelle du thème contre docs/02 §0/§1.2 : UNE seule ombre douce verticale par surface (5 % clair / 30 % sombre), aucun neumorphisme (pas d'ombre double clair+sombre, aucun élément de la couleur du fond en relief), rayons 32/24/16/28 — à juger sur les captures émulateur + appareil ; (3) relire la coquille à l'œil pour tout écart visuel vs tokens docs/02 §1.1. Consigner les résultats dans le SUMMARY du plan (l'humain valide via le flux UAT fin de phase, qui moissonne les human-checks).
  </action>
  <verify>
    <human-check>
      Lég appareil (D-03) : sur le téléphone ARM64 Android 13+ connecté en USB — `adb install -r app/build/outputs/apk/debug/app-debug.apk` puis `adb shell am start -n com.shortifylocal.ai/.MainActivity` réussissent ; les 3 onglets Accueil/Historique/Paramètres naviguent ; bascule de thème → force-stop → relance : le thème restauré est le dernier choisi ; la relance à froid redémarre proprement. (Si le téléphone n'était pas attaché à l'exécution : exécuter la même séquence à la première connexion — la commande scriptée `ANDROID_SERIAL=<serial> bash scripts/verify_emulator_p1.sh` est fournie.)
      Revue visuelle (interdit neumorphisme) : les captures émulateur + appareil montrent une seule ombre douce verticale par surface, aucun relief néumorphique, et les couleurs/rayons conformes aux tokens docs/02 §1.1–1.3 en clair ET en sombre.
    </human-check>
    <automated>cd "D:/projets/android/locut" && bash scripts/verify_p1.sh</automated>
    <fails_when>non-zero exit, or a line starting with "ÉCHEC:" in output</fails_when>
  </verify>
  <acceptance_criteria>
    - Si appareil attaché : la batterie scriptée passe avec ANDROID_SERIAL sur le téléphone (install + lancement + 3 onglets + persistance) et screens/device-shell.png existe
    - Sinon : la lég appareil est consignée « en attente utilisateur » avec la commande exacte, et le reste de la phase est scellé sur l'émulateur
    - Les human-checks ci-dessus sont consignés pour la consolidation UAT fin de phase
    - La batterie statique repasse verte après tout changement
  </acceptance_criteria>
  <done>La double cible R7 est soit prouvée (appareil attaché), soit automatisée à une commande près pour l'utilisateur ; la revue visuelle anti-neumorphisme est outillée avec les captures d'appui.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| adb USB → appareil | L'APK debug s'installe sur un appareil physique personnel |
| Scripts de vérification | Exécutent des commandes adb/gradle sur l'environnement de dev |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-10 | Elevation of privilege | Surface debug sur appareil physique (debuggable, run-as) | low | accept | APK debug à usage local uniquement ; le durcissement release (R8, non-debuggable, allowBackup) est SEC-04 / Phase 7 |
| T-01-11 | Denial of service (flakiness) | Démarrage émulateur / taps uiautomator | low | mitigate | Boucles d'attente bornées + messages ÉCHEC explicites + réutilisation du script sur l'appareil via ANDROID_SERIAL ; l'exécuteur retape à la main sur échec de bounds |
| T-01-SC | Tampering | Vérification finale du checksum fork avant scellement | high | mitigate | Rejouée par verify_p1.sh (section 7) — même valeur épinglée que T-01-SC du plan 01-01 |
</threat_model>

<verification>
- `bash scripts/verify_p1.sh` vert (tous interdits + checksum + E2 + schéma)
- `bash scripts/verify_emulator_p1.sh` vert sur l'émulateur (install, lancement, 3 onglets, persistance, captures)
- Lég appareil : scriptée via ANDROID_SERIAL si attaché, sinon commandes exactes consignées pour l'utilisateur
- Les 12 critères d'acceptation de la SPEC sont couverts : 1–3 et 6–12 par la batterie + build, 4–5 par les cibles (émulateur scripté + appareil human-check)
</verification>

<success_criteria>
Phase 1 scellée : socle build épinglé et reproductible, coquille 3 onglets Soft-Clean installée et lancée sur émulateur (et appareil si attaché), thème persistant au force-stop, 8 interdits prouvés absents par checks rejouables, human-checks consolidés pour l'UAT fin de phase.
</success_criteria>

<output>
Create `.planning/phases/01-chafaudage-d-cisions-bloquantes/01-04-SUMMARY.md` when done
</output>
