# API Coverage — Phase 1 : Échafaudage & décisions bloquantes

No external API integration: socle build + coquille UI + persistance DataStore locale — aucun service distant ni clé d'API ; « SDK » = outillage build Android (compileSdk/NDK).

> Déclaration au sens #2365 (overrule humain du détecteur fail-closed). Les phases d'intégration réelles démarreront leur propre matrice : NewPipeExtractor + ffmpeg-kit (Phase 2+), Gemini BYOK (Phase 4, optionnel), AdMob/UMP (Phase 7) — pleine couverture par défaut à ce moment-là.
