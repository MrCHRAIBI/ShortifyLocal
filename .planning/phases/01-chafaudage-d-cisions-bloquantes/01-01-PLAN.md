---
phase: 01-chafaudage-d-cisions-bloquantes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .gitattributes
  - .gitignore
  - gradle.properties
  - settings.gradle.kts
  - build.gradle.kts
  - gradle/libs.versions.toml
  - gradlew
  - gradlew.bat
  - gradle/wrapper/gradle-wrapper.jar
  - gradle/wrapper/gradle-wrapper.properties
  - app/build.gradle.kts
  - app/proguard-rules.pro
  - app/src/main/AndroidManifest.xml
  - app/src/main/java/com/shortifylocal/ai/ShortifyLocalApp.kt
  - app/src/main/java/com/shortifylocal/ai/MainActivity.kt
  - app/src/main/res/values/strings.xml
  - app/src/main/res/values/themes.xml
  - app/src/main/res/values-night/themes.xml
  - app/src/main/res/values/ic_launcher_background.xml
  - app/src/main/res/mipmap-anydpi-v26/ic_launcher.xml
  - app/src/main/res/mipmap-anydpi-v26/ic_launcher_round.xml
  - app/src/main/res/drawable/ic_launcher_foreground.xml
autonomous: true
requirements: [PROJ-01]
estimate:
  tokens: 55000
  raw_tokens: 55000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "Un clone frais (JDK 17 + Android SDK, sans Gradle global) buildé avec le wrapper produit app-debug.apk installable ; local.properties reste gitigné"
    - "Toutes les versions de gradle/libs.versions.toml correspondent 1:1 à l'errata E2 du 2026-09-05"
  prohibitions:
    - statement: "Aucun fichier de build ne référence l'artefact ffmpeg-kit du vendeur historique retiré (com.arthenica:ffmpeg-kit-full-gpl)"
      status: resolved
      verification: "grep -rn 'com\\.arthenica' settings.gradle.kts build.gradle.kts app/build.gradle.kts gradle/ → 0 résultat"
    - statement: "Aucune permission stockage/localisation/contacts déclarée dans le manifest P1"
      status: resolved
      verification: "grep -nE 'READ_MEDIA|READ_EXTERNAL_STORAGE|ACCESS_.*_LOCATION|READ_CONTACTS' app/src/main/AndroidManifest.xml → 0 résultat"
    - statement: "Ni kapt ni KSP1 — KSP2 (com.google.devtools.ksp 2.3.11) uniquement"
      status: resolved
      verification: "grep -rni 'kapt' settings.gradle.kts build.gradle.kts app/build.gradle.kts gradle/ → 0 résultat"
    - statement: "Aucun android:usesCleartextTraffic=\"true\" dans le manifest (HTTPS-only)"
      status: resolved
      verification: "grep -n 'usesCleartextTraffic' app/src/main/AndroidManifest.xml → 0 résultat"
---

<objective>
Socle build reproductible : le dépôt passe de « zéro fichier build » à un projet Gradle single-module `app/` (package `com.shortifylocal.ai`) dont `./gradlew :app:assembleDebug` produit un APK debug, depuis un catalogue de versions unique conforme à l'errata E2 et le fork ffmpeg-kit maintenu épinglé avec checksum vérifié.

Purpose: PROJ-01 — toute la suite du projet build depuis ce socle épinglé ; le artefact Maven mort est banni et son remplaçant prouvé résoluble dès la première phase (clause de fraîcheur appliquée et notée).
Output: wrapper Gradle 9.7.1 commité, catalogue `gradle/libs.versions.toml` complet, module `app/` minimal installable, classe Application Hilt + manifest sans permissions, checksum du fork vérifié contre l'AAR réellement résolu.
</objective>

**Justification du volume de fichiers (attendu — ne pas scinder) :** les 22 fichiers de `files_modified` ne représentent pas 22 conceptions nouvelles. 4 sont générés mécaniquement par la commande `gradle wrapper` (`gradlew`, `gradlew.bat`, `gradle-wrapper.jar`, `gradle-wrapper.properties` — jamais écrits à la main), 16 sont du boilerplate de template au contenu prescrit verbatim dans les actions (hygiène repo, Patterns 1-5 de 01-RESEARCH.md, manifest minimal, thèmes/icônes/strings façon Android Studio), et seuls `ShortifyLocalApp.kt` et `MainActivity.kt` sont du Kotlin écrit main — volontairement réduit et prescrit. Le filet de vérification est entièrement mécanique (`./gradlew :app:assembleDebug` vert + batterie de grep). Le coût estimé reste dans le budget (0,55 de la fenêtre) ; une scission ajouterait du contexte sans réduire le risque — traiter ce volume comme la normale.

<execution_context>
@D:/projets/android/locut/.zcode/gsd-core/workflows/execute-plan.md
@D:/projets/android/locut/.zcode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-chafaudage-d-cisions-bloquantes/01-SPEC.md
@.planning/phases/01-chafaudage-d-cisions-bloquantes/01-CONTEXT.md
@docs/ERRATA-2026-09-05.md
@.planning/phases/01-chafaudage-d-cisions-bloquantes/01-RESEARCH.md
</context>

## Artifacts this phase produces

Toute la phase 1 crée (vue d'ensemble — ce plan livre la partie build) :
- **Gradle** : `settings.gradle.kts`, `build.gradle.kts` (racine), `gradle.properties`, `gradle/libs.versions.toml`, wrapper 9.7.1 (`gradlew`, `gradlew.bat`, `gradle/wrapper/gradle-wrapper.{jar,properties}`), `app/build.gradle.kts`, `app/proguard-rules.pro`
- **Hygiène repo** : `.gitignore` (Android), `.gitattributes` (gradlew LF)
- **Packages** (créés sur les plans 01-02/01-03) : `com.shortifylocal.ai.{presentation.{theme,navigation,ui.home,ui.history,ui.settings}, domain.{model,repository,usecase}, data.{repository,local.room,local.preferences}, worker, di, util}`
- **Classes** : `ShortifyLocalApp`, `MainActivity` (ce plan) ; `AppDatabase`, `DatabaseModule`, `ThemeMode`, `ThemeRepository`, `ThemeViewModel`, `ThemeModeTest` (01-02) ; `SoftCleanTokens`, `SoftCleanShapes`, `softShadow`, `ShortifyLocalTheme`, `AppShell`, `HomeScreen`, `HistoryScreen`, `SettingsScreen` (01-03)
- **Ressources** : `strings.xml` (app_name, onglets, bascule), `themes.xml` + variante nuit, icône launcher adaptative, `app/schemas/.../1.json` (01-02), `scripts/verify_p1.sh`, `scripts/verify_emulator_p1.sh`, captures `.planning/phases/01-chafaudage-d-cisions-bloquantes/screens/` (01-04)

<tasks>

<task type="tracer">
  <name>Task 1: Système de build de bout en bout — wrapper 9.7.1, catalogue E2, résolution des plugins</name>
  <files>.gitattributes, .gitignore, gradle.properties, settings.gradle.kts, build.gradle.kts, gradle/libs.versions.toml, gradlew, gradlew.bat, gradle/wrapper/gradle-wrapper.jar, gradle/wrapper/gradle-wrapper.properties</files>
  <read_first>
    - .planning/phases/01-chafaudage-d-cisions-bloquantes/01-SPEC.md (exigence 1 + acceptance)
    - .planning/phases/01-chafaudage-d-cisions-bloquantes/01-RESEARCH.md (Patterns 1, 2, 4 — formes exactes des fichiers ; Pitfalls 1, 3, 4, 8)
    - docs/ERRATA-2026-09-05.md (E2 — versions fait foi)
    - .planning/phases/01-chafaudage-d-cisions-bloquantes/01-CONTEXT.md (D-01, D-02 — aucun install manuel, foojay)
  </read_first>
  <action>
Générer le wrapper SANS Gradle global : télécharger https://services.gradle.org/distributions/gradle-9.7.1-bin.zip dans un répertoire temporaire HORS du dépôt, l'extraire (unzip, ou `tar -xf` via bsdtar), puis depuis la racine du dépôt exécuter le `bin/gradle` de la distribution avec `wrapper --gradle-version 9.7.1` — cela génère `gradlew`, `gradlew.bat`, `gradle/wrapper/gradle-wrapper.jar` et `gradle-wrapper.properties`. Supprimer le temporaire.

Créer `.gitattributes` avec `gradlew text eol=lf` et `*.bat text eol=crlf` (pitfall CRLF — pitfall 3) et normaliser `gradlew` en LF. Créer `.gitignore` Android type template : `.gradle/`, `build/`, `app/build/`, `local.properties`, `.idea/`, `captures/`, `*.iml`, `.kotlin/`.

Créer `gradle.properties` (Pattern 2, les deux flags AGP 9 ENSEMBLE) : `android.builtInKotlin=false`, `android.newDsl=false`, `org.gradle.jvmargs=-Xmx4096m -Dfile.encoding=UTF-8`, `android.useAndroidX=true`, `android.nonTransitiveRClass=true`, `org.gradle.java.installations.auto-download=true`, `kotlin.code.style=official`. Ne PAS committer `org.gradle.java.home` (pitfall 8).

Créer `settings.gradle.kts` (Pattern 1) : bloc `plugins` en PREMIER avec `id("org.gradle.toolchains.foojay-resolver-convention") version "1.0.0"` + commentaire TOML/inline documentant l'unique exception « zéro version inline » (limitation Gradle : le catalogue n'est pas visible du bloc plugins du settings) ; `pluginManagement` avec google() filtré par includeGroupByRegex (com.android.*, com.google.*, androidx.*), mavenCentral(), gradlePluginPortal() ; `dependencyResolutionManagement` avec `repositoriesMode = RepositoriesMode.FAIL_ON_PROJECT_REPOS` et repositories google() + mavenCentral() UNIQUEMENT (pas de JitPack en P1 — différé Phase 4) ; `rootProject.name = "ShortifyLocal AI"` ; `include(":app")`.

Créer `gradle/libs.versions.toml` = Pattern 4 de 01-RESEARCH.md, TOUTES les sections, 1:1 avec l'errata E2 : versions kotlin 2.4.10, agp 9.4.0, gradleWrapper 9.7.1, foojayResolver 1.0.0, ksp 2.3.11, composeBom 2026.08.00, activityCompose 1.13.0, lifecycle 2.11.0, coreKtx 1.19.0, hilt 2.60.1, androidxHilt 1.4.0, room 2.8.4, navigationCompose 2.10.0, workManager 2.11.2, securityCrypto 1.1.0, playServicesAds 25.4.0, ump 4.0.0, mlkitFaceDetection 16.1.7, newPipeExtractor v0.26.5, ffmpegKit 8.1.7, whisperCpp b4938 (commentaire), media3 1.11.0, okhttp 5.5.0, coil 3.6.2, datastore 1.2.1, coroutines 1.11.0, serializationJson 1.11.0, junit 4.13.2 ; toutes les entrées [libraries] et les 7 entrées [plugins] du Pattern 4, PLUS l'entrée `androidx-hilt-navigation-compose = { module = "androidx.hilt:hilt-navigation-compose", version.ref = "androidxHilt" }`. Le commentaire du bloc ffmpeg-kit référence UNIQUEMENT la coordonnée vivante `dev.ffmpegkit-maintained:ffmpeg-kit-full-gpl` + SHA-256 `374d3734755fd4a4f2241da23b93ab0b3bcae9bd39f01cf3bf29a5731914658b` + note de vérification registre (2026-09-05, clause de fraîcheur appliquée et notée) — ne jamais mentionner l'ancienne coordonnée du vendeur historique dans ce commentaire (pitfall 7 : le grep d'acceptation scanne aussi les commentaires). Ajouter un commentaire d'en-tête « versions vérifiées contre registres officiels le 2026-09-05 ».

Créer `build.gradle.kts` racine (Pattern 3) : uniquement les 7 alias en `apply false` (android-application, kotlin-android, kotlin-compose, kotlin-serialization, ksp, hilt, room).
  </action>
  <verify>
    <automated>cd "D:/projets/android/locut" && ./gradlew --version</automated>
    <fails_when>non-zero exit, or the "Gradle" line does not report 9.7.1</fails_when>
    <automated>cd "D:/projets/android/locut" && ./gradlew help</automated>
    <fails_when>non-zero exit, or "BUILD FAILED" in output</fails_when>
    <automated>cd "D:/projets/android/locut" && ! grep -q $'\r' gradlew && test -f gradle/wrapper/gradle-wrapper.jar && test -f gradle/wrapper/gradle-wrapper.properties</automated>
    <fails_when>non-zero exit — i.e. gradlew contains CR characters (CRLF corruption, pitfall 3) or gradle/wrapper/gradle-wrapper.jar or gradle/wrapper/gradle-wrapper.properties is absent</fails_when>
  </verify>
  <acceptance_criteria>
    - `./gradlew --version` rapporte Gradle 9.7.1 sans Gradle global installé
    - `./gradlew help` configure le build racine : les 7 alias du catalogue se résolvent (AGP 9.4.0, Kotlin 2.4.10, KSP 2.3.11, Hilt 2.60.1, Room 2.8.4) — toute erreur « extension already registered » ou « plugin is no longer required » = échec pitfall 1
    - `gradle.properties` contient les deux flags `android.builtInKotlin=false` et `android.newDsl=false`
    - `settings.gradle.kts` ne déclare que google() + mavenCentral() en résolution de dépendances, avec FAIL_ON_PROJECT_REPOS
    - `gradle/libs.versions.toml` contient chacune des versions E2 (grep positif : 2.4.10, 9.4.0, 9.7.1, 2026.08.00, 2.60.1, 1.4.0, 2.8.4, 2.3.11, 2.10.0, 2.11.2, 1.1.0, 25.4.0, 4.0.0, 16.1.7, 1.11.0, 5.5.0, 3.6.2, 1.2.1, v0.26.5, 8.1.7, b4938) et la chaîne 374d3734755fd4a4f2241da23b93ab0b3bcae9bd39f01cf3bf29a5731914658b
  </acceptance_criteria>
  <done>Wrapper 9.7.1 opérationnel en LF, catalogue E2 complet parsé, plugins racine résolus, hygiène repo en place (D-02 respecté : zéro installation manuelle).</done>
</task>

<task type="auto">
  <name>Task 2: Module app minimal installable — build, manifest, Application Hilt, icône</name>
  <files>app/build.gradle.kts, app/proguard-rules.pro, app/src/main/AndroidManifest.xml, app/src/main/java/com/shortifylocal/ai/ShortifyLocalApp.kt, app/src/main/java/com/shortifylocal/ai/MainActivity.kt, app/src/main/res/values/strings.xml, app/src/main/res/values/themes.xml, app/src/main/res/values-night/themes.xml, app/src/main/res/values/ic_launcher_background.xml, app/src/main/res/mipmap-anydpi-v26/ic_launcher.xml, app/src/main/res/mipmap-anydpi-v26/ic_launcher_round.xml, app/src/main/res/drawable/ic_launcher_foreground.xml</files>
  <read_first>
    - .planning/phases/01-chafaudage-d-cisions-bloquantes/01-RESEARCH.md (Patterns 3 et 5 — app/build.gradle.kts, manifest, Application ; Pitfalls 2, 5, 7)
    - .planning/phases/01-chafaudage-d-cisions-bloquantes/01-SPEC.md (exigences 1, 5, 6 ; contrainte « aucune permission P1 »)
    - docs/01-Description.md §2.3 (permissions des phases ultérieures — NE PAS les déclarer en P1)
    - app/build.gradle.kts et app/src/main/AndroidManifest.xml créés à la tâche 1 le cas échéant
  </read_first>
  <action>
Créer `app/build.gradle.kts` (Pattern 3) : plugins alias (android-application, kotlin-android, kotlin-compose, ksp, hilt, room) ; android { namespace = "com.shortifylocal.ai", compileSdk = 36, defaultConfig { applicationId = "com.shortifylocal.ai", minSdk = 33, targetSdk = 36, versionCode = 1, versionName = "0.1.0" }, buildTypes.release { isMinifyEnabled = false } (release/signing = Phase 7), compileOptions VERSION_17, buildFeatures { compose = true } } ; `kotlin { jvmToolchain(17) }` (D-02) ; bloc `room { schemaDirectory("$projectDir/schemas") }` ; dependencies (Pattern 3) : platform(compose-bom), compose-ui, compose-material3, compose-icons-extended, compose-foundation, core-ktx, activity-compose, lifecycle-runtime-compose, navigation-compose, hilt-android + ksp hilt-android-compiler, androidx-hilt-work + ksp androidx-hilt-compiler, androidx-hilt-navigation-compose, work-runtime-ktx, room-runtime + ksp room-compiler + room-ktx, datastore-preferences, kotlinx-coroutines-android, ffmpegkit-full-gpl (déclarée, jamais appelée en P1), testImplementation junit. Aucun processeur d'annotation legacy (KSP2 uniquement).

Créer `app/proguard-rules.pro` vide (commentaire d'en-tête). Créer `app/src/main/AndroidManifest.xml` (Pattern 5) : `<application android:name=".ShortifyLocalApp" android:label="@string/app_name" android:icon="@mipmap/ic_launcher" android:roundIcon="@mipmap/ic_launcher_round" android:supportsRtl="true" android:allowBackup="false" android:theme="@style/Theme.ShortifyLocal">` avec MainActivity `exported="true"` + intent-filter MAIN/LAUNCHER, et le provider `androidx.startup.InitializationProvider` (authorities `${applicationId}.androidx-startup`, exported false, tools:node merge) retirant le meta-data `androidx.work.WorkManagerInitializer` (init on-demand). ZÉRO `<uses-permission>`, aucun attribut de trafic en clair, aucun intent-filter deep link (Phase 7/3). PAS d'attribut `package` sur `<manifest>` (namespace géré par Gradle).

Créer `ShortifyLocalApp.kt` : `@HiltAndroidApp class ShortifyLocalApp : Application(), Configuration.Provider` avec `@Inject lateinit var workerFactory: HiltWorkerFactory` et la surcharge en PROPRIÉTÉ Kotlin `override val workManagerConfiguration: Configuration get() = Configuration.Builder().setWorkerFactory(workerFactory).build()` — PAS la forme méthode des docs périmées (pitfall 2 : WorkManager 2.11.2 ne compile qu'avec la propriété).

Créer `MainActivity.kt` minimal (remplacé au plan 01-03) : `class MainActivity : ComponentActivity()` dont onCreate appelle setContent avec un simple `Text(stringResource(R.string.app_name))` — juste de quoi prouver le chemin manifest → Application → Activity → écran.

Créer les ressources : `values/strings.xml` avec `app_name` = « ShortifyLocal » (D-07 — pas « ShortifyLocal AI ») ; `values/themes.xml` avec `style name="Theme.ShortifyLocal" parent="android:Theme.Material.Light.NoActionBar"` et `values-night/themes.xml` avec `parent="android:Theme.Material.NoActionBar"` (thèmes framework — pas de dépendance material XML) ; icône launcher adaptative par défaut façon Android Studio : `values/ic_launcher_background.xml` (couleur `#E4590C`), `drawable/ic_launcher_foreground.xml` (vector 108dp, glyphe blanc simple — triangle lecture path M44,34 L74,54 L44,74 Z), `mipmap-anydpi-v26/ic_launcher.xml` + `ic_launcher_round.xml` (adaptive-icon background couleur + foreground drawable — minSdk 33 donc aucun PNG requis).
  </action>
  <verify>
    <automated>cd "D:/projets/android/locut" && ./gradlew :app:assembleDebug</automated>
    <fails_when>non-zero exit, or "BUILD FAILED" in output</fails_when>
    <automated>cd "D:/projets/android/locut" && test -f app/build/outputs/apk/debug/app-debug.apk && ls -la app/build/outputs/apk/debug/app-debug.apk</automated>
    <fails_when>app-debug.apk absent (test non-zero exit)</fails_when>
    <automated>cd "D:/projets/android/locut" && grep -rn "com\.arthenica" settings.gradle.kts build.gradle.kts app/build.gradle.kts gradle/ | wc -l</automated>
    <fails_when>the count is greater than 0</fails_when>
    <automated>cd "D:/projets/android/locut" && ! grep -q "<uses-permission" app/src/main/AndroidManifest.xml</automated>
    <fails_when>non-zero exit (a uses-permission element is declared in the manifest)</fails_when>
  </verify>
  <acceptance_criteria>
    - `./gradlew :app:assembleDebug` termine sur BUILD SUCCESSFUL et produit app/build/outputs/apk/debug/app-debug.apk
    - Le manifest ne déclare aucune permission, aucun attribut de trafic en clair ; `android:supportsRtl="true"` et `android:allowBackup="false"` présents
    - `ShortifyLocalApp` implémente Configuration.Provider via la propriété Kotlin (la compilation même en est la preuve — la forme méthode ne compilerait pas)
    - Le bloc `room { schemaDirectory("$projectDir/schemas") }` est présent dans app/build.gradle.kts
    - La dépendance ffmpeg-kit (coordonnée vivante 8.1.7) est résolue par le build (le premier assembleDebug la télécharge)
    - `app_name` = ShortifyLocal via ressource, aucune chaîne en dur dans MainActivity
  </acceptance_criteria>
  <done>APK debug produit depuis le socle épinglé ; chemin manifest → Application Hilt → Activity prouvé par un build vert (D-07 : label ressource « ShortifyLocal »).</done>
</task>

<task type="auto">
  <name>Task 3: Verrou supply-chain — checksum de l'AAR résolu + audit inline-versions</name>
  <files>gradle/libs.versions.toml</files>
  <read_first>
    - .planning/phases/01-chafaudage-d-cisions-bloquantes/01-RESEARCH.md (Package Legitimacy Audit ; Validation Architecture R6 ; « Don't Hand-Roll » ligne checksum)
    - docs/ERRATA-2026-09-05.md (E2 — coordonnée fork + le checksum de référence est celui consigné en commentaire du catalogue à la tâche 1)
    - gradle/libs.versions.toml (état courant après tâche 1)
  </read_first>
  <action>
Vérifier indépendamment l'intégrité de l'AAR du fork résolu par la tâche 2 : (1) localiser l'AAR dans le cache Gradle (`~/.gradle/caches/modules-2/files-2.1/dev.ffmpegkit-maintained/ffmpeg-kit-full-gpl/8.1.7/…/*.aar`) — sa présence sous modules-2 prouve la résolution Maven Central (aucun autre dépôt n'est déclaré) ; (2) calculer `sha256sum` de ce fichier et comparer à la valeur consignée en commentaire du catalogue `374d3734755fd4a4f2241da23b93ab0b3bcae9bd39f01cf3bf29a5731914658b` ; (3) contre-vérification indépendante : télécharger `https://repo1.maven.org/maven2/dev/ffmpegkit-maintained/ffmpeg-kit-full-gpl/8.1.7/ffmpeg-kit-full-gpl-8.1.7.aar.sha256` et comparer aux deux valeurs précédentes. En cas de MISMATCH : NE PAS corriger le catalogue à la main — STOP et escalader au propriétaire de spec (le checksum est une décision épinglée, pas un ajustement local).

Auditer ensuite « zéro version inline » : grep des littéraux de version dans settings.gradle.kts, build.gradle.kts, app/build.gradle.kts — la SEULE occurrence autorisée est le resolveur foojay 1.0.0 dans le bloc plugins du settings (exception documentée, limitation Gradle). Vérifier aussi que `newPipeExtractor` est une entrée catalogue SANS dépendance déclarée dans app (le repo JitPack n'existe pas en P1) et qu'aucune entrée catalogue des phases ultérieures (ads, ump, mlkit, media3, okhttp, coil, security-crypto, serialization) n'est référencée par app/build.gradle.kts.
  </action>
  <verify>
    <automated>cd "D:/projets/android/locut" && AAR=$(find "$HOME/.gradle/caches/modules-2/files-2.1/dev.ffmpegkit-maintained" -name '*.aar' | head -1) && echo "$AAR" && sha256sum "$AAR"</automated>
    <fails_when>no AAR found (empty path) or the printed hash is not 374d3734755fd4a4f2241da23b93ab0b3bcae9bd39f01cf3bf29a5731914658b</fails_when>
    <automated>cd "D:/projets/android/locut" && curl -s https://repo1.maven.org/maven2/dev/ffmpegkit-maintained/ffmpeg-kit-full-gpl/8.1.7/ffmpeg-kit-full-gpl-8.1.7.aar.sha256</automated>
    <fails_when>empty output (no network answer) or a hash differing from the catalog comment value</fails_when>
    <automated>cd "D:/projets/android/locut" && grep -rnE '"[0-9]+(\.[0-9]+)+"' settings.gradle.kts build.gradle.kts app/build.gradle.kts | grep -v foojay | wc -l</automated>
    <fails_when>the count is greater than 0 (an inline version outside the documented foojay exception)</fails_when>
  </verify>
  <acceptance_criteria>
    - Le sha256 de l'AAR en cache Gradle = 374d3734755fd4a4f2241da23b93ab0b3bcae9bd39f01cf3bf29a5731914658b = la valeur publiée sur repo1.maven.org (trois sources d'accord)
    - L'AAR résout depuis Maven Central uniquement (chemin modules-2 ; aucun dépôt tiers déclaré)
    - Aucune version inline hors exception foojay dans les fichiers de build
    - En cas de mismatch détecté : aucune correction locale — escalade consignée
  </acceptance_criteria>
  <done>Checksum du fork consigné = checksum de l'AAR réellement résolu = checksum publié (SPEC R6, critère d'acceptation 3) ; approvisionnement Maven Central prouvé.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Maven Central → build | Tout binaire résolu par Gradle entre dans l'APK (supply chain) |
| Manifest système | Composants exposés au système Android (activities, providers) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-SC | Tampering | Résolution `dev.ffmpegkit-maintained:ffmpeg-kit-full-gpl:8.1.7` (slopsquatting/miroir de l'artefact mort) | high | mitigate | Coordonnée épinglée dans le catalogue unique + SHA-256 consigné + tâche 3 : triple vérification (cache Gradle vs repo1.maven.org vs commentaire catalogue) ; résolution Maven Central uniquement, pas de JitPack en P1, pas de -SNAPSHOT |
| T-01-01 | Tampering | Résolution des plugins (AGP/Kotlin/Hilt via gradlePluginPortal/google) | medium | mitigate | Versions épinglées E2 dans le catalogue unique, includeGroupByRegex sur google(), FAIL_ON_PROJECT_REPOS (aucun dépôt déclaré dans les modules) |
| T-01-02 | Elevation of privilege | Composants exportés du manifest | low | mitigate | Seule MainActivity est exported (intent-filter MAIN/LAUNCHER requis) ; InitializationProvider exported=false ; aucun deep link en P1 |
| T-01-03 | Information disclosure | Trafic réseau de l'app P1 | low | accept | Aucune permission, aucun attribut cleartext, aucune API réseau en P1 (HTTPS-only par défaut ; durcissement explicite SEC-04 en Phase 7) |
| T-01-04 | Information disclosure | Sauvegarde Android de la préférence thème | low | accept | `allowBackup="false"` posé dès P1 (forward-compat SEC-04 gratuite) ; la seule donnée P1 est la préférence thème, non sensible |
</threat_model>

<verification>
- `./gradlew :app:assembleDebug` vert depuis un état sans Gradle global (wrapper seul)
- Catalogue = errata E2 1:1 (grep positifs des 21 versions) ; checksum fork triple-source vérifié
- Manifest : 0 permission, 0 cleartext, supportsRtl, allowBackup=false
- 0 référence à l'ancienne coordonnée vendeur dans tout fichier de build (commentaires inclus)
</verification>

<success_criteria>
Le socle build de PROJ-01 est verrouillé : wrapper + catalogue épinglé + module app qui build ; l'APK existe et la supply-chain du fork est prouvée (résolution + checksum). Les plans 01-02/01-03 construisent le code applicatif dessus, 01-04 l'installe sur émulateur/appareil.
</success_criteria>

<output>
Create `.planning/phases/01-chafaudage-d-cisions-bloquantes/01-01-SUMMARY.md` when done
</output>
